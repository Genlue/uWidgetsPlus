# uWidgets 项目解构报告

- **项目**：uWidgets（macOS 风格桌面小组件应用）
- **版本**：1.3.2（个人使用定制版）
- **基线**：官方 [creewick/uWidgets](https://github.com/creewick/uWidgets) v0.6.0
- **技术栈**：C# / .NET 8 / Avalonia 11.1.3（Win32）/ System.Text.Json / MSBuild
- **位置**：`C:\Users\25659\Desktop\Project\uWidgets`
- **交付形态**：单个 `uWidgets.exe`（框架依赖，需 .NET 8 运行时；目标机未装时提示安装）

---

## 1. 项目定位

把常用信息（时钟、天气、日历、提醒、监控、便签）以半透明 macOS 风格小组件常驻桌面。
本定制版相对官方：
1. **新增自定义 Folders 小组件**（文件夹快捷方式/监视文件夹实时刷新）；
2. **语言精简**为简体中文 + 英语（默认简体中文）；
3. **单文件打包**：全部小组件内嵌进 exe，运行时自解压到 `%LocalAppData%\uWidgets`，保留热更新能力；
4. **新增网格管理模式**（1.1.0）：用户可手动将桌面划分为 m×n 个正方形格子的自定义网格，
   网格不要求全覆盖桌面，可自由移动位置与调整格子大小（百分比存储，适配不同分辨率）；
5. **组件大小重塑**（1.1.0）：网格模式下组件自适应格子大小（不可自由拖拽调整），
   内容支持自定义缩放、自定义与网格线的边距与圆角；
6. **外观-字体列出系统全部字体**（1.1.0）：字体选择器枚举 Windows 已安装的
   全部字体族（含捆绑的 Inter）并支持搜索；
7. **修复已知问题**（1.1.0）：更新源可配置、天气请求代理可配置（默认直连）、
   小组件包解压标记改为内容哈希；
8. **毛玻璃恢复 OS 级实时模糊**（1.2.0 修订）：应用层自绘"强度可调"毛玻璃会让玻璃变成
   壁纸静态快照（动态壁纸不再跟手），故已**移除模糊强度设置**并恢复 OS `AcrylicBlur`
   背景（DWM 逐帧采样桌面合成，动态壁纸实时跟手）；
9. **高光描边成为毛玻璃的可选项**（1.2.0）：「描边毛玻璃」并入「毛玻璃」主题，
   描边颜色（RGBA）与粗细（DIP）开放为设置，默认关闭（宽度 0）；
   左上角与右下角最浓、沿每条边渐变到右上角与左下角为零；
10. **纯色模式隐藏玻璃设置**：选中纯色时设置页不显示（也不应用）描边相关项。

---

## 2. 总体架构（三层）

```
┌─────────────────────────────────────────────────────────┐
│  uWidgets（主程序，WinExe）                              │
│  · Avalonia UI：小组件窗口/设置/主题/更新检查             │
│  · DI 容器（Microsoft.Extensions.DependencyInjection）   │
│  · WidgetFactory：读 layout.json → 反射创建小组件窗口    │
│  · WidgetBundle：单文件模式自解压（本定制新增）           │
└───────────────────────┬─────────────────────────────────┘
                        │ 引用 + 宿主提供 Avalonia 运行时
┌───────────────────────▼─────────────────────────────────┐
│  uWidgets.Core（小组件 SDK）                             │
│  · 插件契约：WidgetInfoAttribute / LocaleAttribute       │
│  · AssemblyProvider：AssemblyLoadContext 加载/卸载插件   │
│  · IWidgetFactory / IDataProvider / 设置模型（Theme 等） │
└───────────────────────┬─────────────────────────────────┘
                        │ 运行时从 Widgets\ 目录动态加载（不复制引用）
┌───────────────────────▼─────────────────────────────────┐
│  Widgets\ 目录（7 个小组件 DLL）                         │
│  Clock · Calendar · Weather · Monitor · Notes            │
│  Reminders · Folders（自定义）                           │
└─────────────────────────────────────────────────────────┘
```

### 插件加载机制（关键设计）
- 主程序启动时 `AssemblyProvider` 扫描 `Widgets\*.dll`，读取每个 DLL 的
  `WidgetInfoAttribute`（视图类型/模型类型/设置类型）与 `LocaleAttribute`（图标、本地化）；
- 创建小组件时用**反射** `ActivatorUtilities.CreateInstance` 激活视图类型
  （DI 自动注入 `IWidgetLayoutProvider`、模型等构造函数参数）；
- 每个插件 DLL 放在独立的 `AssemblyLoadContext`（`PluginLoadContext`）中，
  通过 `AssemblyDependencyResolver` + 各插件自己的 `*.deps.json` 解析依赖
  （Avalonia/Core 优先从宿主默认上下文复用；插件自带包如 System.Management、TimeZoneNames）；
- 支持热卸载（`UnloadAssembly`）与"同名取最高版本"。
  ⚠️ 因依赖反射激活，**发布时绝不能开启 Trimming**。

---

## 3. 目录结构解构

```
C:\Users\25659\Desktop\Project\uWidgets
├── build.ps1                     ← 一键构建：单 exe（默认）/ 便携 zip（-Portable）
├── BUILDING.md                   ← 构建与模块化说明
├── Directory.Packages.props      ← 集中包版本管理（CPM，12 个包，唯一版本源）
├── scripts/
│   └── build-widgets-bundle.ps1  ← 小组件打包脚本（被 MSBuild 目标调用）
├── dist/                         ← 构建产物（gitignore）
│   └── win-x64/uWidgets.exe      ← 单文件交付物（≈51 MB）
├── .github/                      ← 官方 CI 与图片素材（保留参考）
└── src/
    ├── uWidgets.sln
    ├── uWidgets/                 ← 主程序（约 45 个源码文件）
    │   ├── Program.cs            ← 入口：先解压 → 再启动 Avalonia
    │   ├── App.axaml(.cs)        ← 应用初始化 + DI 装配
    │   ├── AssemblyInfo.cs       ← 主版本号（1.6.6）
    │   ├── Services/
    │   │   ├── WidgetBundle.cs   ← 【定制】单文件自解压逻辑（.version = zip 内容哈希）
    │   │   ├── WidgetFactory.cs  ← layout → 窗口创建
    │   │   ├── GridService.cs    ← 【定制】三种网格模式（自定义/虚拟/自由）吸附
    │   │   ├── GridMetrics.cs    ← 【定制】网格百分比 → 像素换算（正方形格子）
    │   │   ├── SystemFonts.cs    ← 【定制】Windows 系统字体枚举（注册表 + 捆绑 Inter）
    │   │   ├── GridService / ThemeService / LocaleService
    │   │   ├── UpdateService / UpdateTimer / TimerService / InteropService
    │   ├── ViewModels/           ← 设置页 MVVM（General/Appearance/Advanced/…）
    │   ├── Views/                ← 设置窗口、小组件窗口、网格编辑器、编辑弹窗、控件
    │   │   └── GridEditor.axaml(.cs) ← 【定制】网格可视化编辑器（拖动移动/调格子）
    │   └── Locales/              ← Locale.resx(英) + Locale.zh-Hans.resx
    ├── uWidgets.Core/            ← 小组件 SDK（约 28 个文件）
    │   ├── Const.cs              ← 【定制】数据目录双模式（便携/打包）
    │   ├── Services/
    │   │   ├── AssemblyProvider.cs  ← 插件加载核心
    │   │   └── ProxySettings.cs  ← 【定制】HTTP 代理配置（直连/系统/自定义）
    │   ├── Models/Attributes/    ← WidgetInfoAttribute / LocaleAttribute
    │   ├── Models/Settings/      ← AppSettings/Grid/GridMode/Dimensions(ContentScale)…
    │   └── Interfaces/           ← IWidgetFactory / IDataProvider / IGridService …
    └── Widgets/                  ← 小组件模块，每个子目录一个独立 csproj
        ├── Clock/                ← 模拟/数字/世界时钟（5 种样式）
        ├── Calendar/             ← 月历/日期
        ├── Weather/              ← Open-Meteo 天气/空气/UV/日出日落（代理可配置）
        ├── Monitor/              ← CPU/内存/磁盘/网络（WMI）
        ├── Notes/                ← 便签
        ├── Reminders/            ← 提醒列表
        └── Folders/              ← 【自定义】文件夹小组件
```

---

## 4. 关键流程解构

### 4.1 启动流程
```
Program.Main
 ├─ WidgetBundle.ExtractIfNeeded()        ← ① 先处理数据目录（见 4.3）
 ├─ BuildAvaloniaApp()（读 appSettings 初始化 Win32 平台选项）
 └─ StartWithClassicDesktopLifetime
     └─ App.OnFrameworkInitializationCompleted
         ├─ DI：AppSettingsProvider / AssemblyProvider / WidgetFactory …
         ├─ localeService.SetCulture(zh-Hans) + themeService.Apply
         ├─ WidgetFactory.Create() 读 layout.json → 每个 widget 反射创建并 Show()
         └─ 无 widget 或带 --settings 参数 → 打开设置窗口
```

### 4.2 单文件打包流程（构建期）
```
dotnet publish -c Release -r win-x64 --self-contained false
 └─ MSBuild 目标 BuildWidgetsBundle（钩子：AssignTargetPaths）
     ├─ scripts/build-widgets-bundle.ps1：逐个小程序 dotnet publish
     │   （扁平输出到 staging，删除 pdb/xml/runtimeconfig，保留 deps.json）
     ├─ 压缩为 widgets.zip（≈8 MB，含 7 个 widget + 语言卫星资源 + 原生库 + 依赖 DLL）
     └─ 作为 EmbeddedResource 嵌入：uWidgets.Resources.widgets.zip
 └─ PublishSingleFile + IncludeNativeLibrariesForSelfExtract
     → 单 exe（Avalonia 原生库运行时自解压到临时目录）
```
另：`appSettings.json` / `layout.json` 默认值同样以 EmbeddedResource 内嵌。

### 4.3 数据目录双模式（本定制核心）
`Const.DataFolder` 启动即判定：
| 模式 | 判定条件 | 数据位置 | 说明 |
|------|---------|---------|------|
| 便携 | exe 旁存在 `Widgets\` 目录 | exe 所在目录 | 官方/开发模式，行为不变 |
| 打包 | 无 `Widgets\` 目录 | `%LocalAppData%\uWidgets\` | 首次运行解压内嵌资源 |

`WidgetBundle.ExtractIfNeeded()`：
1. `appSettings.json` / `layout.json` 缺失时写出默认值（**不覆盖用户已有配置**）；
2. widget 包仅在版本标记（`.version` 文件 = 内嵌 zip 的 **SHA-256 内容哈希**，1.1.0 起）变化时重新解压；
   即任何 widget 变更（新 DLL、改版本号、新资源）都会触发重解压，而不只是主 exe 版本变化；
3. 解压带路径穿越防护。

### 4.4 热更新
打包模式下 widget 是 `%LocalAppData%\uWidgets\Widgets\` 下的普通磁盘文件，
替换某个 DLL 后重启应用即生效；内嵌包哈希不变则不会覆盖用户替换的版本。
（若开发者重新构建了 exe 且 widget 包内容有变，哈希变化会覆盖本地替换版本——发布新版本时预期行为。）

---

## 5. 自定义 Folders 小组件解构（0.2.0）

| 文件 | 职责 |
|------|------|
| `Models/FolderModel.cs` | 模型（26 个配置项：固定项/监视目录/网格列表/名称样式…），存于 layout.json |
| `Views/Folder.axaml(.cs)` | 视图 + 交互：FileSystemWatcher 实时刷新、拖放（OLE + WM_DROPFILES）、点击打开 |
| `Views/Settings/FolderSettings.axaml(.cs)` | 设置面板（19KB） |
| `Services/FolderIconService.cs` | Windows 图标提取（SHGetFileInfo 等 Win32 互操作，25KB） |
| `Services/WidgetOleDropTarget.cs` | OLE 拖放目标实现（把文件拖进 widget 即固定） |
| `AssemblyInfo.cs` | `[WidgetInfo(Folder, FolderModel, FolderSettings)]` + `[Locale(...)]` 注册 |

两种模式：**固定项**（Items 路径列表，含自定义名称）与**监视文件夹**（WatchFolder，
实时列出内容，支持排序/隐藏扩展名/多行换行等）。

---

## 6. 配置体系

### appSettings.json（默认值内嵌于 exe）
- `Theme`：暗色/强调色/不透明度/单色/原生边框/字体（默认 `zh-Hans` 下微软雅黑；
  **外观-字体**枚举系统安装的全部字体族并支持搜索，另含应用捆绑的 Inter）
- `Theme.Surface`：表面材质（`0`=毛玻璃、`1`=纯色、`2`=液态玻璃【保留】、`3`=描边毛玻璃【旧配置兼容】）
- `Theme.OutlineColor` / `Theme.OutlineWidth`：毛玻璃的**高光描边颜色**（RGBA HEX，
  默认 `#B3FFFFFF` 柔和灰白，含透明度）与**粗细**（DIP，默认 **0 = 关闭**）。
- ~~`Theme.BlurStrength`（模糊强度）~~：1.2.0 修订后**已移除**（见 §8.4）。
- `Templates`：历史遗留（官方 8 套模板）——界面实际使用 `AppearanceViewModel.SurfaceTemplates`
  硬编码的 **3 套表面预设**（毛玻璃 / 纯色 / 描边毛玻璃）
- `Layout`：`GridMode`（0=自定义网格【默认】、1=虚拟网格、2=自由布局）/ 吸附 / 锁定
- `Dimensions`：Size/Margin/Radius/**ContentScale**（内容缩放 0.5×–2×，默认 1.0）
- `Region.Language`：`zh-Hans`（仅支持 zh-Hans / en）
- `Grid`（自定义网格模式专用，**百分比存储**，适配不同分辨率）：
  - `Columns`（n）、`Rows`（m）：行列数
  - `XPercent`：网格区域左上角 X（工作区宽度的 %）
  - `YPercent`：网格区域顶部线位置（工作区高度的 %）
  - `CellPercent`：格子边长（工作区**宽度**的 %；纵向沿用横向像素值，**保证正方形**）
- `UpdateUrl`：自定义更新源（null/空 = 禁用更新检查，默认禁用）
- `HttpProxy`：`""`/null = 直连（默认，绕过失效的系统代理）、`"system"` = 系统代理、其他 = 代理 URL
- `RunOnStartup` / `IgnoreUpdate`

### 网格管理模式（1.1.0 新增，**默认启用**）
- **自定义网格**（默认）：用户在桌面划分 m×n 正方形格子的网格区域（不要求全覆盖桌面），
  设置页可调行列数/位置/格子大小百分比，或打开**全屏网格编辑器**（`GridEditor`）：
  在桌面全屏蒙版上**拖动网格区域**移动整片网格，**右键网格**弹出面板修改 m×n 与格边，
  变更实时保存，关闭后组件自动吸附并按跨度填入格子；
- **虚拟网格**：官方行为，组件按 `Size` 虚拟单位吸附；
- **自由布局**：无网格，组件自由放置。

### layout.json
```json
[{"Type":"Clock","SubType":"AnalogI","X":31,"Y":387,"Width":155,"Height":155,
  "Settings":{"ShowSeconds":true,"ShowDate":false,"Use24Hours":false,"TimeZoneId":null}},
 {"Type":"Folders","SubType":"Folder","X":1965,"Y":90,"...","Settings":{"Items":[...]}}]
```
- `Type`=插件程序集名，`SubType`=WidgetInfoAttribute 中的视图名，`Settings` 反序列化为各 widget 模型；
- 自定义网格模式下组件 `Width/Height` = 格子跨度 × 格子像素（1×1/2×2/4×2/4×4/8×4，
  右键菜单选择），组件**不可自由拖拽调整大小**，而是自适应网格；位置强制吸附到最近格子；
  组件与网格线的边距由 `Dimensions.Margin` 控制，圆角由 `Dimensions.Radius` 控制，
  内容缩放由 `Dimensions.ContentScale` 控制（基准 72px 设计尺寸）。

---

## 7. 构建与发布

```powershell
.\build.ps1                      # Release + win-x64 单 exe → dist\win-x64\uWidgets.exe
.\build.ps1 -Runtime win-arm64   # ARM64
.\build.ps1 -Portable            # 额外产出官方式便携 zip（exe + Widgets + json）
```
- **包版本**：`Directory.Packages.props` 一处集中（Avalonia 11.1.3 等）。
- **主版本**：`src/uWidgets/AssemblyInfo.cs`（1.2.0）。
- **小组件版本**：各自 `AssemblyInfo.cs`（Clock 1.1.1、Folders 0.2.0、Weather 1.4.1 …）。
- 环境要求：Windows + .NET 8 SDK；分发目标机需 .NET 8 运行时（框架依赖模式）。
- 新增小组件：复制 `src/Widgets/Clock/` 骨架 → 注册 `WidgetInfo` → `dotnet sln add` → 重新 `build.ps1`（打包目标自动发现新模块）。

---

## 8. 已知问题与后续建议

### 8.1 网格与组件系统修复（当前构建）

本修复轮针对三个网格/组件缺陷（均已在 175% 缩放的 2560×1600 实机上验证）：

1. **网格 DPI 单位错配（缝隙/重叠根源）**：网格度量（`GridMetrics`）按工作区**物理像素**计算，
   而 `window.Width/Height` 是 **DIP** —— 缩放屏（如 175%）下组件窗口物理尺寸 = 格子 × 1.75，
   组件既不贴合格子又彼此错位。
   ✅ 已修复：所有**尺寸**运算统一换算为 DIP（`尺寸 = 格子物理像素 ÷ 屏幕缩放`），
   **位置吸附**仍按物理像素（`SnapPosition` 不变）。格子重新紧密贴合（margin=0 时相邻卡片无缝相接）。
   - **旧布局迁移**：旧版保存的尺寸是"物理格子数"（如 230 px），按 DIP 解释会漂移成 2×2；
     `GetSpan()` 现按"物理/DIP 双解释择优"（取离整数跨度更近者）恢复，旧 230px 布局无损还原为 1×1。
2. **内容放缩作用于整张卡片**：`ContentScale` 原通过缩小 `ContentPresenter` 实现，
   整卡（含边距）被缩放 → 卡片与格子脱节、格子间出现无法用边距消除的缝隙。
   ✅ 已修复：`ContentScale` 只对**卡片内的内容**施加中心缩放渲染变换
   （1.0 = 内容填满卡片，0.5×–2× 仅缩放内容，超出部分被卡片裁剪）；
   **卡片大小只由格子和"高级 → 形状 → 小部件边距"决定**（`格子 − 2×边距`）。
3. **毛玻璃覆盖整个网格格**：Avalonia 的 `TransparencyLevelHint=AcrylicBlur` 是**窗口级**背景，
   玻璃（模糊+暗色涂层）铺满整个窗口（即整格），圆角之外的网格区域也被模糊。
   ✅ 已修复：`InteropService.SetWidgetRegion` 用 `SetWindowRgn` 把原生窗口
   （含 OS 级模糊背景）裁剪为「格子 − 边距 + 圆角」的圆角矩形：
   - 毛玻璃只存在于组件卡片本身区域，**边距与圆角都由设置实时生效**；
   - 卡片外的网格区域变为**真正透明且点击穿透**（点击落在格子间隙直接落到桌面）；
   - 原生边框模式（`UseNativeFrame`）不裁剪（避免裁掉系统边框）。

后续建议：
- 多屏/混合缩放：网格基于主屏工作区；副屏组件吸附时按所在屏重算（现状为吸附回主屏网格）。
- 若 `SetWindowRgn` 圆角边缘在超低分辨率/特殊 DPI 下出现锯齿，可改用 DWM 圆角偏好
  （代价是圆角大小由系统固定，无法跟随 `Dimensions.Radius`）。

4. **组件内容塌缩错位（内容缩放学重写引入）**：1.1.0 把 `ContentPresenter` 由旧的
   「显式 Width/Height = 窗口÷scaleFactor」改为仅依赖内容自然尺寸后，在 `Center` 对齐 +
   `DataTemplate→ContentControl` 包装下，内容以**无界限宽度**测量（内容自然宽度）：
   Monitor 小组件的环形+百分比收缩到卡片左侧，卡片未充满格子，且**图标位置随下方数字宽度漂移**
   （2% 与 14% 文本宽度不同 → 内容块宽度不同 → 居中偏移不同）；其他小组件视图同样受影响。
   ✅ 已修复：新增 `UpdateContentSize()`（ctor / Opened / Resized / Scale 时调用），
   `ContentPresenter` 显式设为「格子 − 2×边距」的尺寸；内容重新充满整卡。
   已在 192×192 与 158×158 窗口尺寸下 headless 渲染验证：环形/文本几何与数值无关，
   与图库预览布局一致。

5. **天气预报小部件只显示大号温度**：预报视图按自身尺寸切换 LARGE/WIDE/SMALL/TINY 四种布局，
   阈值以 150 DIP 为界；网格模式在缩放屏（如 175%）下 2×2 格子只有约 146 DIP → 落入 TINY
   （仅显示温度，城市/图标/天气/高低温全部缺失）。且 SMALL 布局是固定尺寸设计，
   比格子更小时会被裁切而非缩放。
   ✅ 已修复：`ForecastSmall` 外包 `Viewbox`（Uniform + DownOnly + 左对齐，
   自适应格子、不放大、不居中漂移）；切换阈值降至 75 DIP（仅真正 1×1 单格保留 TINY 布局）。
   已在 172×172 与 192×192 窗口下 headless 渲染验证：完整显示
   城市/大号温度/天气图标/状况/高低温，与图库预览布局一致。

6. **尺寸档位数字化 + 智能自适应**：右键菜单由预设档（1×1/小 2×2/中 4×2/大 4×4/特大 8×4）
   改为**完整数字矩阵 1×1 … 4×4**（共 16 档，含任意矩形如 1×2、2×3、3×4…），
   子菜单按「尺寸 / 网格尺寸」展开；`ResizeSize("列,行")` 统一处理。
   - Monitor 组件按形状自适应：横屏（宽 > 1.25×高，如 2×1/3×2）→ 固定比例画布
     环形左 + 百分比右整体缩放；方正/竖排 → 环形上 + 百分比下；单格小卡 → 仅环形；
   - Forecast 按形状选布局（SMALL 现自适应缩放；WIDE 高度阈值 150→140 适配 4×2）；
   - 其余组件（时钟/日历/便签/提醒/文件夹）为拉伸或 Viewbox 布局，天然适配任意矩形。
   已在 2×2/2×1/1×2/2×3/3×2/1×1 等 10 种形状下 headless 渲染验证。

### 8.2 更早的已知问题（已修复/说明）

1. ~~**更新检查**指向官方 GitHub 仓库，个人 fork 会误报新版本。~~
   ✅ 已修复（1.1.0）：新增 `UpdateUrl` 设置，**默认禁用更新检查**（null），
   在"高级 → 网络"中配置自有更新源（如 `https://github.com/owner/repo/releases/latest`）后自动启用；
   `IgnoreUpdate` 跳过版本机制保留。
2. ~~**天气代理**：Weather 请求经系统代理（历史日志中 127.0.0.1:7890 拒连），代理软件未开启时天气显示失败。~~
   ✅ 已修复（1.1.0）：天气与更新请求默认**直连**（绕过系统代理，避免残留代理拒连），
   可在"高级 → 网络"中选择系统代理或自定义代理 URL。
3. ~~**版本策略**：`.version` 标记只跟随主 exe 版本变化，改小组件 DLL 版本不会触发重新解压。~~
   ✅ 已修复（1.1.0）：`.version` 改为内嵌 widget 包 zip 的 **SHA-256 内容哈希**，
   任何 widget 包变化都会自动重新解压（手动删除 `Widgets\.version` 可强制刷新）。
4. **旧便携版迁移**：把旧目录 `layout.json` / `appSettings.json` 复制到
   `%LocalAppData%\uWidgets\` 即可无损迁移；或将新 exe 直接放入旧目录走便携模式。
5. **历史崩溃**：旧版 crash_log（"Failed to create an instance of Widget"）在 1.1 构建的
   冒烟测试中未复现；若再出现，检查 `%LocalAppData%\uWidgets\crash_log.txt`。
6. **CI**：官方 `publish.yml` 仅作参考（多运行时 zip 分发）；本定制以 `build.ps1` 为唯一构建入口。
7. **自定义网格与多屏**：网格百分比基于主屏工作区；副屏上的组件切到自定义网格模式后会被吸附回主屏网格。

### 8.3 编辑会话修复（Reminders / Notes / ClickThroughTextBox）
 
本轮针对"清单改名崩溃 + 光标偏移一位"两个缺陷（Avalonia.Headless 探针回归验证）：
 
1. **清单改名崩溃（NullReferenceException）**：三个布局控件（`ListSmall`/`ListWide`/`ListLarge`）的后置代码用
   `(Parent as List)!` 转发事件——`List.OnSizeChanged`（尺寸切换）与 `Widget.OnWidgetLayoutUpdated`
   （保存后重建视图）会替换当前布局控件，替换后 `Parent` 为 null，任何随后的 LostFocus/Click →
   `(Parent as List)!` → NRE 崩溃。
   ✅ 已修复：`List` 构造时把自身引用注入布局控件（`new ListSmall(this, viewModel)`），不再依赖 Parent 查找；
   事件转发永远指向存活对象。
2. **保存时整个 widget 视图被重建**：`Widget.OnWidgetLayoutUpdated` 在 Settings 变化时用
   `ContentPresenter.Content = userControl()` 重建视图——编辑会话（光标/焦点/IME）被销毁，且标题失焦保存
   会先于勾选按钮 Click 触发重建，导致"点勾没反应"。
   ✅ 已修复：新增 `IWidgetSelfRefreshing` 契约；Reminders/Notes 视图实现之——布局变化时**原位刷新**
   （更新 VM 并通知绑定），不再重建控件；其他 widget 保持原有"从模型重建视图"路径（编辑对话框流程不变）。
3. **光标偏移一位**：激活编辑必须双击（单击穿透给 widget 拖拽），Avalonia 默认在双击时做**单词选择**，
   中文文本下收起选择后光标比点击位置偏左一位——第一次 Backspace 删除倒数第二个字。
   ✅ 已修复：`ClickThroughTextBox` 激活双击后把单词选择**收起到选中词的末端**（`SelectionEnd`），
   光标回到点击处；框内普通双击仍保留单词选择。无头验证：激活双击后光标 = 文本末尾。
4. **VM 只读 + 保存路径陈旧模型**：`RemindersViewModel.ListName`/`NoteViewModel.Title/Content` 为 getter-only，
   绑定写回被静默丢弃；保存时又从视图的陈旧字段重建模型，会覆盖其他框正在编辑的内容。
   ✅ 已修复：VM 属性可写（绑定写回直接更新模型），VM 成为会话期模型的唯一来源
   （`View.Model`），保存/刷新均以 VM 模型为基准；`UpdateModel` 不再更换 DataContext。
 
全部变更经 Debug/Release 构建 + `uw-caret-probe`（Avalonia.Headless + Skia + Inter，真实 List/Note 控件）回归：
崩溃路径、原位刷新、焦点保持、VM 写回、End/Backspace、双击收起全部通过。

### 8.4 1.2.0：毛玻璃恢复 OS 实时模糊 + 高光描边选项

**毛玻璃（OS 级实时，强度设置已移除）**
- 组件窗口的模糊来自 OS 背景（`Styles/Transparent.axaml` 设置 `AcrylicBlur`、
  WinUI Composition 宿主背景画笔）：DWM/合成器**逐帧**采样窗口背后的桌面合成画面，
  动态壁纸/轮播/视频壁纸均实时跟手；
- 曾尝试"应用层自绘毛玻璃 + 可调强度"（`BackdropService` 壁纸裁剪 + `BlurEffect` 半径
  0–30）：因模糊源是**静态位图快照**，动态壁纸不再跟手，且 OS 半径被 Avalonia 硬编码
  30px（`WinUIGaussianBlurEffect` 固定 STANDARD_DEVIATION），二者不可兼得 ——
  **已回滚**：`BackdropService.cs` 删除、`Theme.BlurStrength` 与设置页「毛玻璃强度」
  移除，`BackgroundOpacity` 恢复 = `OpacityLevel`；旧配置中的 `BlurStrength` 字段被忽略。

**高光描边（毛玻璃的可选项）**
- 「描边毛玻璃」主题并入「毛玻璃」：主题按钮回到两个（毛玻璃 / 纯色），
  `SurfaceStyle.OutlinedAcrylic` 仅兼容旧配置；描边任意玻璃表面
  `OutlineWidth > 0` 即启用，默认 **0 = 关闭**；
- 描边为 **ConicGradientBrush**（0°=正上、顺时针；`GradientStop` 偏移为 0–1 的 360° 比例，
  角度须 `/360`）：**左上/右下最浓**（`Angle` 起点 = 左上角），沿每条边线性渐变，
  右上/左下恰为零；分段角宽 TL→TR / BR→BL 各 `2·α`、TR→BR / BL→TL 各 `180−2·α`
  （`α = atan2(宽/2, 高/2)`），任意宽高比贴合；描边画在卡片 `Border.BorderBrush`
  （位于自身背景之上、内容之下），与 OS 模糊层不冲突；
- 纯色主题：不应用且**设置页隐藏**描边设置（`ShowGlassSettings`）。





5. **光标偏移+输入崩溃（Avalonia 框架级已知缺陷，11.1/11.2/11.3/master 均未修复）**：
   混合 Inter+CJK 回退字体下命中测试/光标索引漂移 +1：双击标题后光标显示在末字右侧，
   实际索引却在前一位（Backspace 删倒数第二个字）；且该漂移会在 IME 组合时把光标索引推到
   文本长度之外，触发 `TextPresenter.CreateTextLayout` → `GetCombinedText` 的
   `String.Substring` 越界异常（**crash_log 实证**：`Index and length must refer to a location
   within the string`），布局阶段即崩溃。
   ✅ 已修复（应用层防护，不依赖上游修复）：
   - **索引钳制**：`ClickThroughTextBox` 监听 Text/CaretIndex/Selection 变化，光标/选区越界
     （<0 或 >文本长度）即回钳到合法区间——`GetCombinedText` 的 Substring 不再可能越界；
   - **末端点击置尾**：激活双击（隧道阶段处理，不依赖手势事件）时，点击点越过最后一个字形
     的右缘（以 TextLayout 实测文本宽度判定）→ 光标强制置于文本末尾，Backspace 删除最后一位；
   - 单词选择收起到末端（`SelectionEnd`）保持不变。

---

## 9. 1.3.0：外观细化（单色配色 / 主题名 / 纯色自定义 / 背景透明度修复）

1. **单色模式可选配色**：单色开启后新增"配色"下拉，可选 **黑白** 与 **强调色**（默认）。
   - 黑白：深色模式为白、浅色模式为黑（新增 `Styles/MonochromeBlackWhite.axaml`）；
   - 强调色：原行为（浅色用 `SystemAccentColorDark1`、深色用 `SystemAccentColorLight2`，
     `Styles/Monochrome.axaml` 不变）；
   - 旧配置的 `Monochrome: true`（无 `MonochromeVariant` 字段）映射为强调色，外观不变。
   - 1.3.2 修订：**强调色与文字色统一**。小组件存在两套前景色——强调色
     （标题/强调图标，`SystemControlForegroundAccentBrush`）与文字色
     （`SystemControlForegroundBaseHighBrush`）；此前黑白模式只覆盖文字色，
     标题仍保持强调色（两色未统一）。现强调色定义移入常驻 `Styles/Accent.axaml`
     （挂在样式集合，位于单色样式之前），黑白样式追加覆盖该强调色为黑/白——
     开启单色后两种颜色一律统一：选强调色则均为强调色，选黑白则均为黑白
     （含标题、图标、表针、文字）。
2. **主题按钮下方显示名称**：外观-应用主题 的预览下方显示"毛玻璃"/"纯色"
   （`ThemeButton.ThemeName`，本地化 zh-Hans / en）。
3. **纯色背景自定义**（仅纯色主题显示，`ShowSolidBackground`）：
   - 深色模式与浅色模式背景颜色分别可调（`Theme.SolidBackgroundDark/Light`，HEX）；
   - 每侧提供"应用到浅色 / 应用到深色"按钮，把当前所选颜色复制到另一方；
   - 默认值 = 原有效果：深色 `#2E2E2E`（黑灰）、浅色 `#FFFFFF`（白）；字段为 `null` 时
     回落到默认，旧配置自动兼容。
4. **纯色背景透明度修复**：纯色模式的 `WidgetBackground` 此前为全不透明（App.axaml 兜底
   画笔无 `Opacity`），不透明度滑块失效。现新增 `Styles/Solid.axaml`：背景画笔
   `Opacity="{DynamicResource BackgroundOpacity}"` 跟随不透明度设置。
   1.3.1 修订：**窗口透明 hint 改为代码本地值**（`Widget`/`Settings` 的
   `ApplyTransparencyHint()`，毛玻璃 → `AcrylicBlur`、纯色 → `Transparent`；
   样式里保留兜底）。此前由样式切换 hint 在运行时可能残留 OS 模糊背景
   （切换表面后不透明度一调，半透明卡片立刻透出模糊 → 看起来像毛玻璃）；
   本地值 + `DataChanged` 重新应用后，运行时表面切换确定性地重建原生透明度，
   已实机验证：玻璃=逐帧模糊、纯色=逐像素透明不模糊。
5. **颜色选择器全部换成 RGB 调色盘**（1.3.1）：去掉此前隐藏光谱/分量滑块的
   精简配置，恢复完整面板（2D 光谱 + R/G/B 滑杆 + 色相 + 十六进制值输入 + 色板），
   黑色可直接取到；仅「描边颜色」保留透明度修改（`IsAlphaVisible`）。
6. **背景颜色适用于毛玻璃与纯色两种材质**（1.3.1）：`Transparent.axaml` 的背景画笔
   与 `Solid.axaml` 一致采用每变体主题字典（深/浅色自定义颜色 × 不透明度），
   设置页「背景颜色」不再限定纯色模式，两种材质均可分别自定义深色/浅色背景；
   默认值仍为深色 `#2E2E2E`、浅色 `#FFFFFF`。
7. **颜色存储规范化**（1.3.1）：保存颜色一律写 `#AARRGGBB` 十六进制
   （`Color.ToString()` 可能返回命名色如 "White"，不便于新旧值解析回退）。

---

## 10. 1.3.x：设置窗口 macOS 红绿灯标题栏

1. **模型**：`TitleBarStyle` 枚举（`Native=0` 原生系统按钮 / `TrafficLights=1` macOS 红绿灯）与
   `TitleBarSize`（红绿灯直径，DIP）。`AppSettings` 新增两个可空字段——旧配置 `null` 回退
   `Native` / 14px，外观无损兼容。
2. **`TrafficLightTitleBar` 控件**（`Views/Controls/`，36px 栏，复刻 macOS/IconForge 设计）：
   - 三圆点 + 1px 描边：红 `#FF5F57`/`#E0443E`（关闭）、黄 `#FEBC2E`/`#D89E24`（最小化）、
     绿 `#28C840`/`#1FAD30`（最大化/还原），按下变暗（如红 `#D14E47`）；间距 = 直径 × 2/3
     （圆心距 5/3×直径），12px = macOS 标准；
   - 图标（lucide X / Minus / Maximize2 / Minimize2）默认隐藏，悬停/键盘聚焦 0.08s 渐显，
     仅被悬停的按钮显示；
   - `ApplyGeometry()` 按 `Size` 整体等比缩放（直径/圆角/间距/左偏移/图标），钳制 12–24px；
   - **失焦灰化**：窗口失焦 → 按钮变灰（`#808080`/`#C0C0C0`/`#9C9C9C`）+ 整组 55% 不透明度；
   - **交互**：空白处按下拖动窗口（按钮区除外），双击切换最大化；绿钮图标**跟随窗口真实状态**
     ——任何路径最大化（按钮/双击/Win+Up/拖顶边/任务栏快捷方式）都切换为还原图标，ToolTip 同步。
3. **集成**（`Settings` 窗口 `ApplyTitleBarStyle()`）：
   - `TrafficLights` → `ExtendClientAreaChromeHints = NoChrome`（隐藏系统按钮）+
     `ExtendClientAreaTitleBarHeightHint = 36`；`Native` → `Default` + 50；
   - 挂在 `appSettingsProvider.DataChanged`，运行时切换即时重建原生 chrome（Win32 平台层），无需重启；
   - `AppTitle`（原生栏标题）与 `TrafficBar` 互斥显示。
4. **外观页**：新增"标题栏"下拉（原生系统按钮 / macOS 红绿灯）；红绿灯样式下显示"大小"档位
   （12 / 14 / 16 / 18 / 20px，默认 14——比 macOS 标准 12px 略大，适配高 DPI），
   `ShowTitleBarSize` 控制显隐。
5. **本地化**：`TrafficLight_Close/Minimize/Maximize/Restore` + 设置页条目（zh-Hans / en）。

---

## 11. 1.4.0：导入/导出备份（高级 → 备份与恢复）

1. **范围**：单个 JSON 备份文件保存四类数据：
   - **软件设置**：`appSettings.json` 全量（主题/尺寸/代理/更新源/标题栏…）；
   - **网格配置**：`appSettings.json` 的 `Grid` 段（行列/位置/格子百分比）与 `Layout.GridMode`；
   - **卡片布局**：`layout.json` 每张卡片 X/Y/Width/Height（跨度与吸附位置）；
   - **卡片内容**：每张卡片的 `Settings` 模型（各小组件个性化配置）。
2. **格式**：`BackupData(Version, AppSettings, Layout)`，缩进 JSON 可人工阅读；字段名大小写不敏感；
   版本必须 = `CurrentVersion`（1），缺段/无效 JSON 直接拒绝导入——备份文件天然防误用。
3. **导出**（高级页"导出备份"按钮）：`SaveFilePicker` 写出 `uWidgets-backup.json`；
   `BackupService.Create() = (1, appSettingsProvider.Get(), layoutProvider.Get())`——只读快照，
   不触碰运行中的文件，零副作用。
4. **导入**（高级页"导入备份"按钮）：`OpenFilePicker` → `BackupService.Parse()` 校验 →
   `ConfirmDialog` 确认（"导入并重启" / "取消"）→ 经 `appSettingsProvider.Save(...)` 与
   `layoutProvider.Save(...)` 恢复（走 Provider 保持内存缓存 + `DataChanged` 监听同步，主题/标题栏
   等运行时即时生效）→ `AppRestart.Restart()` 重启重建全部卡片窗口。失败路径（无效文件/异常）
   弹提示对话框，不破坏现有配置。
5. **新增组件**：
   - `uWidgets.Core/Models/BackupData.cs`：备份记录模型与版本常量；
   - `uWidgets/Services/BackupService.cs`：序列化/解析/校验/读写；
   - `uWidgets/Services/AppRestart.cs`：应用重启辅助（设置窗口原"重启"逻辑抽出复用）；
   - `uWidgets/Views/ConfirmDialog.axaml(.cs)`：小型模态确认/提示对话框；
   - 高级页新增"备份与恢复"区块；`SettingsViewModel.SetCurrentPage` 为
     Advanced 注入 `ILayoutProvider`；本地化 11 个新条目（zh-Hans / en）。

---

## 12. 1.5.0：多屏显示与管理（v2 布局格式）

1. **存储结构（layout.json v2）**：单一 JSON 文件由数组改为一组按屏幕分组的配置：
   ```json
   { "Version": 2, "Screens": [ { "Id": "primary", "Layout": [] } ] }
   ```
   - `ScreensLayout(Screens, Version=2)`；每条 `ScreenLayout(Id, Key, Alias, DeviceName, Grid, ContentScale, Layout)`；
   - **Id**：屏幕稳定 GUID（首次出现时生成），屏幕别名/手动改绑/所有权都用它；
   - **Key**：自动身份 `"FriendlyName|WidthxHeight"`（如 `"DELL U2723QE|2560x1600"`）；
   - **旧 "primary" 条目（Key=null）**：兼容 v1 —— 存**桌面绝对坐标**，运行时匹配"当前主屏"；
     新屏幕条目坐标**相对该屏工作区原点**（v1 迁移不做——旧文件读取时自动包装进 primary 条目，
     拖动后自然转相对坐标并落到对应屏条目）；
   - `Grid`/`ContentScale` 留空 → 回落全局 `AppSettings.Grid` / `Dimensions.ContentScale`，再回落内置默认。
2. **屏幕识别（ScreenMatcher.Match）**：优先级依次为
   ① `DeviceName` 手动绑定（`"\\.\DISPLAY1"`，改绑专用）② `Key` 型号+分辨率 ③ 旧 primary 条目（当前主屏）；
   同型号双屏靠分辨率区分，仍分不清时用"多屏"页手动改绑（把某保存配置写到该屏的 `DeviceName`）。
3. **DisplayMonitorService（新增）**：Win32 `EnumDisplayDevices/EnumDisplaySettings` 取设备名+EDID 友名+分辨率，
   与 Avalonia `Screens` 按几何配对（分辨率精确匹配优先，再按枚举序占用）；常驻
   **WidgetAnchorWindow**（1×1 透明、移出屏幕外、不进 Alt-Tab）提供 TopLevel 锚点；每 1.5s 轮询
   屏幕签名（型号+分辨率+位置+DPI），变化即触发 `ScreensChanged`。
4. **热插拔**：拔屏 → `WidgetFactory.OnScreensChanged` 关闭该屏全部窗口（配置留盘）；重插 →
   按盘上配置原位重建；新屏默认空布局（首次放卡片时 `EnsureConfig` 建条目）。
5. **跨屏拖动**：`Widget.AfterMove` 检测窗口落入另一屏 → `TransferOwnership` 把该卡片从原屏布局移到
   目标屏（坐标改相对目标屏工作区、`IWidgetLayoutProvider.ScreenId` 重绑定）；拖动前先以**原屏**格子
   解析 span（防 2×2→1×1 塌缩），手动网格下按目标屏格子重算尺寸。
6. **每屏独立配置**：网格（`screen.Grid`，GridEditor 全屏覆盖被编辑屏幕）、内容缩放（`screen.ContentScale`，
   `Widget.Scale` 优先取所属屏）、布局；`layoutProvider.DataChanged` → 每个 Widget `OnLayoutDataUpdated`
   原地应用网格/缩放变化，无需重启。
7. **备份 v2（全量 + 单屏）**：`BackupData(Version=2, AppSettings, Screens)`；
   - 全量导出 = 全局设置 + 所有屏；**单屏导出**（`BackupService.CreateSingle`）= 全局设置 + 一条屏幕配置；
   - 导入：按 Key/型号自动匹配，缺匹配落主屏，管理页可手动改绑；分辨率不同的屏幕导入时坐标按比例缩放；
   - **v1 备份拒绝导入**（Version≠2 抛 `FormatException`）；恢复后 `AppRestart` 重建窗口。
8. **多屏管理页（设置 → 多屏，新增）**：列出每块已连接屏（别名/型号/分辨率/DPI/组件数/内容缩放下拉/
   绑定下拉），每屏可：编辑网格、导出该屏、导入该屏、命名别名、手动改绑；另列出"已保存配置但未连接"
   的屏，可导出/删除；页面订阅 `ScreensChanged` 实时刷新。
9. **接线**：`App.axaml.cs` 注册 `DisplayMonitorService` + 创建锚窗 + `ScreensChanged → OnScreensChanged`；
   `WidgetFactory` / `GridService` / `Widget` / `GridEditor` / `Gallery` / `Settings` / `Advanced` 均注入显示器服务；
   `IWidgetFactory` 新增 `Add(ScreenLayout, WidgetLayout)`（旧单参 `Add(WidgetLayout)` 保留兼容）；
   `JsonParser.data` 改 `protected` 供 `LayoutProvider.Get()` 覆盖（v2 反序列化 + v1 数组回退包装）。
10. **新增/改动文件**：
    - 新增：`uWidgets.Core/Models/ScreenLayout.cs`（ScreensLayout/ScreenLayout + 工具方法）、
      `uWidgets.Core/Models/ScreenIdentity.cs`（ScreenIdentity/ScreenMatcher）、
      `uWidgets/Services/DisplayMonitorService.cs`、`uWidgets/Views/WidgetAnchorWindow.cs`、
      `uWidgets/Views/Pages/MultiScreen.axaml(.cs)`；
    - 改写：`LayoutProvider`（v2 默认 + v1 兼容）、`WidgetLayoutProvider`（带 ScreenId）、
      `BackupService`（v2 全量/单屏）、`WidgetFactory`、`GridService`、`GridEditor`、`Widget.AfterMove`
      拖动转移、`Gallery` 按屏落位、`Advanced` 网格编辑器目标屏；
    - 本地化：`Settings_MultiScreen*` 17 个条目（zh-Hans / en）。

---

## 13. 1.5.1：多屏模式逻辑梳理与修复

### 13.1 两个问题与根因

**问题 1：导入备份后在左上角出现一个 1×1 组件（两层组件重叠）**
1. **相同 Key 的双胞胎条目**（现场数据实证）：同一块 `GameViewer Virtual Display Adapter|2800x1840`
   在 `layout.json` 里出现**两条**配置。成因：屏幕首次出现时 `EnsureConfig` 建条目后**不刷新内存 Attached 列表**，
   另一块"双胞胎"屏（虚拟显示器常以两个显示设备呈现）对着**旧列表**匹配不到 → 又建一条**同 Key 空条目**。
2. **匹配不消费**：`ScreenMatcher.Match` 每条屏独立 `FirstOrDefault`，两块同 Key 的屏会**同时命中第一条配置**，
   → 同一份组件布局被渲染两次，落在相同桌面坐标 → "两层组件"；另一条永远命中不了，成为"已保存但未连接"
   （多屏页逻辑混乱的直接来源）。
3. **导入语义错位**：legacy primary 条目存**绝对桌面坐标**，而 per-screen 条目存**相对该屏工作区**。
   旧格式备份（Key=null）被导入到 per-screen 目标时坐标原样拷贝 → 相对坐标里出现绝对偏移（如 2048、2304），
   或按比例缩放后落在目标屏左上角 → "幽灵 1×1 组件"。
4. **重启重叠**：`AppRestart` 等新实例窗口出现（原 1s 上限）才关闭旧实例；慢启动时两个实例同时存活，
   所有组件短暂/持续地显示两次。

**问题 2：网格编辑器点"完成"后网格不生效**
- 网格值**保存是实时的**（编辑即写盘），但组件只在**激活（Activated）**时才 `SnapPosition` 重新吸附；
  编辑器关闭后组件停在旧位置 → "点了完成没反应"。
- 更深一层：v1 的 11 个组件全在 legacy primary 条目里（绝对坐标），其中 6 个实际位于副屏（虚拟显示器），
  而吸附/尺寸解析用的是"组件当前所在屏"的网格 → 编辑主屏网格对副屏组件无效，编辑目标与组件归属错位。

### 13.2 梳理后的设计（语义统一）

| 概念 | 规则 |
|------|------|
| **坐标系** | 主屏 legacy 条目：绝对桌面坐标（兼容 v1）；per-screen 条目：相对该屏工作区。**迁移后主屏条目只含主屏组件**，副屏组件各自落在自己的条目里 |
| **匹配** | ① 手动绑定 `DeviceName` → ② `Key`(型号+分辨率) → ③ legacy primary（仅主屏）。**消费式**：每条配置最多匹配一块屏 |
| **去重** | 同 Key 条目：空条目冗余直接剔除（保留有组件/有别名的）；两条都有组件 = 真实双胞胎屏，保留 |
| **保底** | 新屏（无配置）首用时 `EnsureConfig` 建条目并**立即刷新** Attached → 不再产生同 Key 双胞胎；随后 `MigrateLegacyWidgets` 把落在该屏的 legacy 组件拆过去 |
| **即时生效** | `layoutProvider.DataChanged` → 组件：网格变化 → 重算尺寸 + **立即重新吸附并落盘位置**（不再等激活） |
| **导入** | per-screen 备份：按目标屏分辨率比例缩放后写目标条目；legacy 备份（Key=null）：按**组件中心的绝对位置**重分配到对应屏（主屏 keep 绝对、副屏转相对）——不再产生左上角幽灵 |
| **重启** | 新实例窗口出现前等 5s（原 1s）；旧实例关闭 → 重叠窗口消失 |

### 13.3 修改清单

- `uWidgets.Core/Models/ScreenLayout.cs`：`DisplayName`/`AllWidgets` 加 `[JsonIgnore]`（不再写进 JSON）；
  新增 `ScreensLayout.Deduplicate()`（同 Key 冗余清理，未变时返回原实例）。
- `uWidgets.Core/Models/ScreenIdentity.cs`：`ScreenMatcher.Match` 新增**消费式重载**（`consumedIds`）。
- `uWidgets.Core/Services/LayoutProvider.cs`：加载时 `Deduplicate()`；`Normalize` 统一 `Version=2`。
- `uWidgets/Services/DisplayMonitorService.cs`：`Refresh` 始终刷新 `Attached`（签名不变也刷新，事件仍只在变化时触发）、
  签名加入配置 Id、消费式匹配、去重保存；`EnsureConfig` 保存后 `Refresh()` + 末尾调用迁移；
  新增 `MigrateLegacyWidgets()`（幂等拆分：legacy 组件按中心落在哪块屏 → 搬进该屏条目并转相对坐标）。
- `uWidgets/Services/WidgetFactory.cs`：`Create()` / `OnScreensChanged()` 顶部执行迁移；
  `Create()` 增加 `activeWidgets` 去重守卫（热插拔路径已建则跳过，杜绝二次创建）。
- `uWidgets/Views/Widget.axaml.cs`：`OnLayoutDataUpdated` 网格变化后立即 `SnapPosition` + 保存位置。
- `uWidgets/Views/Pages/MultiScreen.axaml.cs`：`ApplyImportTo` 增加 legacy 分支（按位置重分配）。
- `uWidgets/Services/AppRestart.cs`：握手等待 1s → 5s。
- `uWidgets/ViewModels/AdvancedViewModel.cs`（1.5.2 追加）：高级页网格数字改为读写**主屏生效网格**
  （per-screen primary 条目 Grid → 全局 AppSettings.Grid → 默认值），与网格编辑器**同一存储目标、同一回退链**；
  并订阅 `layoutProvider.DataChanged` / `appSettingsProvider.DataChanged` 即时刷新显示 —— 以前高级页
  数字绑定全局 `AppSettings.Grid`，而编辑器写入主屏条目 → 两侧不同步（"编辑器改完、高级页数字没变"）。

### 13.4 数据迁移（自动，无需手动）

下次启动自动完成：① 同 Key 空条目剔除（你当前文件里第二条 `GameViewer…2800x1840` 空配置会被删除）；
② legacy primary 里位于副屏的组件搬入副屏条目（坐标转相对，位置不变）；
③ 文件统一写回 `Version: 2` 且不再包含 `AllWidgets`/`DisplayName` 冗余字段（文件体积明显变小）。
之后"多屏"页每个物理屏只对应一条配置，网格编辑/导入导出/跨屏拖动都按屏生效。

---

## 14. 1.6.4：世界时钟重构 / 天气滚轮 / 文件夹固定模式 hover 修复

### 14.1 文件夹组件：固定模式 hover 不再截断文件名

- **问题**：固定行高（RowLayout=Fixed）时，hover 缩放的是整个条目 Border（1.12×），
  缩放以中心为原点向下扩张，会把底部文件名挤出单元格/视口而被截断。
- **方案**：固定模式下 hover 只缩放**图标**（网格模板中为 Image 增加 ScaleTransform），
  名称文字保持原位，背景高亮仍由 `:pointerover` 提供；
  Auto 模式维持原有整条目缩放。

### 14.2 天气组件：每小时滚动条支持鼠标滚轮

- **目标**：天气预报第二部分（横向滑动的逐小时条）滚轮上滑=向右滑、下滑=向左滑。
- **实现**：`ForecastWide` / `ForecastLarge` 的 HourlyScroller 上挂
  `PointerWheelChanged`（Tunnel 阶段），指针落在小时条内时把 Delta.Y 映射为
  `Offset.X += ΔY×60` 并 `Handled=true`；垂直 7 天列表不受影响。

### 14.3 世界时钟重构（2×2 / 4×2 / 4×4 / 其它自适应）

- **模型**：`WorldClockModel` 新增 `CityNames`（4 个可选自定义城市名，缺省回退时区推导名）；
  `AnalogClockViewModel.CustomCityName` 供标签使用。
- **设置**：世界时钟设置页每个时区下新增「城市名称」输入框（中英文资源
  `Clock_CityName`），留空则继续使用时区自带城市名。
- **表盘**：`AnalogWorldSingle` 增加 12 根刻度条（围绕中心代码生成，大小自适应），
  去掉 Canvas 的 80px 内边距与表盘下方三行文字，表盘明显变大；
  数字 1–12 由宿主按布局开关（`ShowNumbers`），刻度条始终显示。
- **布局**（仿 Monitor Multi-Dashboard 的定比 Board + Viewbox）：
  - **2×2**：四钟 2×2 排列，中心显示本地数字时钟（24h，无秒）；
  - **4×2**：四个钟一行 + 每列下方城市名（参考 4×2 多仪表盘），表盘更大；
  - **4×4**：2×2 四钟 + 每钟下方城市名（参考 4×4 多仪表盘）；
  - **1×1**：单钟全卡；**其它跨度**按 4 列一行 / 1 列 4 行 / 2×2 自适应。
- **代码修正**：`WorldClockViewModel` 原来每次属性访问都新建 4 个
  AnalogClockViewModel（计时器泄漏），现改为构造时缓存一份并在 Dispose 统一释放；
  数字时钟 `Digital` 随视图一起创建/释放。

### 14.4 版本号

- 主程序 `AssemblyVersion`：1.3.2 → **1.6.4**（之前 git 已到 1.6.3 但 AssemblyInfo 未同步）。
- Clock：1.1.1 → 1.2.0；Weather：1.4.1 → 1.4.2；Folders：0.2.0 → 0.2.1。

---

## 15. 1.6.5：世界时钟布局修订 + 天气滚轮修复

### 15.1 天气预报滚轮真正生效

- 1.6.4 首次实现时把 `PointerWheelChanged` 挂在 Tunnel 阶段，但鼠标滚轮是
  **Bubble** 路由事件，Tunnel 处理器根本不会触发；另外用 `Bounds.Contains`
  判断指针位置也用错了坐标系（`Bounds` 相对父级，`GetPosition` 相对自身）。
- 修复：改为 `RoutingStrategies.Bubble + handledEventsToo:true`，并用局部矩形
  `(0,0,Viewport.Width,Viewport.Height)` 判断指针是否在小时条内；
  上滑=右移、下滑=左移，垂直 7 天列表不受影响。

### 15.2 2×2 / 4×4 世界时钟：每钟中心两行（城市名 + 数字时钟）

- 需求澄清后不再在四钟中心放单个本地数字时钟，而是**每个表盘自己的中心**
  显示两行：上一行 = 自定义城市名（留空回退时区城市），下一行 = 该时区数字时钟；
  两行都居中。
- 实现：`AnalogWorldSingle` 内新增 `CenterOverlay`（表盘中心，代码控制显示），
  `AnalogClockViewModel` 增加 `DigitalTimeText`（24h，随 5s 表针刷新）。
- **4×4 与 2×2 共用同一块 200×200 Board**，整个组件由 Viewbox 等比放大，
  因此 4×4 就是 2×2 的等比放大版。

### 15.3 4×2 世界时钟：表盘上移 + 下方两行

- 四个表盘位于上方（行内 `*` 行），下方分两行：第一行城市名、第二行数字时钟；
  文字大小调小（城市 18、数字 20，单位为 Board 设计单位）。
- 上/下边距对称：条目 `Margin` 上下均为 8（Board 内比例），保证
  **表盘顶端到上边界的距离 = 数字时钟底端到下边界的距离**。

### 15.4 版本号

- 主程序 `AssemblyVersion`：1.6.4 → **1.6.5**；Clock：1.2.0 → 1.2.1；Weather：1.4.2 → 1.4.3。

---

## 16. 1.6.6：滚轮方向反转 + 城市名仅取自定义值

### 16.1 天气滚轮方向

- 1.6.5 的方向与网页习惯相反；按反馈改为：**滚轮下滑 = 右滑**（横向滚动条向右），
  滚轮上滑 = 左滑。实现上把 `Offset.X` 的增量从 `+Delta.Y×60` 改为 `-Delta.Y×60`。

### 16.2 表盘/下方城市名只用自定义输入

- `AnalogClockViewModel.CityName` 不再回退到时区推导的城市名：
  用户在城市名称设置里输入什么就显示什么；留空则显示空白（不显示时区城市）。
- 该字段同时用于 2×2/4×4 表盘中心第一行和 4×2 表盘下方第一行，行为一致。

### 16.3 版本号

- 主程序 `AssemblyVersion`：1.6.5 → **1.6.6**；Clock：1.2.1 → 1.2.2；Weather：1.4.3 → 1.4.4。
