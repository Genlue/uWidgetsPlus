# uWidgets 项目解构报告

- **项目**：uWidgets（macOS 风格桌面小组件应用）
- **版本**：1.7.4（个人使用定制版）
- **基线**：官方 [creewick/uWidgets](https://github.com/creewick/uWidgets) v0.6.0
- **技术栈**：C# / .NET 8 / Avalonia 11.1.3（Win32）/ SkiaSharp 2.88.8 / System.Text.Json / MSBuild
- **位置**：`C:\Users\25659\Desktop\Project\uWidgets`
- **交付形态**：单个 `uWidgets.exe`（框架依赖，需 .NET 8 运行时；目标机未装时提示安装）

---

## 1. 项目定位

把常用信息（时钟、天气、日历、提醒、监控、便签）以半透明 macOS 风格小组件常驻桌面。
本定制版相对官方：
1. **新增自定义 Folders 小组件**（文件夹快捷方式/监视文件夹实时刷新）；
2. **语言精简**为简体中文 + 英语（默认简体中文）；
3. **单文件打包**：全部小组件内嵌进 exe，运行时自解压到 `%LocalAppData%\uWidgets`，保留热更新能力；
4. **新增网格管理模式**（1.1.0）：用户可手动将桌面划分为 m×n 个正方形格子的自定义网格，
   网格不要求全覆盖桌面，可自由移动位置与调整格子大小（百分比存储，适配不同分辨率）；
5. **组件大小重塑**（1.1.0）：网格模式下组件自适应格子大小（不可自由拖拽调整），
   内容支持自定义缩放、自定义与网格线的边距与圆角；
6. **外观-字体列出系统全部字体**（1.1.0）：字体选择器枚举 Windows 已安装的
   全部字体族（含捆绑的 Inter）并支持搜索；
7. **修复已知问题**（1.1.0）：更新源可配置、天气请求代理可配置（默认直连）、
   小组件包解压标记改为内容哈希；
8. **毛玻璃恢复 OS 级实时模糊**（1.2.0 修订）：应用层自绘"强度可调"毛玻璃会让玻璃变成
   壁纸静态快照（动态壁纸不再跟手），故已**移除模糊强度设置**并恢复 OS `AcrylicBlur`
   背景（DWM 逐帧采样桌面合成，动态壁纸实时跟手）；
9. **高光描边成为毛玻璃的可选项**（1.2.0）：「描边毛玻璃」并入「毛玻璃」主题，
   描边颜色（RGBA）与粗细（DIP）开放为设置，默认关闭（宽度 0）；
   左上角与右下角最浓、沿每条边渐变到右上角与左下角为零；
10. **纯色模式隐藏玻璃设置**：选中纯色时设置页不显示（也不应用）描边相关项。

---

## 2. 总体架构（三层）

```
┌─────────────────────────────────────────────────────────┐
│  uWidgets（主程序，WinExe）                              │
│  · Avalonia UI：小组件窗口/设置/主题/更新检查             │
│  · DI 容器（Microsoft.Extensions.DependencyInjection）   │
│  · WidgetFactory：读 layout.json → 反射创建小组件窗口    │
│  · WidgetBundle：单文件模式自解压（本定制新增）           │
└───────────────────────┬─────────────────────────────────┘
                        │ 引用 + 宿主提供 Avalonia 运行时
┌───────────────────────▼─────────────────────────────────┐
│  uWidgets.Core（小组件 SDK）                             │
│  · 插件契约：WidgetInfoAttribute / LocaleAttribute       │
│  · AssemblyProvider：AssemblyLoadContext 加载/卸载插件   │
│  · IWidgetFactory / IDataProvider / 设置模型（Theme 等） │
└───────────────────────┬─────────────────────────────────┘
                        │ 运行时从 Widgets\ 目录动态加载（不复制引用）
┌───────────────────────▼─────────────────────────────────┐
│  Widgets\ 目录（7 个小组件 DLL）                         │
│  Clock · Calendar · Weather · Monitor · Notes            │
│  Reminders · Folders（自定义）                           │
└─────────────────────────────────────────────────────────┘
```

### 插件加载机制（关键设计）
- 主程序启动时 `AssemblyProvider` 扫描 `Widgets\*.dll`，读取每个 DLL 的
  `WidgetInfoAttribute`（视图类型/模型类型/设置类型）与 `LocaleAttribute`（图标、本地化）；
- 创建小组件时用**反射** `ActivatorUtilities.CreateInstance` 激活视图类型
  （DI 自动注入 `IWidgetLayoutProvider`、模型等构造函数参数）；
- 每个插件 DLL 放在独立的 `AssemblyLoadContext`（`PluginLoadContext`）中，
  通过 `AssemblyDependencyResolver` + 各插件自己的 `*.deps.json` 解析依赖
  （Avalonia/Core 优先从宿主默认上下文复用；插件自带包如 System.Management、TimeZoneNames）；
- 支持热卸载（`UnloadAssembly`）与"同名取最高版本"。
  ⚠️ 因依赖反射激活，**发布时绝不能开启 Trimming**。

---

## 3. 目录结构解构

```
C:\Users\25659\Desktop\Project\uWidgets
├── build.ps1                     ← 一键构建：单 exe（默认）/ 便携 zip（-Portable）
├── BUILDING.md                   ← 构建与模块化说明
├── Directory.Packages.props      ← 集中包版本管理（CPM，12 个包，唯一版本源）
├── scripts/
│   └── build-widgets-bundle.ps1  ← 小组件打包脚本（被 MSBuild 目标调用）
├── dist/                         ← 构建产物（gitignore）
│   └── win-x64/uWidgets.exe      ← 单文件交付物（≈51 MB）
├── .github/                      ← 官方 CI 与图片素材（保留参考）
└── src/
    ├── uWidgets.sln
    ├── uWidgets/                 ← 主程序（约 45 个源码文件）
    │   ├── Program.cs            ← 入口：先解压 → 再启动 Avalonia
    │   ├── App.axaml(.cs)        ← 应用初始化 + DI 装配
    │   ├── AssemblyInfo.cs       ← 主版本号（1.6.7）
    │   ├── Services/
    │   │   ├── WidgetBundle.cs   ← 【定制】单文件自解压逻辑（.version = zip 内容哈希）
    │   │   ├── WidgetFactory.cs  ← layout → 窗口创建
    │   │   ├── GridService.cs    ← 【定制】三种网格模式（自定义/虚拟/自由）吸附
    │   │   ├── GridMetrics.cs    ← 【定制】网格百分比 → 像素换算（正方形格子）
    │   │   ├── SystemFonts.cs    ← 【定制】Windows 系统字体枚举（注册表 + 捆绑 Inter）
    │   │   ├── GridService / ThemeService / LocaleService
    │   │   ├── LiquidGlassRenderer.cs ← 【定制】液态玻璃光学模型（距离场/斜面/折射/高光，§18.3）
    │   │   ├── LiquidGlassWallpaper.cs ← 【定制】壁纸采样（Progman 抓图 + 文件回退，含 DesktopCapturer）
    │   │   ├── UpdateService / UpdateTimer / TimerService / InteropService
    │   ├── ViewModels/           ← 设置页 MVVM（General/Appearance/Advanced/…）
    │   ├── Views/                ← 设置窗口、小组件窗口、网格编辑器、编辑弹窗、控件
    │   │   ├── Controls/LiquidGlassSurface.cs ← 【定制】材质位图控件（防抖 + 后台渲染 + 缓存）
    │   │   ├── WallpaperAlignDialog.axaml(.cs) ← 【定制】壁纸对准校正对话框
    │   │   └── GridEditor.axaml(.cs) ← 【定制】网格可视化编辑器（拖动移动/调格子）
    │   └── Locales/              ← Locale.resx(英) + Locale.zh-Hans.resx
    ├── uWidgets.Core/            ← 小组件 SDK（约 28 个文件）
    │   ├── Const.cs              ← 【定制】数据目录双模式（便携/打包）
    │   ├── Services/
    │   │   ├── AssemblyProvider.cs  ← 插件加载核心
    │   │   └── ProxySettings.cs  ← 【定制】HTTP 代理配置（直连/系统/自定义）
    │   ├── Models/Attributes/    ← WidgetInfoAttribute / LocaleAttribute
    │   ├── Models/Settings/      ← AppSettings/Grid/GridMode/Dimensions(ContentScale)…
    │   └── Interfaces/           ← IWidgetFactory / IDataProvider / IGridService …
    └── Widgets/                  ← 小组件模块，每个子目录一个独立 csproj
        ├── Clock/                ← 模拟/数字/世界时钟（5 种样式）
        ├── Calendar/             ← 月历/日期
        ├── Weather/              ← Open-Meteo 天气/空气/UV/日出日落（代理可配置）
        ├── Monitor/              ← CPU/内存/磁盘/网络（WMI）
        ├── Notes/                ← 便签
        ├── Reminders/            ← 提醒列表
        └── Folders/              ← 【自定义】文件夹小组件
```

---

## 4. 关键流程解构

### 4.1 启动流程
```
Program.Main
 ├─ WidgetBundle.ExtractIfNeeded()        ← ① 先处理数据目录（见 4.3）
 ├─ BuildAvaloniaApp()（读 appSettings 初始化 Win32 平台选项）
 └─ StartWithClassicDesktopLifetime
     └─ App.OnFrameworkInitializationCompleted
         ├─ DI：AppSettingsProvider / AssemblyProvider / WidgetFactory …
         ├─ localeService.SetCulture(zh-Hans) + themeService.Apply
         ├─ WidgetFactory.Create() 读 layout.json → 每个 widget 反射创建并 Show()
         └─ 无 widget 或带 --settings 参数 → 打开设置窗口
```

### 4.2 单文件打包流程（构建期）
```
dotnet publish -c Release -r win-x64 --self-contained false
 └─ MSBuild 目标 BuildWidgetsBundle（钩子：AssignTargetPaths）
     ├─ scripts/build-widgets-bundle.ps1：逐个小程序 dotnet publish
     │   （扁平输出到 staging，删除 pdb/xml/runtimeconfig，保留 deps.json）
     ├─ 压缩为 widgets.zip（≈8 MB，含 7 个 widget + 语言卫星资源 + 原生库 + 依赖 DLL）
     └─ 作为 EmbeddedResource 嵌入：uWidgets.Resources.widgets.zip
 └─ PublishSingleFile + IncludeNativeLibrariesForSelfExtract
     → 单 exe（Avalonia 原生库运行时自解压到临时目录）
```
另：`appSettings.json` / `layout.json` 默认值同样以 EmbeddedResource 内嵌。

### 4.3 数据目录双模式（本定制核心）
`Const.DataFolder` 启动即判定：
| 模式 | 判定条件 | 数据位置 | 说明 |
|------|---------|---------|------|
| 便携 | exe 旁存在 `Widgets\` 目录 | exe 所在目录 | 官方/开发模式，行为不变 |
| 打包 | 无 `Widgets\` 目录 | `%LocalAppData%\uWidgets\` | 首次运行解压内嵌资源 |

`WidgetBundle.ExtractIfNeeded()`：
1. `appSettings.json` / `layout.json` 缺失时写出默认值（**不覆盖用户已有配置**）；
2. widget 包仅在版本标记（`.version` 文件 = 内嵌 zip 的 **SHA-256 内容哈希**，1.1.0 起）变化时重新解压；
   即任何 widget 变更（新 DLL、改版本号、新资源）都会触发重解压，而不只是主 exe 版本变化；
3. 解压带路径穿越防护。

### 4.4 热更新
打包模式下 widget 是 `%LocalAppData%\uWidgets\Widgets\` 下的普通磁盘文件，
替换某个 DLL 后重启应用即生效；内嵌包哈希不变则不会覆盖用户替换的版本。
（若开发者重新构建了 exe 且 widget 包内容有变，哈希变化会覆盖本地替换版本——发布新版本时预期行为。）

---

## 5. 自定义 Folders 小组件解构（0.2.0）

| 文件 | 职责 |
|------|------|
| `Models/FolderModel.cs` | 模型（26 个配置项：固定项/监视目录/网格列表/名称样式…），存于 layout.json |
| `Views/Folder.axaml(.cs)` | 视图 + 交互：FileSystemWatcher 实时刷新、拖放（OLE + WM_DROPFILES）、点击打开 |
| `Views/Settings/FolderSettings.axaml(.cs)` | 设置面板（19KB） |
| `Services/FolderIconService.cs` | Windows 图标提取（SHGetFileInfo 等 Win32 互操作，25KB） |
| `Services/WidgetOleDropTarget.cs` | OLE 拖放目标实现（把文件拖进 widget 即固定） |
| `AssemblyInfo.cs` | `[WidgetInfo(Folder, FolderModel, FolderSettings)]` + `[Locale(...)]` 注册 |

两种模式：**固定项**（Items 路径列表，含自定义名称）与**监视文件夹**（WatchFolder，
实时列出内容，支持排序/隐藏扩展名/多行换行等）。

---

## 6. 配置体系

### appSettings.json（默认值内嵌于 exe）
- `Theme`：暗色/强调色/不透明度/单色/原生边框/字体（默认 `zh-Hans` 下微软雅黑；
  **外观-字体**枚举系统安装的全部字体族并支持搜索，另含应用捆绑的 Inter）
- `Theme.Surface`：表面材质（`0`=毛玻璃、`1`=纯色、`2`=液态玻璃、`3`=描边毛玻璃【旧配置兼容】）
- `Theme.LiquidGlass`：液态玻璃光学参数（1.7.0 新增，`null` = 默认值；
  写入前一律经 `LiquidGlassSettings.Normalize()` 钳制，见 §18.5）
- `Theme.OutlineColor` / `Theme.OutlineWidth`：毛玻璃的**高光描边颜色**（RGBA HEX，
  默认 `#B3FFFFFF` 柔和灰白，含透明度）与**粗细**（DIP，默认 **0 = 关闭**）。
- ~~`Theme.BlurStrength`（模糊强度）~~：1.2.0 修订后**已移除**（见 §8.4）。
- `Templates`：历史遗留（官方 8 套模板）——界面实际使用 `AppearanceViewModel.SurfaceTemplates`
  硬编码的 **3 套表面预设**（毛玻璃 / 纯色 / 描边毛玻璃）
- `Layout`：`GridMode`（0=自定义网格【默认】、1=虚拟网格、2=自由布局）/ 吸附 / 锁定
- `Dimensions`：Size/Margin/Radius/**ContentScale**（内容缩放 0.5×–2×，默认 1.0）
- `Region.Language`：`zh-Hans`（仅支持 zh-Hans / en）
- `Grid`（自定义网格模式专用，**百分比存储**，适配不同分辨率）：
  - `Columns`（n）、`Rows`（m）：行列数
  - `XPercent`：网格区域左上角 X（工作区宽度的 %）
  - `YPercent`：网格区域顶部线位置（工作区高度的 %）
  - `CellPercent`：格子边长（工作区**宽度**的 %；纵向沿用横向像素值，**保证正方形**）
- `UpdateUrl`：自定义更新源（null/空 = 禁用更新检查，默认禁用）
- `HttpProxy`：`""`/null = 直连（默认，绕过失效的系统代理）、`"system"` = 系统代理、其他 = 代理 URL
- `RunOnStartup` / `IgnoreUpdate`

### 网格管理模式（1.1.0 新增，**默认启用**）
- **自定义网格**（默认）：用户在桌面划分 m×n 正方形格子的网格区域（不要求全覆盖桌面），
  设置页可调行列数/位置/格子大小百分比，或打开**全屏网格编辑器**（`GridEditor`）：
  在桌面全屏蒙版上**拖动网格区域**移动整片网格，**右键网格**弹出面板修改 m×n 与格边，
  变更实时保存，关闭后组件自动吸附并按跨度填入格子；
- **虚拟网格**：官方行为，组件按 `Size` 虚拟单位吸附；
- **自由布局**：无网格，组件自由放置。

### layout.json
```json
[{"Type":"Clock","SubType":"AnalogI","X":31,"Y":387,"Width":155,"Height":155,
  "Settings":{"ShowSeconds":true,"ShowDate":false,"Use24Hours":false,"TimeZoneId":null}},
 {"Type":"Folders","SubType":"Folder","X":1965,"Y":90,"...","Settings":{"Items":[...]}}]
```
- `Type`=插件程序集名，`SubType`=WidgetInfoAttribute 中的视图名，`Settings` 反序列化为各 widget 模型；
- 自定义网格模式下组件 `Width/Height` = 格子跨度 × 格子像素（1×1/2×2/4×2/4×4/8×4，
  右键菜单选择），组件**不可自由拖拽调整大小**，而是自适应网格；位置强制吸附到最近格子；
  组件与网格线的边距由 `Dimensions.Margin` 控制，圆角由 `Dimensions.Radius` 控制，
  内容缩放由 `Dimensions.ContentScale` 控制（基准 72px 设计尺寸）。

---

## 7. 构建与发布

```powershell
.\build.ps1                      # Release + win-x64 单 exe → dist\win-x64\uWidgets.exe
.\build.ps1 -Runtime win-arm64   # ARM64
.\build.ps1 -Portable            # 额外产出官方式便携 zip（exe + Widgets + json）
```
- **包版本**：`Directory.Packages.props` 一处集中（Avalonia 11.1.3 等）。
- **主版本**：`src/uWidgets/AssemblyInfo.cs`（1.2.0）。
- **小组件版本**：各自 `AssemblyInfo.cs`（Clock 1.1.1、Folders 0.2.0、Weather 1.4.1 …）。
- 环境要求：Windows + .NET 8 SDK；分发目标机需 .NET 8 运行时（框架依赖模式）。
- 新增小组件：复制 `src/Widgets/Clock/` 骨架 → 注册 `WidgetInfo` → `dotnet sln add` → 重新 `build.ps1`（打包目标自动发现新模块）。

---

## 8. 已知问题与后续建议

### 8.1 网格与组件系统修复（当前构建）

本修复轮针对三个网格/组件缺陷（均已在 175% 缩放的 2560×1600 实机上验证）：

1. **网格 DPI 单位错配（缝隙/重叠根源）**：网格度量（`GridMetrics`）按工作区**物理像素**计算，
   而 `window.Width/Height` 是 **DIP** —— 缩放屏（如 175%）下组件窗口物理尺寸 = 格子 × 1.75，
   组件既不贴合格子又彼此错位。
   ✅ 已修复：所有**尺寸**运算统一换算为 DIP（`尺寸 = 格子物理像素 ÷ 屏幕缩放`），
   **位置吸附**仍按物理像素（`SnapPosition` 不变）。格子重新紧密贴合（margin=0 时相邻卡片无缝相接）。
   - **旧布局迁移**：旧版保存的尺寸是"物理格子数"（如 230 px），按 DIP 解释会漂移成 2×2；
     `GetSpan()` 现按"物理/DIP 双解释择优"（取离整数跨度更近者）恢复，旧 230px 布局无损还原为 1×1。
2. **内容放缩作用于整张卡片**：`ContentScale` 原通过缩小 `ContentPresenter` 实现，
   整卡（含边距）被缩放 → 卡片与格子脱节、格子间出现无法用边距消除的缝隙。
   ✅ 已修复：`ContentScale` 只对**卡片内的内容**施加中心缩放渲染变换
   （1.0 = 内容填满卡片，0.5×–2× 仅缩放内容，超出部分被卡片裁剪）；
   **卡片大小只由格子和"高级 → 形状 → 小部件边距"决定**（`格子 − 2×边距`）。
3. **毛玻璃覆盖整个网格格**：Avalonia 的 `TransparencyLevelHint=AcrylicBlur` 是**窗口级**背景，
   玻璃（模糊+暗色涂层）铺满整个窗口（即整格），圆角之外的网格区域也被模糊。
   ✅ 已修复：`InteropService.SetWidgetRegion` 用 `SetWindowRgn` 把原生窗口
   （含 OS 级模糊背景）裁剪为「格子 − 边距 + 圆角」的圆角矩形：
   - 毛玻璃只存在于组件卡片本身区域，**边距与圆角都由设置实时生效**；
   - 卡片外的网格区域变为**真正透明且点击穿透**（点击落在格子间隙直接落到桌面）；
   - 原生边框模式（`UseNativeFrame`）不裁剪（避免裁掉系统边框）。

后续建议：
- 多屏/混合缩放：网格基于主屏工作区；副屏组件吸附时按所在屏重算（现状为吸附回主屏网格）。
- 若 `SetWindowRgn` 圆角边缘在超低分辨率/特殊 DPI 下出现锯齿，可改用 DWM 圆角偏好
  （代价是圆角大小由系统固定，无法跟随 `Dimensions.Radius`）。

4. **组件内容塌缩错位（内容缩放学重写引入）**：1.1.0 把 `ContentPresenter` 由旧的
   「显式 Width/Height = 窗口÷scaleFactor」改为仅依赖内容自然尺寸后，在 `Center` 对齐 +
   `DataTemplate→ContentControl` 包装下，内容以**无界限宽度**测量（内容自然宽度）：
   Monitor 小组件的环形+百分比收缩到卡片左侧，卡片未充满格子，且**图标位置随下方数字宽度漂移**
   （2% 与 14% 文本宽度不同 → 内容块宽度不同 → 居中偏移不同）；其他小组件视图同样受影响。
   ✅ 已修复：新增 `UpdateContentSize()`（ctor / Opened / Resized / Scale 时调用），
   `ContentPresenter` 显式设为「格子 − 2×边距」的尺寸；内容重新充满整卡。
   已在 192×192 与 158×158 窗口尺寸下 headless 渲染验证：环形/文本几何与数值无关，
   与图库预览布局一致。

5. **天气预报小部件只显示大号温度**：预报视图按自身尺寸切换 LARGE/WIDE/SMALL/TINY 四种布局，
   阈值以 150 DIP 为界；网格模式在缩放屏（如 175%）下 2×2 格子只有约 146 DIP → 落入 TINY
   （仅显示温度，城市/图标/天气/高低温全部缺失）。且 SMALL 布局是固定尺寸设计，
   比格子更小时会被裁切而非缩放。
   ✅ 已修复：`ForecastSmall` 外包 `Viewbox`（Uniform + DownOnly + 左对齐，
   自适应格子、不放大、不居中漂移）；切换阈值降至 75 DIP（仅真正 1×1 单格保留 TINY 布局）。
   已在 172×172 与 192×192 窗口下 headless 渲染验证：完整显示
   城市/大号温度/天气图标/状况/高低温，与图库预览布局一致。

6. **尺寸档位数字化 + 智能自适应**：右键菜单由预设档（1×1/小 2×2/中 4×2/大 4×4/特大 8×4）
   改为**完整数字矩阵 1×1 … 4×4**（共 16 档，含任意矩形如 1×2、2×3、3×4…），
   子菜单按「尺寸 / 网格尺寸」展开；`ResizeSize("列,行")` 统一处理。
   - Monitor 组件按形状自适应：横屏（宽 > 1.25×高，如 2×1/3×2）→ 固定比例画布
     环形左 + 百分比右整体缩放；方正/竖排 → 环形上 + 百分比下；单格小卡 → 仅环形；
   - Forecast 按形状选布局（SMALL 现自适应缩放；WIDE 高度阈值 150→140 适配 4×2）；
   - 其余组件（时钟/日历/便签/提醒/文件夹）为拉伸或 Viewbox 布局，天然适配任意矩形。
   已在 2×2/2×1/1×2/2×3/3×2/1×1 等 10 种形状下 headless 渲染验证。

### 8.2 更早的已知问题（已修复/说明）

1. ~~**更新检查**指向官方 GitHub 仓库，个人 fork 会误报新版本。~~
   ✅ 已修复（1.1.0）：新增 `UpdateUrl` 设置，**默认禁用更新检查**（null），
   在"高级 → 网络"中配置自有更新源（如 `https://github.com/owner/repo/releases/latest`）后自动启用；
   `IgnoreUpdate` 跳过版本机制保留。
2. ~~**天气代理**：Weather 请求经系统代理（历史日志中 127.0.0.1:7890 拒连），代理软件未开启时天气显示失败。~~
   ✅ 已修复（1.1.0）：天气与更新请求默认**直连**（绕过系统代理，避免残留代理拒连），
   可在"高级 → 网络"中选择系统代理或自定义代理 URL。
3. ~~**版本策略**：`.version` 标记只跟随主 exe 版本变化，改小组件 DLL 版本不会触发重新解压。~~
   ✅ 已修复（1.1.0）：`.version` 改为内嵌 widget 包 zip 的 **SHA-256 内容哈希**，
   任何 widget 包变化都会自动重新解压（手动删除 `Widgets\.version` 可强制刷新）。
4. **旧便携版迁移**：把旧目录 `layout.json` / `appSettings.json` 复制到
   `%LocalAppData%\uWidgets\` 即可无损迁移；或将新 exe 直接放入旧目录走便携模式。
5. **历史崩溃**：旧版 crash_log（"Failed to create an instance of Widget"）在 1.1 构建的
   冒烟测试中未复现；若再出现，检查 `%LocalAppData%\uWidgets\crash_log.txt`。
6. **CI**：官方 `publish.yml` 仅作参考（多运行时 zip 分发）；本定制以 `build.ps1` 为唯一构建入口。
7. **自定义网格与多屏**：网格百分比基于主屏工作区；副屏上的组件切到自定义网格模式后会被吸附回主屏网格。

### 8.3 编辑会话修复（Reminders / Notes / ClickThroughTextBox）
 
本轮针对"清单改名崩溃 + 光标偏移一位"两个缺陷（Avalonia.Headless 探针回归验证）：
 
1. **清单改名崩溃（NullReferenceException）**：三个布局控件（`ListSmall`/`ListWide`/`ListLarge`）的后置代码用
   `(Parent as List)!` 转发事件——`List.OnSizeChanged`（尺寸切换）与 `Widget.OnWidgetLayoutUpdated`
   （保存后重建视图）会替换当前布局控件，替换后 `Parent` 为 null，任何随后的 LostFocus/Click →
   `(Parent as List)!` → NRE 崩溃。
   ✅ 已修复：`List` 构造时把自身引用注入布局控件（`new ListSmall(this, viewModel)`），不再依赖 Parent 查找；
   事件转发永远指向存活对象。
2. **保存时整个 widget 视图被重建**：`Widget.OnWidgetLayoutUpdated` 在 Settings 变化时用
   `ContentPresenter.Content = userControl()` 重建视图——编辑会话（光标/焦点/IME）被销毁，且标题失焦保存
   会先于勾选按钮 Click 触发重建，导致"点勾没反应"。
   ✅ 已修复：新增 `IWidgetSelfRefreshing` 契约；Reminders/Notes 视图实现之——布局变化时**原位刷新**
   （更新 VM 并通知绑定），不再重建控件；其他 widget 保持原有"从模型重建视图"路径（编辑对话框流程不变）。
3. **光标偏移一位**：激活编辑必须双击（单击穿透给 widget 拖拽），Avalonia 默认在双击时做**单词选择**，
   中文文本下收起选择后光标比点击位置偏左一位——第一次 Backspace 删除倒数第二个字。
   ✅ 已修复：`ClickThroughTextBox` 激活双击后把单词选择**收起到选中词的末端**（`SelectionEnd`），
   光标回到点击处；框内普通双击仍保留单词选择。无头验证：激活双击后光标 = 文本末尾。
4. **VM 只读 + 保存路径陈旧模型**：`RemindersViewModel.ListName`/`NoteViewModel.Title/Content` 为 getter-only，
   绑定写回被静默丢弃；保存时又从视图的陈旧字段重建模型，会覆盖其他框正在编辑的内容。
   ✅ 已修复：VM 属性可写（绑定写回直接更新模型），VM 成为会话期模型的唯一来源
   （`View.Model`），保存/刷新均以 VM 模型为基准；`UpdateModel` 不再更换 DataContext。
 
全部变更经 Debug/Release 构建 + `uw-caret-probe`（Avalonia.Headless + Skia + Inter，真实 List/Note 控件）回归：
崩溃路径、原位刷新、焦点保持、VM 写回、End/Backspace、双击收起全部通过。

### 8.4 1.2.0：毛玻璃恢复 OS 实时模糊 + 高光描边选项

**毛玻璃（OS 级实时，强度设置已移除）**
- 组件窗口的模糊来自 OS 背景（`Styles/Transparent.axaml` 设置 `AcrylicBlur`、
  WinUI Composition 宿主背景画笔）：DWM/合成器**逐帧**采样窗口背后的桌面合成画面，
  动态壁纸/轮播/视频壁纸均实时跟手；
- 曾尝试"应用层自绘毛玻璃 + 可调强度"（`BackdropService` 壁纸裁剪 + `BlurEffect` 半径
  0–30）：因模糊源是**静态位图快照**，动态壁纸不再跟手，且 OS 半径被 Avalonia 硬编码
  30px（`WinUIGaussianBlurEffect` 固定 STANDARD_DEVIATION），二者不可兼得 ——
  **已回滚**：`BackdropService.cs` 删除、`Theme.BlurStrength` 与设置页「毛玻璃强度」
  移除，`BackgroundOpacity` 恢复 = `OpacityLevel`；旧配置中的 `BlurStrength` 字段被忽略。

**高光描边（毛玻璃的可选项）**
- 「描边毛玻璃」主题并入「毛玻璃」：主题按钮回到两个（毛玻璃 / 纯色），
  `SurfaceStyle.OutlinedAcrylic` 仅兼容旧配置；描边任意玻璃表面
  `OutlineWidth > 0` 即启用，默认 **0 = 关闭**；
- 描边为 **ConicGradientBrush**（0°=正上、顺时针；`GradientStop` 偏移为 0–1 的 360° 比例，
  角度须 `/360`）：**左上/右下最浓**（`Angle` 起点 = 左上角），沿每条边线性渐变，
  右上/左下恰为零；分段角宽 TL→TR / BR→BL 各 `2·α`、TR→BR / BL→TL 各 `180−2·α`
  （`α = atan2(宽/2, 高/2)`），任意宽高比贴合；描边画在卡片 `Border.BorderBrush`
  （位于自身背景之上、内容之下），与 OS 模糊层不冲突；
- 纯色主题：不应用且**设置页隐藏**描边设置（`ShowGlassSettings`）。





5. **光标偏移+输入崩溃（Avalonia 框架级已知缺陷，11.1/11.2/11.3/master 均未修复）**：
   混合 Inter+CJK 回退字体下命中测试/光标索引漂移 +1：双击标题后光标显示在末字右侧，
   实际索引却在前一位（Backspace 删倒数第二个字）；且该漂移会在 IME 组合时把光标索引推到
   文本长度之外，触发 `TextPresenter.CreateTextLayout` → `GetCombinedText` 的
   `String.Substring` 越界异常（**crash_log 实证**：`Index and length must refer to a location
   within the string`），布局阶段即崩溃。
   ✅ 已修复（应用层防护，不依赖上游修复）：
   - **索引钳制**：`ClickThroughTextBox` 监听 Text/CaretIndex/Selection 变化，光标/选区越界
     （<0 或 >文本长度）即回钳到合法区间——`GetCombinedText` 的 Substring 不再可能越界；
   - **末端点击置尾**：激活双击（隧道阶段处理，不依赖手势事件）时，点击点越过最后一个字形
     的右缘（以 TextLayout 实测文本宽度判定）→ 光标强制置于文本末尾，Backspace 删除最后一位；
   - 单词选择收起到末端（`SelectionEnd`）保持不变。

---

## 9. 1.3.0：外观细化（单色配色 / 主题名 / 纯色自定义 / 背景透明度修复）

1. **单色模式可选配色**：单色开启后新增"配色"下拉，可选 **黑白** 与 **强调色**（默认）。
   - 黑白：深色模式为白、浅色模式为黑（新增 `Styles/MonochromeBlackWhite.axaml`）；
   - 强调色：原行为（浅色用 `SystemAccentColorDark1`、深色用 `SystemAccentColorLight2`，
     `Styles/Monochrome.axaml` 不变）；
   - 旧配置的 `Monochrome: true`（无 `MonochromeVariant` 字段）映射为强调色，外观不变。
   - 1.3.2 修订：**强调色与文字色统一**。小组件存在两套前景色——强调色
     （标题/强调图标，`SystemControlForegroundAccentBrush`）与文字色
     （`SystemControlForegroundBaseHighBrush`）；此前黑白模式只覆盖文字色，
     标题仍保持强调色（两色未统一）。现强调色定义移入常驻 `Styles/Accent.axaml`
     （挂在样式集合，位于单色样式之前），黑白样式追加覆盖该强调色为黑/白——
     开启单色后两种颜色一律统一：选强调色则均为强调色，选黑白则均为黑白
     （含标题、图标、表针、文字）。
2. **主题按钮下方显示名称**：外观-应用主题 的预览下方显示"毛玻璃"/"纯色"
   （`ThemeButton.ThemeName`，本地化 zh-Hans / en）。
3. **纯色背景自定义**（仅纯色主题显示，`ShowSolidBackground`）：
   - 深色模式与浅色模式背景颜色分别可调（`Theme.SolidBackgroundDark/Light`，HEX）；
   - 每侧提供"应用到浅色 / 应用到深色"按钮，把当前所选颜色复制到另一方；
   - 默认值 = 原有效果：深色 `#2E2E2E`（黑灰）、浅色 `#FFFFFF`（白）；字段为 `null` 时
     回落到默认，旧配置自动兼容。
4. **纯色背景透明度修复**：纯色模式的 `WidgetBackground` 此前为全不透明（App.axaml 兜底
   画笔无 `Opacity`），不透明度滑块失效。现新增 `Styles/Solid.axaml`：背景画笔
   `Opacity="{DynamicResource BackgroundOpacity}"` 跟随不透明度设置。
   1.3.1 修订：**窗口透明 hint 改为代码本地值**（`Widget`/`Settings` 的
   `ApplyTransparencyHint()`，毛玻璃 → `AcrylicBlur`、纯色 → `Transparent`；
   样式里保留兜底）。此前由样式切换 hint 在运行时可能残留 OS 模糊背景
   （切换表面后不透明度一调，半透明卡片立刻透出模糊 → 看起来像毛玻璃）；
   本地值 + `DataChanged` 重新应用后，运行时表面切换确定性地重建原生透明度，
   已实机验证：玻璃=逐帧模糊、纯色=逐像素透明不模糊。
5. **颜色选择器全部换成 RGB 调色盘**（1.3.1）：去掉此前隐藏光谱/分量滑块的
   精简配置，恢复完整面板（2D 光谱 + R/G/B 滑杆 + 色相 + 十六进制值输入 + 色板），
   黑色可直接取到；仅「描边颜色」保留透明度修改（`IsAlphaVisible`）。
6. **背景颜色适用于毛玻璃与纯色两种材质**（1.3.1）：`Transparent.axaml` 的背景画笔
   与 `Solid.axaml` 一致采用每变体主题字典（深/浅色自定义颜色 × 不透明度），
   设置页「背景颜色」不再限定纯色模式，两种材质均可分别自定义深色/浅色背景；
   默认值仍为深色 `#2E2E2E`、浅色 `#FFFFFF`。
7. **颜色存储规范化**（1.3.1）：保存颜色一律写 `#AARRGGBB` 十六进制
   （`Color.ToString()` 可能返回命名色如 "White"，不便于新旧值解析回退）。

---

## 10. 1.3.x：设置窗口 macOS 红绿灯标题栏

1. **模型**：`TitleBarStyle` 枚举（`Native=0` 原生系统按钮 / `TrafficLights=1` macOS 红绿灯）与
   `TitleBarSize`（红绿灯直径，DIP）。`AppSettings` 新增两个可空字段——旧配置 `null` 回退
   `Native` / 14px，外观无损兼容。
2. **`TrafficLightTitleBar` 控件**（`Views/Controls/`，36px 栏，复刻 macOS/IconForge 设计）：
   - 三圆点 + 1px 描边：红 `#FF5F57`/`#E0443E`（关闭）、黄 `#FEBC2E`/`#D89E24`（最小化）、
     绿 `#28C840`/`#1FAD30`（最大化/还原），按下变暗（如红 `#D14E47`）；间距 = 直径 × 2/3
     （圆心距 5/3×直径），12px = macOS 标准；
   - 图标（lucide X / Minus / Maximize2 / Minimize2）默认隐藏，悬停/键盘聚焦 0.08s 渐显，
     仅被悬停的按钮显示；
   - `ApplyGeometry()` 按 `Size` 整体等比缩放（直径/圆角/间距/左偏移/图标），钳制 12–24px；
   - **失焦灰化**：窗口失焦 → 按钮变灰（`#808080`/`#C0C0C0`/`#9C9C9C`）+ 整组 55% 不透明度；
   - **交互**：空白处按下拖动窗口（按钮区除外），双击切换最大化；绿钮图标**跟随窗口真实状态**
     ——任何路径最大化（按钮/双击/Win+Up/拖顶边/任务栏快捷方式）都切换为还原图标，ToolTip 同步。
3. **集成**（`Settings` 窗口 `ApplyTitleBarStyle()`）：
   - `TrafficLights` → `ExtendClientAreaChromeHints = NoChrome`（隐藏系统按钮）+
     `ExtendClientAreaTitleBarHeightHint = 36`；`Native` → `Default` + 50；
   - 挂在 `appSettingsProvider.DataChanged`，运行时切换即时重建原生 chrome（Win32 平台层），无需重启；
   - `AppTitle`（原生栏标题）与 `TrafficBar` 互斥显示。
4. **外观页**：新增"标题栏"下拉（原生系统按钮 / macOS 红绿灯）；红绿灯样式下显示"大小"档位
   （12 / 14 / 16 / 18 / 20px，默认 14——比 macOS 标准 12px 略大，适配高 DPI），
   `ShowTitleBarSize` 控制显隐。
5. **本地化**：`TrafficLight_Close/Minimize/Maximize/Restore` + 设置页条目（zh-Hans / en）。

---

## 11. 1.4.0：导入/导出备份（高级 → 备份与恢复）

1. **范围**：单个 JSON 备份文件保存四类数据：
   - **软件设置**：`appSettings.json` 全量（主题/尺寸/代理/更新源/标题栏…）；
   - **网格配置**：`appSettings.json` 的 `Grid` 段（行列/位置/格子百分比）与 `Layout.GridMode`；
   - **卡片布局**：`layout.json` 每张卡片 X/Y/Width/Height（跨度与吸附位置）；
   - **卡片内容**：每张卡片的 `Settings` 模型（各小组件个性化配置）。
2. **格式**：`BackupData(Version, AppSettings, Layout)`，缩进 JSON 可人工阅读；字段名大小写不敏感；
   版本必须 = `CurrentVersion`（1），缺段/无效 JSON 直接拒绝导入——备份文件天然防误用。
3. **导出**（高级页"导出备份"按钮）：`SaveFilePicker` 写出 `uWidgets-backup.json`；
   `BackupService.Create() = (1, appSettingsProvider.Get(), layoutProvider.Get())`——只读快照，
   不触碰运行中的文件，零副作用。
4. **导入**（高级页"导入备份"按钮）：`OpenFilePicker` → `BackupService.Parse()` 校验 →
   `ConfirmDialog` 确认（"导入并重启" / "取消"）→ 经 `appSettingsProvider.Save(...)` 与
   `layoutProvider.Save(...)` 恢复（走 Provider 保持内存缓存 + `DataChanged` 监听同步，主题/标题栏
   等运行时即时生效）→ `AppRestart.Restart()` 重启重建全部卡片窗口。失败路径（无效文件/异常）
   弹提示对话框，不破坏现有配置。
5. **新增组件**：
   - `uWidgets.Core/Models/BackupData.cs`：备份记录模型与版本常量；
   - `uWidgets/Services/BackupService.cs`：序列化/解析/校验/读写；
   - `uWidgets/Services/AppRestart.cs`：应用重启辅助（设置窗口原"重启"逻辑抽出复用）；
   - `uWidgets/Views/ConfirmDialog.axaml(.cs)`：小型模态确认/提示对话框；
   - 高级页新增"备份与恢复"区块；`SettingsViewModel.SetCurrentPage` 为
     Advanced 注入 `ILayoutProvider`；本地化 11 个新条目（zh-Hans / en）。

---

## 12. 1.5.0：多屏显示与管理（v2 布局格式）

1. **存储结构（layout.json v2）**：单一 JSON 文件由数组改为一组按屏幕分组的配置：
   ```json
   { "Version": 2, "Screens": [ { "Id": "primary", "Layout": [] } ] }
   ```
   - `ScreensLayout(Screens, Version=2)`；每条 `ScreenLayout(Id, Key, Alias, DeviceName, Grid, ContentScale, Layout)`；
   - **Id**：屏幕稳定 GUID（首次出现时生成），屏幕别名/手动改绑/所有权都用它；
   - **Key**：自动身份 `"FriendlyName|WidthxHeight"`（如 `"DELL U2723QE|2560x1600"`）；
   - **旧 "primary" 条目（Key=null）**：兼容 v1 —— 存**桌面绝对坐标**，运行时匹配"当前主屏"；
     新屏幕条目坐标**相对该屏工作区原点**（v1 迁移不做——旧文件读取时自动包装进 primary 条目，
     拖动后自然转相对坐标并落到对应屏条目）；
   - `Grid`/`ContentScale` 留空 → 回落全局 `AppSettings.Grid` / `Dimensions.ContentScale`，再回落内置默认。
2. **屏幕识别（ScreenMatcher.Match）**：优先级依次为
   ① `DeviceName` 手动绑定（`"\\.\DISPLAY1"`，改绑专用）② `Key` 型号+分辨率 ③ 旧 primary 条目（当前主屏）；
   同型号双屏靠分辨率区分，仍分不清时用"多屏"页手动改绑（把某保存配置写到该屏的 `DeviceName`）。
3. **DisplayMonitorService（新增）**：Win32 `EnumDisplayDevices/EnumDisplaySettings` 取设备名+EDID 友名+分辨率，
   与 Avalonia `Screens` 按几何配对（分辨率精确匹配优先，再按枚举序占用）；常驻
   **WidgetAnchorWindow**（1×1 透明、移出屏幕外、不进 Alt-Tab）提供 TopLevel 锚点；每 1.5s 轮询
   屏幕签名（型号+分辨率+位置+DPI），变化即触发 `ScreensChanged`。
4. **热插拔**：拔屏 → `WidgetFactory.OnScreensChanged` 关闭该屏全部窗口（配置留盘）；重插 →
   按盘上配置原位重建；新屏默认空布局（首次放卡片时 `EnsureConfig` 建条目）。
5. **跨屏拖动**：`Widget.AfterMove` 检测窗口落入另一屏 → `TransferOwnership` 把该卡片从原屏布局移到
   目标屏（坐标改相对目标屏工作区、`IWidgetLayoutProvider.ScreenId` 重绑定）；拖动前先以**原屏**格子
   解析 span（防 2×2→1×1 塌缩），手动网格下按目标屏格子重算尺寸。
6. **每屏独立配置**：网格（`screen.Grid`，GridEditor 全屏覆盖被编辑屏幕）、内容缩放（`screen.ContentScale`，
   `Widget.Scale` 优先取所属屏）、布局；`layoutProvider.DataChanged` → 每个 Widget `OnLayoutDataUpdated`
   原地应用网格/缩放变化，无需重启。
7. **备份 v2（全量 + 单屏）**：`BackupData(Version=2, AppSettings, Screens)`；
   - 全量导出 = 全局设置 + 所有屏；**单屏导出**（`BackupService.CreateSingle`）= 全局设置 + 一条屏幕配置；
   - 导入：按 Key/型号自动匹配，缺匹配落主屏，管理页可手动改绑；分辨率不同的屏幕导入时坐标按比例缩放；
   - **v1 备份拒绝导入**（Version≠2 抛 `FormatException`）；恢复后 `AppRestart` 重建窗口。
8. **多屏管理页（设置 → 多屏，新增）**：列出每块已连接屏（别名/型号/分辨率/DPI/组件数/内容缩放下拉/
   绑定下拉），每屏可：编辑网格、导出该屏、导入该屏、命名别名、手动改绑；另列出"已保存配置但未连接"
   的屏，可导出/删除；页面订阅 `ScreensChanged` 实时刷新。
9. **接线**：`App.axaml.cs` 注册 `DisplayMonitorService` + 创建锚窗 + `ScreensChanged → OnScreensChanged`；
   `WidgetFactory` / `GridService` / `Widget` / `GridEditor` / `Gallery` / `Settings` / `Advanced` 均注入显示器服务；
   `IWidgetFactory` 新增 `Add(ScreenLayout, WidgetLayout)`（旧单参 `Add(WidgetLayout)` 保留兼容）；
   `JsonParser.data` 改 `protected` 供 `LayoutProvider.Get()` 覆盖（v2 反序列化 + v1 数组回退包装）。
10. **新增/改动文件**：
    - 新增：`uWidgets.Core/Models/ScreenLayout.cs`（ScreensLayout/ScreenLayout + 工具方法）、
      `uWidgets.Core/Models/ScreenIdentity.cs`（ScreenIdentity/ScreenMatcher）、
      `uWidgets/Services/DisplayMonitorService.cs`、`uWidgets/Views/WidgetAnchorWindow.cs`、
      `uWidgets/Views/Pages/MultiScreen.axaml(.cs)`；
    - 改写：`LayoutProvider`（v2 默认 + v1 兼容）、`WidgetLayoutProvider`（带 ScreenId）、
      `BackupService`（v2 全量/单屏）、`WidgetFactory`、`GridService`、`GridEditor`、`Widget.AfterMove`
      拖动转移、`Gallery` 按屏落位、`Advanced` 网格编辑器目标屏；
    - 本地化：`Settings_MultiScreen*` 17 个条目（zh-Hans / en）。

---

## 13. 1.5.1：多屏模式逻辑梳理与修复

### 13.1 两个问题与根因

**问题 1：导入备份后在左上角出现一个 1×1 组件（两层组件重叠）**
1. **相同 Key 的双胞胎条目**（现场数据实证）：同一块 `GameViewer Virtual Display Adapter|2800x1840`
   在 `layout.json` 里出现**两条**配置。成因：屏幕首次出现时 `EnsureConfig` 建条目后**不刷新内存 Attached 列表**，
   另一块"双胞胎"屏（虚拟显示器常以两个显示设备呈现）对着**旧列表**匹配不到 → 又建一条**同 Key 空条目**。
2. **匹配不消费**：`ScreenMatcher.Match` 每条屏独立 `FirstOrDefault`，两块同 Key 的屏会**同时命中第一条配置**，
   → 同一份组件布局被渲染两次，落在相同桌面坐标 → "两层组件"；另一条永远命中不了，成为"已保存但未连接"
   （多屏页逻辑混乱的直接来源）。
3. **导入语义错位**：legacy primary 条目存**绝对桌面坐标**，而 per-screen 条目存**相对该屏工作区**。
   旧格式备份（Key=null）被导入到 per-screen 目标时坐标原样拷贝 → 相对坐标里出现绝对偏移（如 2048、2304），
   或按比例缩放后落在目标屏左上角 → "幽灵 1×1 组件"。
4. **重启重叠**：`AppRestart` 等新实例窗口出现（原 1s 上限）才关闭旧实例；慢启动时两个实例同时存活，
   所有组件短暂/持续地显示两次。

**问题 2：网格编辑器点"完成"后网格不生效**
- 网格值**保存是实时的**（编辑即写盘），但组件只在**激活（Activated）**时才 `SnapPosition` 重新吸附；
  编辑器关闭后组件停在旧位置 → "点了完成没反应"。
- 更深一层：v1 的 11 个组件全在 legacy primary 条目里（绝对坐标），其中 6 个实际位于副屏（虚拟显示器），
  而吸附/尺寸解析用的是"组件当前所在屏"的网格 → 编辑主屏网格对副屏组件无效，编辑目标与组件归属错位。

### 13.2 梳理后的设计（语义统一）

| 概念 | 规则 |
|------|------|
| **坐标系** | 主屏 legacy 条目：绝对桌面坐标（兼容 v1）；per-screen 条目：相对该屏工作区。**迁移后主屏条目只含主屏组件**，副屏组件各自落在自己的条目里 |
| **匹配** | ① 手动绑定 `DeviceName` → ② `Key`(型号+分辨率) → ③ legacy primary（仅主屏）。**消费式**：每条配置最多匹配一块屏 |
| **去重** | 同 Key 条目：空条目冗余直接剔除（保留有组件/有别名的）；两条都有组件 = 真实双胞胎屏，保留 |
| **保底** | 新屏（无配置）首用时 `EnsureConfig` 建条目并**立即刷新** Attached → 不再产生同 Key 双胞胎；随后 `MigrateLegacyWidgets` 把落在该屏的 legacy 组件拆过去 |
| **即时生效** | `layoutProvider.DataChanged` → 组件：网格变化 → 重算尺寸 + **立即重新吸附并落盘位置**（不再等激活） |
| **导入** | per-screen 备份：按目标屏分辨率比例缩放后写目标条目；legacy 备份（Key=null）：按**组件中心的绝对位置**重分配到对应屏（主屏 keep 绝对、副屏转相对）——不再产生左上角幽灵 |
| **重启** | 新实例窗口出现前等 5s（原 1s）；旧实例关闭 → 重叠窗口消失 |

### 13.3 修改清单

- `uWidgets.Core/Models/ScreenLayout.cs`：`DisplayName`/`AllWidgets` 加 `[JsonIgnore]`（不再写进 JSON）；
  新增 `ScreensLayout.Deduplicate()`（同 Key 冗余清理，未变时返回原实例）。
- `uWidgets.Core/Models/ScreenIdentity.cs`：`ScreenMatcher.Match` 新增**消费式重载**（`consumedIds`）。
- `uWidgets.Core/Services/LayoutProvider.cs`：加载时 `Deduplicate()`；`Normalize` 统一 `Version=2`。
- `uWidgets/Services/DisplayMonitorService.cs`：`Refresh` 始终刷新 `Attached`（签名不变也刷新，事件仍只在变化时触发）、
  签名加入配置 Id、消费式匹配、去重保存；`EnsureConfig` 保存后 `Refresh()` + 末尾调用迁移；
  新增 `MigrateLegacyWidgets()`（幂等拆分：legacy 组件按中心落在哪块屏 → 搬进该屏条目并转相对坐标）。
- `uWidgets/Services/WidgetFactory.cs`：`Create()` / `OnScreensChanged()` 顶部执行迁移；
  `Create()` 增加 `activeWidgets` 去重守卫（热插拔路径已建则跳过，杜绝二次创建）。
- `uWidgets/Views/Widget.axaml.cs`：`OnLayoutDataUpdated` 网格变化后立即 `SnapPosition` + 保存位置。
- `uWidgets/Views/Pages/MultiScreen.axaml.cs`：`ApplyImportTo` 增加 legacy 分支（按位置重分配）。
- `uWidgets/Services/AppRestart.cs`：握手等待 1s → 5s。
- `uWidgets/ViewModels/AdvancedViewModel.cs`（1.5.2 追加）：高级页网格数字改为读写**主屏生效网格**
  （per-screen primary 条目 Grid → 全局 AppSettings.Grid → 默认值），与网格编辑器**同一存储目标、同一回退链**；
  并订阅 `layoutProvider.DataChanged` / `appSettingsProvider.DataChanged` 即时刷新显示 —— 以前高级页
  数字绑定全局 `AppSettings.Grid`，而编辑器写入主屏条目 → 两侧不同步（"编辑器改完、高级页数字没变"）。

### 13.4 数据迁移（自动，无需手动）

下次启动自动完成：① 同 Key 空条目剔除（你当前文件里第二条 `GameViewer…2800x1840` 空配置会被删除）；
② legacy primary 里位于副屏的组件搬入副屏条目（坐标转相对，位置不变）；
③ 文件统一写回 `Version: 2` 且不再包含 `AllWidgets`/`DisplayName` 冗余字段（文件体积明显变小）。
之后"多屏"页每个物理屏只对应一条配置，网格编辑/导入导出/跨屏拖动都按屏生效。

---

## 14. 1.6.4：世界时钟重构 / 天气滚轮 / 文件夹固定模式 hover 修复

### 14.1 文件夹组件：固定模式 hover 不再截断文件名

- **问题**：固定行高（RowLayout=Fixed）时，hover 缩放的是整个条目 Border（1.12×），
  缩放以中心为原点向下扩张，会把底部文件名挤出单元格/视口而被截断。
- **方案**：固定模式下 hover 只缩放**图标**（网格模板中为 Image 增加 ScaleTransform），
  名称文字保持原位，背景高亮仍由 `:pointerover` 提供；
  Auto 模式维持原有整条目缩放。

### 14.2 天气组件：每小时滚动条支持鼠标滚轮

- **目标**：天气预报第二部分（横向滑动的逐小时条）滚轮上滑=向右滑、下滑=向左滑。
- **实现**：`ForecastWide` / `ForecastLarge` 的 HourlyScroller 上挂
  `PointerWheelChanged`（Tunnel 阶段），指针落在小时条内时把 Delta.Y 映射为
  `Offset.X += ΔY×60` 并 `Handled=true`；垂直 7 天列表不受影响。

### 14.3 世界时钟重构（2×2 / 4×2 / 4×4 / 其它自适应）

- **模型**：`WorldClockModel` 新增 `CityNames`（4 个可选自定义城市名，缺省回退时区推导名）；
  `AnalogClockViewModel.CustomCityName` 供标签使用。
- **设置**：世界时钟设置页每个时区下新增「城市名称」输入框（中英文资源
  `Clock_CityName`），留空则继续使用时区自带城市名。
- **表盘**：`AnalogWorldSingle` 增加 12 根刻度条（围绕中心代码生成，大小自适应），
  去掉 Canvas 的 80px 内边距与表盘下方三行文字，表盘明显变大；
  数字 1–12 由宿主按布局开关（`ShowNumbers`），刻度条始终显示。
- **布局**（仿 Monitor Multi-Dashboard 的定比 Board + Viewbox）：
  - **2×2**：四钟 2×2 排列，中心显示本地数字时钟（24h，无秒）；
  - **4×2**：四个钟一行 + 每列下方城市名（参考 4×2 多仪表盘），表盘更大；
  - **4×4**：2×2 四钟 + 每钟下方城市名（参考 4×4 多仪表盘）；
  - **1×1**：单钟全卡；**其它跨度**按 4 列一行 / 1 列 4 行 / 2×2 自适应。
- **代码修正**：`WorldClockViewModel` 原来每次属性访问都新建 4 个
  AnalogClockViewModel（计时器泄漏），现改为构造时缓存一份并在 Dispose 统一释放；
  数字时钟 `Digital` 随视图一起创建/释放。

### 14.4 版本号

- 主程序 `AssemblyVersion`：1.3.2 → **1.6.4**（之前 git 已到 1.6.3 但 AssemblyInfo 未同步）。
- Clock：1.1.1 → 1.2.0；Weather：1.4.1 → 1.4.2；Folders：0.2.0 → 0.2.1。

---

## 15. 1.6.5：世界时钟布局修订 + 天气滚轮修复

### 15.1 天气预报滚轮真正生效

- 1.6.4 首次实现时把 `PointerWheelChanged` 挂在 Tunnel 阶段，但鼠标滚轮是
  **Bubble** 路由事件，Tunnel 处理器根本不会触发；另外用 `Bounds.Contains`
  判断指针位置也用错了坐标系（`Bounds` 相对父级，`GetPosition` 相对自身）。
- 修复：改为 `RoutingStrategies.Bubble + handledEventsToo:true`，并用局部矩形
  `(0,0,Viewport.Width,Viewport.Height)` 判断指针是否在小时条内；
  上滑=右移、下滑=左移，垂直 7 天列表不受影响。

### 15.2 2×2 / 4×4 世界时钟：每钟中心两行（城市名 + 数字时钟）

- 需求澄清后不再在四钟中心放单个本地数字时钟，而是**每个表盘自己的中心**
  显示两行：上一行 = 自定义城市名（留空回退时区城市），下一行 = 该时区数字时钟；
  两行都居中。
- 实现：`AnalogWorldSingle` 内新增 `CenterOverlay`（表盘中心，代码控制显示），
  `AnalogClockViewModel` 增加 `DigitalTimeText`（24h，随 5s 表针刷新）。
- **4×4 与 2×2 共用同一块 200×200 Board**，整个组件由 Viewbox 等比放大，
  因此 4×4 就是 2×2 的等比放大版。

### 15.3 4×2 世界时钟：表盘上移 + 下方两行

- 四个表盘位于上方（行内 `*` 行），下方分两行：第一行城市名、第二行数字时钟；
  文字大小调小（城市 18、数字 20，单位为 Board 设计单位）。
- 上/下边距对称：条目 `Margin` 上下均为 8（Board 内比例），保证
  **表盘顶端到上边界的距离 = 数字时钟底端到下边界的距离**。

### 15.4 版本号

- 主程序 `AssemblyVersion`：1.6.4 → **1.6.5**；Clock：1.2.0 → 1.2.1；Weather：1.4.2 → 1.4.3。

---

## 16. 1.6.6：滚轮方向反转 + 城市名仅取自定义值

### 16.1 天气滚轮方向

- 1.6.5 的方向与网页习惯相反；按反馈改为：**滚轮下滑 = 右滑**（横向滚动条向右），
  滚轮上滑 = 左滑。实现上把 `Offset.X` 的增量从 `+Delta.Y×60` 改为 `-Delta.Y×60`。

### 16.2 表盘/下方城市名只用自定义输入

- `AnalogClockViewModel.CityName` 不再回退到时区推导的城市名：
  用户在城市名称设置里输入什么就显示什么；留空则显示空白（不显示时区城市）。
- 该字段同时用于 2×2/4×4 表盘中心第一行和 4×2 表盘下方第一行，行为一致。

### 16.3 版本号

- 主程序 `AssemblyVersion`：1.6.5 → **1.6.6**；Clock：1.2.1 → 1.2.2；Weather：1.4.3 → 1.4.4。

---

## 17. 1.6.7：修复世界时钟表盘城市名不显示（DataContext 被内部覆盖）

### 17.1 根因

- `AnalogWorldSingle` 的构造函数里自己创建了 `DataContext = new AnalogClockViewModel(clockModel)`，
  因此 `World` 里给 `Item0..Item3` 设置的 per-clock ViewModel（含自定义城市名/正确时区）
  **被表盘控件自身的 DataContext 覆盖**，表盘中心绑定的 CityName 永远为空、数字时钟也显示本地时区。

### 17.2 修复

- `AnalogWorldSingle` 不再拥有/创建 DataContext，也不再在 Unloaded 里释放 ViewModel
  （现在由 `World` 统一创建并释放，避免计时器泄漏）；
- `World` 的四个条目把 `AnalogClockViewModel` 作为 DataContext 传给表盘，
  表盘中心第一行显示出**自定义城市名**，第二行显示**对应时区的数字时钟**；
- 4×2 下方两行同步受益（城市名 + 数字时钟均来自 per-clock ViewModel）。

### 17.3 版本号

- 主程序 `AssemblyVersion`：1.6.6 → **1.6.7**；Clock：1.2.2 → 1.2.3。

---

## 18. 1.7.0：液态玻璃材质（静态壁纸折射 + 斜面高光）

### 18.1 定位

在**外观 → 应用主题**新增第三个表面预设「液态玻璃」（`SurfaceStyle.LiquidGlass = 2`，
此前的占位值正式启用）。它与毛玻璃的本质区别：

- 毛玻璃走 **OS 级实时模糊**（DWM `AcrylicBlur` 背板，动态壁纸跟手，见 §8.4）；
- 液态玻璃是**应用层静态材质**：抓取真实桌面像素 → 按光学模型折射/高光/色散 → 渲染成
  一张缓存位图当背景，文字与图标仍按原生 DPI 独立绘制，所以内容始终清晰。
  代价是材质不随动态壁纸逐帧更新，只在参数变化/移动/缩放时重渲染（2 秒抓图缓存）。

`Theme.UsesNativeBlur`（= 是玻璃但**不是**液态玻璃）成为「是否启用 OS 丙烯背板」的唯一判据，
`Widget`/`Settings` 的 `TransparencyLevelHint` 与 `ThemeService` 的样式切换都改用它。

### 18.2 壁纸采样源（`LiquidGlassWallpaper` + `DesktopCapturer`）

1. **首选：真实合成桌面抓图**。`PrintWindow(Progman, PW_RENDERFULLCONTENT)` 取整个虚拟桌面
   1:1 位图（`SM_*VIRTUALSCREEN` 物理像素，支持负坐标多屏），短 TTL 缓存（2 秒），
   采样时按恒等映射对齐——与 widget 背后实际显示的像素逐像素一致，不受
   任务栏替换程序（myDockFinder 等）、壁纸引擎、DWM 摆放规则影响。
   抓图前后用 16×9 采样点校验「不是纯黑、不是纯色」，失败重试 3 次。
2. **回退：壁纸文件 + 注册表**。`TranscodedWallpaper`（或 `InteropService.GetWallpaperPath`）
   + `Control Panel\Desktop\WallpaperStyle/TileWallpaper` + `Colors\Background`，
   支持填充/适应/拉伸/居中/平铺与跨屏拼接（`DrawWallpaper`）。
3. 用户可用**壁纸对准校正**对话框手调采样偏移（±1000 DIP，即时全局生效并随玻璃参数保存），
   用于抓图不可用而文件公式与真实显示错位的场景。

### 18.3 光学模型（`LiquidGlassRenderer`，1.7.0 完全参照 iOS / visionOS 原生效果重构）

**这是透镜，不是磨砂板，更不是信封折痕板**——iOS / visionOS Liquid Glass 与旧式粗暴实现的根本区别。
彻底根除此前存在的**X 型对角折痕**与**生硬白线/脏阴影高光**，完全基于真实物理透镜与 Apple HIG 规范重构：

| 维度 | 旧版问题 | 现版 iOS 原生质感重构 |
| --- | --- | --- |
| **折射范围** | 侵入组件达 0.7× 短边（波及全卡） | **严格收敛在外周弯月斜面（Meniscus Rim，12~24 DIP）**；中心平整区位移**恒等于 0**，底纹与网格绝对平直 |
| **几何法线** | 用平滑最大值判定中轴骨架，形成 45° 对角跳变（X 型撕裂折痕） | **圆角处纯径向极坐标、直边处纯正交**，切点处 $C^1$ 绝对平滑连续，**无任何中轴骨架脊线** |
| **透镜位移剖面** | 复合折返波带造成角部"蝴蝶结"扭曲 | **平滑弯月透镜曲线**：外缘为 0（无锯齿露底）、外 1/3 达到峰值光学放大、内缘平滑归零且导数为 0（$S'=0$）无缝融入平坦区 |
| **高光与边框** | 粗暴全向 158 级粉笔白线 + 45° 假划痕 | **方向性感光微细发丝边（1.2 DIP）**：迎光侧晶莹微亮、背光侧自然隐退；彻底删除中央假划痕 |
| **镜面光照** | 指数 10 的大片塑料白斑 | **弯月斜面双叶镜面高光（Dual-Lobe Specular）**：指数 28 晶莹水光 + 指数 8 柔光，紧贴迎光斜面 |
| **色彩混合** | 线性加减法（过曝冲淡原色，暗部脏黑） | **光学滤色混合（Screen Blending）**：保留底层壁纸色彩饱和度，高光晶莹通透不泛白死曝 |
| **空间厚度** | 无（或脏影） | **天光垂向渐变（+3.5% Ambient Luster）**，赋予玻璃体真实厚度与悬浮感 |

每像素处理流程：
1. **距离场与法线**：`BevelField` 在圆角扇区求径向法线，在直边求正交法线，内部平滑扇形展开，切线连续；
2. **透镜位移采样**：归一化深度 $t = d / W_{\text{lens}}$，曲线 $\sin(\pi t)\sqrt{1-t}$。$d \ge W_{\text{lens}}$ 时位移严格为 0；
3. **色散与 Vibrancy**：微量边缘色散（$t < 1$ 局域化）+ Vibrancy 1.18× 饱和度微调 + 自适应防死黑霜化；
4. **方向性发丝光**：根据 $\mathbf{n} \cdot \mathbf{L}$ 计算边线受光，迎光侧清亮，背光侧隐退；
5. **弯月面光泽**：双叶镜面反射与斜面 Fresnel 掠射光仅在迎光侧斜面上呈现；
6. **滤色合成**：先微弱遮蔽背光侧，再以光学滤色模式叠加总高光。

### 18.4 渲染管线与性能

- `LiquidGlassSurface`（`Control`）只画材质位图：属性/位置/可见性变化 → 90 ms 防抖 →
  后台线程渲染 → `revision` 号丢弃过期结果；`RenderSlots` 信号量最多 2 块同时渲染，
  像素循环 `Parallel.For` 用 `min(核数/2, 8)` 线程；离开视觉树立即释放位图。
- 超大 widget 按面积降采样（上限 2048 px / 120 万像素），文字仍按原生 DPI 绘制。
- 实测：1200×800（96 万像素）默认参数 **约 120 ms**；1280×720 预览含 4 张卡片约 1 s（含抓图与编码）。

### 18.5 设置项（外观 → 应用主题 → 液态玻璃）

| 参数 | 范围 | 默认 | 作用 |
| --- | --- | --- | --- |
| 模糊度 Blur | 0–60 | 12 | 背景高斯模糊 σ = 值×缩放/8（折射主导的轻雾：默认 12 → σ1.5 px，中心保留约 70% 壁纸结构） |
| 折射 Refraction | 0–100 | 28 | 透镜位移强度：100% = 32 DIP 环位移，**默认 28% ≈ 9 DIP 环位移、峰值 14 px = 实机强度** |
| 边缘宽度 EdgeWidth | **0–80 DIP**（1.9.6 起下限由 4 放宽到 0 = 关掉透镜环） | 24 | 透镜环宽度（位移过渡区） |
| 高光 Highlight | 0–100 | 65 | 亮边线 + 镜面强度，**65% = 实机强度** |
| 色散 Dispersion | 0–100 | 18 | 边缘棱镜色散，默认 ≈ 0.35 px（实机几乎无彩虹） |
| 光照方向 LightAngle | 0–360° | 225 | 镜面/内阴影方位（225° = 左上） |
| 边缘染色 EdgeTint | 0–100 | 50 | 自动边缘染色的强度（0 = 关闭；实机靠 vibrancy 自适应，此为可选补充） |
| 壁纸偏移 X/Y | ±1000 DIP | 0 | 手动对准校正 |

每项滑块 + 数值框双向绑定、即时保存；另有「壁纸对准校正…」「重置参数」「刷新壁纸」三个按钮。
所有值经 `Normalize()` 钳制，导入的越界/NaN 配置会回落到范围内默认值。

### 18.6 量化回归检查（`tests/LiquidGlassChecks`）

无需测试框架：`dotnet run --project tests/LiquidGlassChecks -c Release -- dist/glass-checks`。
除原有的配置兼容/模糊/折射/高光/色散/移动采样/回退/极端尺寸检查外，`OpticsProfile`
把「好不好看」变成可断言的数字（下列为 1.7.0 现版实测值）：

- **透镜剖面**：最外缘位移 **0.000 px**（保护带收敛）、峰值 **14.03 px** 落在深度 24 px 的
  肩部、中心仍有 1.47 px 的柔和整体弯曲；放大率 **×1.91**、二阶差分 0.0152 px（无接缝）；
- **圆角/中轴线**：法线每 0.4 px 转角 0.02°（连续，无缺口）；边界深度 0.50 px、中心 150 px；
- **渲染一致性**：用生产位移公式**预测**每个像素的采样位置，与实际渲染像素比对，
  平均误差 **0.284 级**（纯 8 位量化噪声）、最差 0.772 级；
- **折射可见性**：边缘 100% 与 0% 的平均差 **32.7 级**，中心 0.00 级；
- **亮边线**：峰值在最外一行、FWHM **1 px**、仅 0.50% 像素截白（线条本身）、
  迎光角比背光角亮、平面中心只有 12 级（对角光泽带）、滑块 25/50/75/100% 积分单调递增；
- **清晰度**（用 20 px 网格量「透射了多少壁纸结构」）：模糊 0/4/8/12/20/40 → 86/86/75/**70**/54/29%，
  默认档位落在 Apple 的「清透」区间；饱和色 chroma 140 → 154（110%）；暗背景 25.9 → 50.9 级；
- **真实壁纸**：玻璃**不新增**色边（卡内 |R−B| 57.1 vs 原图 69.8，模糊只会降低它）；
- **色散**：0% 时 |R−B| 恒为 0；100% 时边缘 13.2 级 vs 平面 1.08 级（保持克制）；
- **性能**：96 万像素 < 1.5 s（实测约 100–150 ms）。
- 同时输出 `optics-profile.png`（透镜剖面 + 亮边线剖面 + ×4 边缘条）、
  `liquid-glass-real.png`（**1:1** 真实壁纸两卡片 + 20 px 网格两卡片，网格可直接看出边缘弯折）、
  `liquid-glass-preview.png`（合成壁纸三卡片对比）、`optics-displacement.csv`。

### 18.7 版本号

- 主程序 `AssemblyVersion`：1.6.7 → **1.7.0**；新增包引用 `SkiaSharp 2.88.8`（与 Avalonia 自带版本一致）。

### 18.8 设计参考

- Apple Human Interface Guidelines / Technology Overviews：Liquid Glass 的 lensing、specular
  highlight、自适应着色与三条原则（hierarchy / harmony / consistency）——
  <https://developer.apple.com/documentation/technologyoverviews/liquid-glass>
- 逐项对照 iOS 26 实机测量的复刻实现（AGSL 着色器 + 参数表：lens 9–11 dp、blur 3–12 dp、
  reflW = 0.7·minDim、rimLine 3 px、adaptive lift 0.30、色散 0.018×位移）——
  <https://github.com/himanshu-lal4/react-native-liquid-glassmorphism>
- WebGL 折射玻璃拆解（SDF + 距离场梯度作法线 + 边缘强度系数）——
  <https://github.com/masakazuimai/zenn-content/blob/main/articles/liquid-glass-webgl-refraction.md>
- shuding / liquid-glass-react 的位移贴图实现（圆角矩形 SDF + smoothstep 位移）——
  <https://github.com/shuding/liquid-glass>、<https://github.com/rdev/liquid-glass-react>

---

## 19. 音乐播放器小组件（WinRT SMTC 智能识别 + 2×2/4×2/4×4 macOS 三形态）

### 19.1 定位与系统集成

深度复刻 macOS Sonoma / Sequoia 原生音乐组件的美学与操作手感，新增独立的 `Music` 插件模块（`src/Widgets/Music/`）：
- **Windows 原生媒体控制体系（WinRT SMTC）**：直接调用 Windows 系统的 `GlobalSystemMediaTransportControlsSessionManager`，毫秒级响应歌曲信息、专辑封面、播放/暂停、切歌与时间进度。
- **目标框架**：`net8.0-windows10.0.19041.0`（通过 `NoWarn CA1416` 消除平台兼容告警），完全二进制兼容宿主 `net8.0`；
- **单文件打包兼容**：自解压产物被打包构建脚本 `build-widgets-bundle.ps1` 自动识别并压入 `widgets.zip`，单文件 `uWidgets.exe` 交付不受影响。

### 19.2 软件自选与智能识别机制

1. **软件规则模型（`PlayerRule`）**：
   - 字段：`Name`（友好名称）、`MatchPattern`（AUMID 或进程名关键字，支持 `|` 分割）、`IsEnabled`（开关）、`Priority`（优先级序号）、`ExePath`（可选自定义可执行文件路径）。
2. **出厂预设库（`PresetPlayers`）**：
   - 预置国内及国际主流音乐软件：网易云音乐 (`cloudmusic`)、QQ音乐 (`qqmusic`)、Spotify (`spotify`)、Apple Music (`applemusic`)、汽水音乐 (`qishui`)、酷狗音乐 (`kugou`)、酷我音乐 (`kwmusic`)、Foobar2000 (`foobar2000`)、YesPlayMusic (`yesplaymusic`)、Windows 媒体播放器 (`mediaplayer`) 以及主流浏览器 (`chrome|msedge|firefox|brave`)。
3. **智能识别算法（`MediaManagerService`）**：
   - 监听 `SessionsChanged` 与 `CurrentSessionChanged`；
   - 优先捕捉正在播放（`Playing`）的受支持软件；若多个软件同时播放，按用户规则列表优先级（从上到下）选取；若所有软件均处于暂停状态，则展示最后活跃的软件；无活跃媒体时进入优雅的空闲（Idle）状态。
4. **设置面板交互（`MusicSettings`）**：
   - 可一键从当前系统运行的活跃媒体会话中添加软件；
   - 可通过文件选择器选取本地 `.exe` 文件添加；
   - 支持手动输入规则名称与匹配特征；
   - 支持软件启用/禁用勾选、删除以及 ↑/↓ 优先级动态调整；
   - 具备“恢复默认预设”按钮。

### 19.3 视觉风格与三形态自适应（macOS 质感）

依托 `SizeTiers.ResolveTier(this, e.NewSize)` 实现 2×2 / 4×2 / 4×4 三种形态的无缝切换与高 DPI 贴合：
1. **2×2（Small，紧凑形态）**：
   - 左侧 68×68 大圆角（12px）专辑封面（无封面时呈现黑胶唱片质感矢量图示）；
   - 右侧播放源小胶囊徽标 + 歌曲名加粗（单行省略）+ 歌手名；
   - 底部全宽可交互细时间进度条（支持点击跳转 Seek）+ 迷你经典播放控制组（上一首、播放/暂停胶囊按键、下一首）。
2. **4×2（Medium，经典长条）**：
   - 左侧 104×104 高清大圆角（16px）专辑封面，带自然微外阴影与 Apple 质感边框；
   - 右侧多行排布：播放源指示灯胶囊、16px 加粗歌曲标题、歌手与专辑名、细胶囊时间进度条与双侧时间标签（当前/总时长）、居中排列的经典大号圆球播放/暂停键与切歌按钮。
3. **4×4（Large，沉浸大画框）**：
   - 上部大画框 184×184 专辑封面（20px 圆角），居中悬浮；
   - 下部宏大的居中歌曲标题（18px）、艺术家、交互式全长进度条与大号控制中心按钮组；
   - 背景支持基于专辑封面的动态流光漫反射氛围（Ambient Glow）。
4. **原位无损刷新（`IWidgetSelfRefreshing`）**：
   - 实现 `IWidgetSelfRefreshing` 契约，在修改设置并保存时在内存中更新配置并通知绑定，避免重建控件导致播放监听中断或界面闪烁。

---

## 20. 1.7.1：单文件组件（Folders 插件内）与音乐播放器排版精细化

### 20.1 “文件夹”分类内新增“单文件”组件（`SingleFile`）
1. **定位与注册**：
   - 位于 `src/Widgets/Folders/`，通过 `[assembly: WidgetInfo(typeof(SingleFile), typeof(SingleFileModel), typeof(SingleFileSettings), "Folders_SingleFile_Title", "Folders_SingleFile_Subtitle", defaultColumns: 1, defaultRows: 1)]` 注册。
   - 契约层扩展：`WidgetInfoAttribute` 扩充 `defaultColumns` 与 `defaultRows`（默认 2×2），使单文件组件在 Gallery 添加时默认初始化为 **1×1**，其余组件保持 2×2。
2. **交互与排版**：
   - **居中放置，无名称**：仅展示文件/文件夹的高清 Shell 图标，严格居中，不占位显示文字文件名。
   - **图标比例自适应**：支持自定义图标占据组件比例（`IconPercent`，默认 70%，范围 10%~100%）。无论组件缩放至多大，图标均按短边百分比动态居中重绘。
   - **拖入绑定**：支持 Avalonia 原生 DragDrop 与 Windows 原生 OLE 拖放（`WidgetOleDropTarget`），拖入单个文件或文件夹即可自动绑定；空状态下显示轻量引导提示。
   - **双击/单击启动**：左键单击直接通过默认关联程序启动（`Process.Start`，自动屏蔽 Ctrl 窗口拖动）。
   - **编辑设置面板**：右键编辑窗口可选择文件、选择文件夹、清空或滑动调节图标大小比例，实时无损刷新（`IWidgetSelfRefreshing`）。

### 20.2 音乐播放器排版精细化
1. **4×2 组件封面与进度条几何对称**：
   - 封面左边距调整为与上下边距严格相等（`pad = (Height - coverSide) / 2.0`），消除此前水平居中导致的左右过宽感。
   - 进度条（`ProgressBar`）与右侧歌名/艺术家文字左对齐，并横向拉伸至最右端距离卡片右边界刚好等于 `pad`，实现两端等宽的 macOS 原生对称感。
2. **2×2 组件呼吸感与进度条对齐**：
   - 顶部封面与文字解除固定高度限制，进度条与底部控制按钮适度下移，为上方歌名腾出充足垂直空间。
   - 进度条长度从居中 72px 调整为与上方两端贴合：左侧对齐封面左边缘，右侧对齐文字最右侧边界。

---

## 21. 1.7.2：“工具”小组件模块（剪贴板多格式监听 + 智能双向对译）

### 21.1 模块架构与分类注册
- **新增模块**：`src/Widgets/Tools/`（独立程序集 `Tools.dll`），统一归入主程序设置与小组件库的 **“工具”（Tools）** 分类。
- **程序集特性声明**：
  ```csharp
  [assembly: WidgetInfo(typeof(ClipboardView), typeof(ClipboardModel), typeof(ClipboardSettings), "Tools_Clipboard_Title", "Tools_Clipboard_Subtitle", defaultColumns: 2, defaultRows: 2)]
  [assembly: WidgetInfo(typeof(TranslatorView), typeof(TranslatorModel), typeof(TranslatorSettings), "Tools_Translator_Title", "Tools_Translator_Subtitle", defaultColumns: 4, defaultRows: 2)]
  [assembly: Locale(typeof(Locale), "Tools", "M22.7,19 L13.6,9.9 C14.5,7.6 14,4.9 12.1,3 C10.1,1 7.1,0.6 4.7,1.7 L9,6 L6,9 L1.7,4.7 C0.6,7.1 1,10.1 3,12.1 C4.9,14 7.6,14.5 9.9,13.6 L19,22.7 C19.4,23.1 20,23.1 20.4,22.7 L22.7,20.4 C23.1,20 23.1,19.4 22.7,19 Z")]
  ```
- **单文件打包兼容**：通过 `build-widgets-bundle.ps1` 自动集成进 `widgets.zip`，交付单文件 `uWidgets.exe` 零配置自解压可用。

### 21.2 剪贴板小组件（`Clipboard`）
1. **轻量无感后台监听**：
   - 基于 Win32 `GetClipboardSequenceNumber()` 毫秒级轮询，无入侵性窗口钩子；
   - 支持文本（`CF_UNICODETEXT`）、文件列表（`CF_HDROP`）、位图图片（`CF_DIB` / `CF_DIBV5`）；
   - 图片持久化转换并缓存至 `%LocalAppData%\uWidgets\ClipboardCache\*.png`。
2. **历史管理与回写复用**：
   - 设置面板支持调节保存最近条数上限 $N$（5 ~ 100 条，默认 20 条），超出上限自动轮换并物理清理失效图片；
   - 卡片单击或点击复制按钮，可将对应数据无缝灌入系统剪贴板（文本写入字符串、文件写入标准 `DROPFILES` 结构体支持在资源管理器中直接 `Ctrl+V` 粘贴、图片写入位图数据）。
3. **视觉与交互**：
   - 具备条数统计徽标、一键清空、全部/文本/图片/文件分类筛选标签；
   - 复制操作触发全局反馈气泡（Toast Banner，1.5 秒自动隐去）。

### 21.3 翻译小组件（`Translator`）
1. **双框上下对译结构**：
   - 上框：源文本输入框，带字数统计与一键清空按钮；
   - 中间栏：源语言下拉、目标语言下拉、**“⇄” 语言与文本反向对译切换按钮**、翻译源选择、状态动效与即时翻译按钮；
   - 下框：译文只读框，带一键复制到剪贴板按钮与错误诊断。
2. **多源驱动与国内直连免 Key 保障**：
   - **有道翻译（默认推荐）**：基于有道轻量端接口，国内直连无需 Key，响应时间 < 1s，支持自动语种识别；
   - **MyMemory**：国际公开翻译知识库直连免 Key；
   - **百度翻译开放平台**：支持用户在设置中填入个人 AppID 与 SecretKey；
   - **DeepLX / 自定义接口**：支持在设置中自定义配置本地或远程代理接口。
3. **智能交互细节**：
   - 键入停止 600ms 后自动触发防抖异步翻译；回车键即刻发起；
   - 点击“⇄”按钮时，互换语种并自动把当前译文反转为输入内容，实现无缝双向对比。

---

## 22. 1.7.3：“搜索”独立分类与全尺寸小组件（1×1/2×2/4×1/4×2/4×4 自适应 + 下拉菜单与自定义引擎）

### 22.1 模块定位与分类声明
- **新增模块**：`src/Widgets/Search/`（独立程序集 `Search.dll`），归入全新的 **“搜索”（Search）** 分类。
- **程序集特性声明**：
  ```csharp
  [assembly: WidgetInfo(typeof(SearchView), typeof(SearchModel), typeof(SearchSettings), "Search_Title", "Search_Subtitle", defaultColumns: 4, defaultRows: 1)]
  [assembly: Locale(typeof(Locale), "Search", "M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 14z")]
  ```
- **单文件自动集成**：通过 `build-widgets-bundle.ps1` 编译并打入 `widgets.zip`，单文件 `uWidgets.exe` 自解压即用。

### 22.2 5 种形态视觉与交互自适应
1. **1×1（微型启动器）**：居中展示当前引擎高清矢量 Logo，带右下角悬浮放大镜气泡和底部引擎名标签。单击弹出即输即搜快速输入浮窗（`Flyout`），回车即刻发起搜索并关闭浮窗；右键直接切换引擎。
2. **4×1（经典单行 Spotlight）**：左侧当前引擎胶囊选择器（图标+名称+`▾`下拉） + 纵向分割线 + 中间无边框伸缩输入框（占位符、回车触发） + 清空键（`✕`） + 右侧圆形强调色搜索按钮。
3. **2×2（正方卡片）**：顶部引擎选择栏（含快速打开官网图标） + 中部胶囊搜索栏 + 底部常用 4 大引擎快捷切换圆形图标 Dock。
4. **4×2（中型卡片）**：顶部引擎选择与分类药丸标签（全部/通用/开发/AI） + 中部 Spotlight 搜索栏 + 底部 7 大主流引擎图标横向 Dock 与历史搜索下拉按键。
5. **4×4（大型搜索控制台）**：顶部品牌与分类药丸 + 中部大号搜索栏 + 核心 8 引擎 4×2 矩阵直达网格（点击单项直接用当前词发起对应引擎搜索） + 底部最近历史搜索词流（点击重搜、一键清空）。

### 22.3 下拉菜单与引擎自定义管理
- **即时下拉菜单**：点击任意形态中的引擎图标或按钮，即可弹出附带品牌彩色图标和选中对勾标记（`✓`）的引擎切换菜单，底部附带“在浏览器打开官网”选项；
- **预设库**：预置 Google、必应、百度、DuckDuckGo、GitHub、哔哩哔哩、知乎、秘塔 AI、Kimi、YouTube 等 10 大常用引擎；
- **设置面板**：支持新增自定义引擎（名称、URL 模板 `https://...{q}`、分类、图标、品牌色）、编辑已有引擎、删除、上移/下移调整优先级、一键恢复出厂预设，以及搜索后自动清空输入框与历史记录开关。

### 22.4 离线视觉无损检验与断言
- 提供独立的无头视觉与布局验证工具（`tests/SearchVisualChecks`），自动将 5 种尺寸分别在空白态与输入态下测量排版并渲染出 2× 高清位图（`dist/search-checks/*.png`），断言所有视觉节点的布局尺寸无 NaN/越界/截断，且经图像核验确保边距均匀、文字与按钮无重叠。

---

## 23. 1.7.4：“无边框时钟”（满格顶头拉伸、手机锁屏艺术大字集、三大主题深度适配）

### 23.1 需求背景与核心特性
1. **满格填充（顶天立地）**：
   - 彻底脱离传统小组件的卡片框架与外层圆角衬底，实现小组件内容直接与桌面壁纸融合；
   - 数字（例如 `12:10`）严格顶到网格单元格上下边缘（$y=0$ 至 $y=H$），左右支持直接充满拉伸（`StretchFill`）或等比居中；
   - 消除 OpenType 字体自带的 Internal Leading（字形上/下空隙）与系统边距。
2. **手机锁屏精选艺术字体集（Curated Lockscreen Fonts）**：
   - 参考 iOS 16+、小米 HyperOS、vivo OriginOS、Android 14 等手机锁屏时钟大字风格，预置精选艺术字体组并内置极佳拉伸字体：
     - **🔥 华为锁屏超窄体（HarmonyOS Sans Condensed）**：开箱即体内置打包至 `Clock.dll`，专为纵向拉伸设计，高度满格视觉极其震撼；
     - **🔥 iOS 16 经典厚重块体（Impact）**：美式重型大字，块状饱满力量感；
     - **🔥 德国工业精工 DIN（Bahnschrift）**：现代工业标准几何窄体，规整严谨；
     - **🔥 超粗硬核无衬线（Arial Black）**：超宽实心黑体，视觉存在感极强；
     - **🔥 iOS 高定复古衬线（Georgia）**：粗细笔触优雅对比，潮流时尚锁屏大字；
     - **🔥 包豪斯极简几何（Century Gothic）**：纯粹几何圆融线条，清新现代美学；
     - **🔥 赛博极客终端等宽（Cascadia Code）**：代码终端科技质感，工整硬派风格；
     - **🔥 自由随性自然手写（Ink Free）**：灵动生活手写笔触，亲和随性自然；
     - **🔥 文艺古典罗马体（Palatino Linotype）**：人文主义典雅字形；
     - **🔥 原生流体现代体（Segoe UI Variable）**：微软最新原生流体设计字族；
   - 兼顾系统所有已安装字体，字体设置面板带有精选大字标题与特性描述。
3. **字重粗度全阶梯自定义（100~900）**：
   - 提供从 100 Thin（纤细）、200 ExtraLight、300 Light、400 Normal、500 Medium、600 SemiBold、700 Bold（默认粗体）、800 ExtraBold 到 900 Black（浓黑）的完整阶梯选择。
4. **三大主题深度差异化适配（Three-Theme Deep Adaptation）**：
   - **毛玻璃（Acrylic）· OS 级硬件实时模糊**：
     - 宿主窗口透明度设为 `WindowTransparencyLevel.AcrylicBlur`；
     - 通过 `InteropService.SetWindowRegionFromSpans` 基于 Win32 `ExtCreateRegion` 与 `RGNDATA`，将数字字形的高清扫描线实时绑定为窗口物理区域（HWND Region）；
     - Windows DWM 硬件级逐帧对数字内部采样桌面合成毛玻璃，对动态壁纸（Wallpaper Engine 等）、视频壁纸及后台窗口移动实现 **60fps/144fps 零延迟实时跟手**，彻底解决静态快照不跟手问题；
     - 数字表面叠加亚克力微光色调与立体反光描边。
   - **液态玻璃（Liquid Glass）· 数字本体 3D 折射光学**：
     - 宿主窗口透明度设为 `WindowTransparencyLevel.Transparent`，清除 HWND Region 保持 32 位全透明；
     - 建立专用的 `GlyphLiquidGlassRenderer` 服务，使用 2D 欧几里得距离变换（EDT）计算字符笔画内每个像素到外边缘的精确深度 `depth` 与表面法线 $(n_x, n_y)$；
     - 沿每个数字的笔画边缘实现真实的凹凸透镜折射位移（Lens Refraction）、色散光斑（Dispersion）、3D 倾斜光源高光（Specular Glint/Sheen）、1.6px 玻璃轮廓线与色彩饱和度提亮；
     - 使得 1、2、:、1、0 每一个字符本身都成为晶莹剔透的 3D 实体玻璃。
   - **纯色（Solid）· 纯粹矢量抗锯齿填充**：
     - 清除 HWND Region，直接以矢量抗锯齿填充主题背景色/前景色或自定义强调色，支持透明度滑块调节，零模糊、零多余计算。
5. **自定义遮罩与强调色跟随**：
   - 可在数字表面叠加自定义颜色遮罩（Overlay Tint），支持一键勾选“跟随强调色”联动系统全局 Accent Color，或手动输入 Hex 色值与滑动调节不透明度。
6. **原位平滑刷新（`IWidgetSelfRefreshing`）**：
   - 设置面板保存时通过内存注入实时触发重绘，无闪烁、无窗口重建。

### 23.2 关键架构与核心代码
1. **底层 Win32 多边形区域引擎（`InteropService.cs`）**：
   - `SetWindowRegionFromSpans(Window window, IReadOnlyList<(int Left, int Top, int Right, int Bottom)> rects)`：直接将水平扫描线打包为 `RGNDATA` 并调用 `ExtCreateRegion`，0.1ms 内生成原生 HWND 剪裁区域。
2. **宿主无边框透明度自适应（`Widget.axaml.cs`）**：
   - `ApplyTransparencyHint` 动态识别：无边框组件在毛玻璃模式下启用 `AcrylicBlur`，在液态玻璃/纯色模式下启用 `Transparent`；
   - `ApplyWidgetRegion` 动态适配：毛玻璃模式下由子组件接管字形 Region，液态玻璃/纯色模式下调用 `ClearWidgetRegion`。
3. **字形本体液态玻璃光学引擎（`GlyphLiquidGlassRenderer.cs`）**：
   - 基于 2-pass 欧几里得距离场变换，计算字符轮廓法线并执行凹凸透镜光线弯曲、色散分离与 3D 镜面反光。
4. **视觉与布局自动化检验（`ClockVisualChecks`）**：
   - 针对 14 种测试用例（华为超窄体、Impact 粗块体、Georgia 高定衬线、包豪斯几何、DIN 工业窄体、极端 100/900 字重、强调色/金色遮罩、毛玻璃/液态玻璃/纯色独立主题切换等）进行无头测量渲染，输出高清 PNG 验证图至 `dist/frameless-clock-checks/`。

### 23.3 1.7.5 进阶精进：光学精细化、独立主题、官方选色器与后台预缓存自动清理引擎

1. **数字液态玻璃光学精细化（Subtle & Crisp Bevel）**：
   - 彻底优化字形折射光学参数：将 `LensDips` 由 $28\text{dp}$ 收敛至 $10\text{dp}$；
   - 折射边缘宽度 `lensWidth` 引入 $0.25\times$ 动态收敛比例，严格限制在 $1.5\text{dp} \sim 4.5\text{dp}$ 的外圈细窄斜面；
   - 数字笔画中央大部分区域保持纯粹光学平整与水晶般高透，彻底避免内部过度扭曲变形，同时笔画外缘保留极具立体感的 iOS 18 / VisionOS 风格菲涅尔高光与微细色散光边。
2. **小组件独立视觉主题选择（Per-Widget Theme Override）**：
   - `FramelessClockModel` 新增 `ThemeMode` 配置（`0`: 跟随全局主题，`1`: 毛玻璃 Acrylic，`2`: 液态玻璃 Liquid Glass，`3`: 纯色 Solid）；
   - `FramelessClockSettings.axaml` 顶部引入标准下拉选择框，支持单独定制无边框时钟的渲染主题；
   - 视图层动态接管宿主窗口的 `TransparencyLevelHint`：Acrylic 模式自动切换至 `AcrylicBlur` 并应用字形扫描线 Region；Liquid Glass / Solid 模式自动重置为 `Transparent` 并清除 Region，实现无需重启、零闪烁的无缝切换。
3. **引入官方标准选色器模块（ColorPicker）**：
   - `Clock.csproj` 引入 `Avalonia.Controls.ColorPicker`；
   - 在自定义遮罩颜色设置项中嵌入标准 `ColorPicker`（`Height="36" Width="72" IsAlphaVisible="False"`），与日历/外观等模块完全一致；
   - `FramelessClockSettingsViewModel` 实现 `Color` 与 `#RRGGBB` 十六进制字符串的双向联动同步。
4. **液态玻璃后台预缓存与全自动内存清理引擎（Background Pre-Caching & Auto-Cleanup）**：
   - **提前缓存下一分钟**：在分钟模式下，当前时间为 $T$ 时，除快速展示或渲染当前帧外，后台线程利用 `Task.Run` 与 `CancellationToken` 异步静默计算并渲染下一分钟 $T+1\text{m}$ 的液态玻璃帧存入缓存；当整点时间推进到下一分钟时，**0ms 瞬时命中缓存切换**，彻底根除 Skia 距离场与光线追踪造成的微小帧延迟；
   - **秒级滚动预渲染**：在显示秒数模式下，后台维护滑动预渲染缓冲队列（超前预渲染未来 1~5 秒），确保秒针步进流畅无阻；
   - **严密全自动内存清理（Zero-Leak Automatic Cleanup）**：
     - **时间推进剔除**：每次时钟滴答触发 `EvictExpiredCacheEntries`，立即 Dispose 释放并从缓存字典中移除早于当前时间的旧 Bitmap；
     - **状态失效清空**：当窗口发生拖拽移动、跨显示器漫游、尺寸缩放、字体/字重切换、主题切换时，触发 `ClearLiquidGlassCache` 瞬间取消所有后台排队任务并释放全部已缓存 Bitmap；
     - **组件卸载彻底销毁**：小组件关闭或移除时，取消所有任务、清空字典并 Dispose 当前显示的位图与 Win32 HWND 区域，杜绝内存泄漏。

---

## 24. 1.8.1：无边框时钟跟随全局液态玻璃、全屏/最大化按屏判定、内存泄漏治理、Fixed 整数倍放大、日历镂空数字

### 24.1 无边框时钟「跟随全局主题」= 真正的液态玻璃（`GlyphLiquidGlassRenderer`）
- **根因**：`FramelessClockModel.RefractionWidth` 默认 `0`，而渲染器把 `0` 解释为「不做折射透镜」——于是数字内部只剩「模糊后的壁纸 + 着色」，看起来就是一块毛玻璃；同时 `GlyphLiquidGlassRenderer` 走的是 `wallpaper.ImageBytes`（桌面抓图 PNG 的惰性编码结果），既丢掉了实时抓图（实时抓图只有 `CachedBitmap`），又在每一帧把整个虚拟桌面编码/解码一次。
- **修复**：`RefractionWidth <= 0` 改为「自动」——按全局光学参数（`EdgeWidth` + `Refraction`）求解透镜，与卡片材质同源；`> 0` 仍为手动覆盖。抽出 `ResolveLens()` 便于数值断言，并把退化掩膜下 `Math.Clamp` 上下界倒置会抛异常、静默中断整帧渲染的隐患改为 `SoftClamp`。壁纸采样改回 `CachedBitmap` 优先。数字的采样模糊 `sigma` 收敛到透镜宽度之下（`≤ max(0.75·scale, 0.55·lensW)`），否则模糊会先把透镜要弯的细节抹平。
- 设置项文案改为「0 = 自动跟随全局液态玻璃折射参数」。
- 回归：`tests/ClockThemeChecks` 新增断言——默认 0 必须产生真实透镜、显式值仍可覆盖、**实时抓图必须真的被采样**（合成壁纸的红/蓝必须出现在数字像素里），并输出 `glyph-lens-preview.png`。

### 24.2 全屏 / 最大化：按屏判定 + 不再隐藏组件（`FullscreenWatcherService` / `WidgetFactory`）
- **判据改为「所有屏都被覆盖」**：`AllScreensCovered(screens, windows)` 逐屏判断，最大化窗口按「工作区覆盖 / 屏幕中心点归属」认领自己那块屏；独占全屏（D3D/演示模式）视为覆盖全部屏幕。只要还有一块屏可见就不降占用——副屏组件不会因为主屏最大化游戏而停摆。
- **窗口不再隐藏**：组件本来就在 z 序最底层，隐藏再恢复才是「回到桌面后要等一会儿才出现」的原因。现在只暂停共享定时器 + 释放缓存：`LiquidGlassWallpaper.Release()` 丢掉整张桌面抓图（进程内最大的一块分配），时钟保留「当前显示的那一帧」只清预渲染队列，避免恢复瞬间闪回兜底渲染。
- 检测新增 DWM cloaked 过滤（挂起的 UWP / 其它虚拟桌面窗口不再被当作覆盖），`SHQueryUserNotificationState` 与几何判定保持不变。
- 回归：`tests/FullscreenChecks` 重写为多屏语义（仅主屏最大化 / 仅副屏全屏 / 双屏各一个最大化 / 跨屏全屏 / cloaked 等 23 项）。

### 24.3 内存治理（实测：数小时长会话 700 MB → 稳定 ~280 MB 且不再增长）
审计定位到的实际泄漏（全部已修）：
- **剪贴板历史**：每张图按原始分辨率解码（1080p ≈ 8.3 MB/张、4K ≈ 33 MB/张，上限 20 张 = 最多 600 MB），且移除/裁剪/清空都只丢引用不 Dispose。现改为 `DecodeToWidth(320)` 缩略图 + 全路径 Dispose（延迟到渲染之后，避免画到已释放位图）；DIB 读取去掉一次全图 LOH 拷贝。
- **音乐组件**：实例从不 Dispose（250 ms DispatcherTimer + SMTC 事件 + 1.5 s 轮询 Timer 常驻），封面按原图解码且替换时不释放；设置页自己还 new 了一个 SMTC 服务。现在视图 Unloaded 释放、Loaded 重建，封面限宽 256 px。
- **静态订阅泄漏**：`BigFolder` 订阅静态 `WallpaperInvalidated` 且没有退订入口（宿主每次保存设置就重建视图 ⇒ 每次泄漏一整个组件）；`Month` 订阅 `Application.Current.ActualThemeVariantChanged` 不退订；`Widget` 的 `ContextMenu.Opened` 匿名委托挂在窗口自身上。均已补 `-=`（大文件夹经 `LiquidGlassBridge.UnsubscribeWallpaperInvalidated`）。
- **Gallery 预览**：页面缓存 + 每个组件类型都实例化真控件（28 个），现在离树即释放、再次进入重建。
- **视图模型生命周期**：Weather 六个视图、Monitor、Progress、Fixed 的 VM 都订阅进程级定时器并各自持有 `HttpClient` 却从不释放；现统一为 Unloaded 释放 / Loaded 重建（`Dispose` 幂等）。`MetricService` 的 WMI `ManagementObjectCollection`/`ManagementObject` 全部 using 释放。
- **缓存预算**：文件夹图标缓存 160 → 64（每个 256 px 图标 ≈ 256 KB）；无边框时钟预渲染帧上限由「70 帧」（1080p 下最多 580 MB）改为 48 MB 内存预算（6~12 帧）；字形渲染器的 8 个 `width×height` 缓冲改为按像素数池化复用（每帧 ~29 B/px 的 LOH 抖动 → 0）。
- **`TrimProcessMemory`** 的 GC 由非阻塞改为**阻塞 + 压缩**（原来工作集裁剪在回收完成前就执行，所以全屏暂停时「内存没降」）；新增 `MemoryTrimmerService`：每 5 分钟、且进程私有内存超过 150 MB 时，在后台线程做一次回收 + 工作集裁剪，压平长会话的缓慢爬升。

### 24.4 需求：Fixed（4×2 仪表盘）支持整数倍放大
- `IsAllowedSpan` 由「4 的倍数 × 2 的倍数」放宽为「列 = 2 × 行」：4×2 / 6×3 / 8×4 / 10×5 …；`SnapSpan` 统一到同一格点（`k = round((列/2 + 行)/2, AwayFromZero)`，下限 k = 2），两个步进器都能走到 6×3 不再回弹。右键菜单新增「6×3（等比放大 150%）」。内容本身是 `Viewbox` 里的定比矢量设计，等比放大无需额外适配。

### 24.5 需求：日历默认镂空今天数字 + 深浅模式圆点颜色独立
- 新增 `Views/Controls/HollowTodayMark`：把「今天」画成**一个几何体**——圆盘与日期数字轮廓做 `GeometryCombineMode.Exclude` 布尔差，数字处成为真正的镂空（透出卡片背后的液态玻璃/壁纸），而不是在圆盘上叠一层文字。`MonthCalendarModel.HollowTodayNumber` 默认 `true`；关闭时回落到「圆盘 + 白色数字」的旧观感。设置页新增「镂空今天数字」开关。
- 圆点颜色沿用 `TodayColorLight` / `TodayColorDark`（浅色/深色分别可调），切到「自定义颜色」时用当前强调色预填两个主题的取色器，避免其中一种模式静默回落。资源键新增 `Calendar_HollowToday(_Subtitle)`（中英）。
- 回归：新增 `tests/CalendarHollowChecks`——离屏渲染真实 `Month` 控件，按像素断言「圆盘存在 + 数字处透明（镂空）」，并断言关闭镂空时数字以白色绘制、盘内无透明像素。

---

## 25. 1.8.2：多屏独立网格排布与网格编辑器精准物理定位

### 25.1 需求背景与数据流隔离
- **根因分析**：
  在多显示器环境下，用户为副屏调整网格排布时，`GridEditor.axaml.cs` 在保存时无条件执行了 `appSettingsProvider.Save(appSettingsProvider.Get() with { Grid = grid })`。这导致每次编辑任何副屏的网格时，全局默认网格 `appSettings.Grid` 都会被意外覆盖污染，连带破坏主屏及其它继承全局设置的屏幕排布。
- **数据流隔离修复**：
  - 当编辑特定屏幕（`screenId != null`）时，只持久化更新对应的 `ScreenLayout.Grid`（保存至 `layoutProvider`），明确跳过对 `appSettingsProvider.Save(...)` 的调用；
  - 继承与重置语义：当屏幕的 `ScreenLayout.Grid` 为非空时表示该屏幕拥有专属独立网格规则；为 `null` 时自动继承全局默认 `appSettings.Grid`；
  - 在「多屏」设置页与「高级」设置页中均提供「恢复默认网格」操作，将对应屏幕的 `ScreenLayout.Grid` 置为 `null`，重归全局继承。

### 25.2 网格编辑器（GridEditor）精准物理屏幕定位
- **跨屏定位失效排查**：
  - `GridEditor` 原先仅接收 `string? screenId`，并通过 `displayMonitor.FindByConfigId` 反查。但从设置界面直接点击打开时，若目标副屏上尚未添加任何小组件，`Attached` 集合为空，导致定位失效并错误回退到主屏 `Screens.Primary`；
  - Avalonia 在 Windows 系统中直接通过 `Window.Position` 定位时，在包含负坐标屏幕或混合 DPI 缩放的多屏桌面下，常因窗口管理器介入而被钳制或弹回主显示器。
- **底层物理定位方案**：
  - `GridEditor` 构造函数扩展支持直接传入目标物理屏幕实例 `Screen? targetScreen`，彻底脱离对小组件附加态的依赖；
  - 引入底层 Win32 `SetWindowPos`：在 `InteropService` 中封装 `SetWindowPosition(Window window, int x, int y, int width, int height, bool topmost = false)`，利用原生 API 以绝对物理像素精确定位；
  - 在 `GridEditor.OnOpened` 中通过 `ApplyScreenPlacement()` 锁定目标屏幕的 `Bounds` 物理边界（`target.Bounds.X, target.Bounds.Y, target.Bounds.Width, target.Bounds.Height`），并设置 `HWND_TOPMOST` 与 `SWP_SHOWWINDOW`，确保编辑蒙版 100% 精确覆盖在被编辑的物理屏幕上；
  - 顶部编辑工具栏动态显示当前目标屏幕的型号与物理分辨率标签（例如 `[DELL U2723QE · 2560×1440] 8列 × 6行，格子边长 128px`），编辑对象一目了然。

### 25.3 设置界面多屏协同交互完善
- **「多屏」设置页（`MultiScreen.axaml.cs`）**：
  - 各屏幕卡片直观显示当前网格状态（独立自定义参数或跟随全局默认）；
  - 点击卡片内的「编辑网格」直接传入对应的物理 `Screen` 实例，在目标屏幕上快速拉起全屏网格编辑器；
  - 当屏幕为自定义网格时，卡片内动态出现「恢复默认网格」按键，方便快速重置；
  - 完善事件监听与退订（`layoutProvider.DataChanged` 与 `appSettingsProvider.DataChanged`），避免内存泄漏。
- **「高级」设置页（`Advanced.axaml` / `AdvancedViewModel.cs`）**：
  - 顶部新增「网格排布目标屏幕」下拉选择器（`ScreenGridTargetOption`），可切换「全局默认」或具体某个屏幕（带有型号、索引与分辨率标注）；
  - 行数、列数、边距、留白等数值输入框双向绑定当前选中屏幕的网格参数，编辑数值即时保存至对应屏幕，各屏互不干扰；
  - 选中的屏幕拥有自定义网格时，提供「恢复默认网格」按钮；
  - 底部「编辑网格」按钮自动在当前所选的物理屏幕上弹出 `GridEditor`。

### 25.4 自动化验证与工程交付
- **独立检验程序（`tests/MultiScreenGridChecks`）**：
  - 编写专用多屏网格检验套件，覆盖数据流隔离、物理坐标跨屏计算、独立参数修改、恢复默认以及 DPI 缩放下的边界锁定等 5 大核心场景，断言全部通过；
  - 回归测试：`CalendarHollowChecks`、`ClockThemeChecks`、`FullscreenChecks` 全数通过（100%），全解决方案编译 0 错误 0 警告。

---

## 26. 1.8.6：多主题配置完全独立隔离、废弃虚拟网格模式、自由布局原生缩放手柄与像素级尺寸自定义

### 26.1 主题配置独立持久化体系（SurfaceThemes）
- **核心痛点**：
  - 各主题（亚克力、纯色、液态玻璃、多彩）此前共用全局扁平的 `Theme` 结构；切换到多彩主题时强制将单色主题设为 false 并重写透明度；切回液态玻璃时，不仅个性化设置丢失，且因 `AppearanceViewModel.DataChanged` 缺乏通知，导致 UI 显示开关开启但单色黑白下拉框消失、样式失效。
- **架构方案**：
  - 在 `AppSettings` 中引入 `Dictionary<string, Theme>? SurfaceThemes` 字典，配合 `GetThemeForSurface` 与 `WithThemeForSurface` 为不同材质独立保存完整的个性化样式快照；
  - `ThemeButton.Apply` 在切换前将当前材质配置存入快照，切换时加载目标材质独立快照，深浅色、字体等全局偏好保持跨材质同步；
  - `AppSettingsProvider.Normalize` 自动向下兼容并迁移历史配置；
  - `AppearanceViewModel.DataChanged` 补齐所有单色变体与外观属性通知。

### 26.2 虚拟网格模式废弃（移除 Virtual，保留 Manual 与 Free）
- **废弃重构**：
  - 用户指定直接取消虚拟网格；`GridMode` 中将 `Virtual = 1` 标记为 `[Obsolete]`；
  - `AppSettingsProvider.Normalize` 自动将历史配置中的 `GridMode == 1` 升级为 `GridMode.Manual`；
  - 设置界面精简：`AdvancedViewModel.GridModes` 仅暴露「自定义网格」与「自由布局」，移除吸附相关失效选项。

### 26.3 自由布局组件尺寸交互升级
- **右下角原生自由缩放抓手（ResizeHandle）**：
  - 在 `Widget.axaml` 底部右下角增设视觉手柄，自由布局下鼠标悬停呈现对角拉伸光标；
  - 通过 `Widget.axaml.cs` 调用 Avalonia 原生 `BeginResizeDrag(WindowEdge.SouthEast, e)` 机制，解除缩放死锁。
- **自定义尺寸精确输入弹窗（CustomSizeDialog）**：
  - 新增 `CustomSizeDialog`，在自由布局下右键组件菜单提供「自定义尺寸…」，支持按物理/逻辑像素直接设定宽高（范围 48 ~ 3840 px）；
  - 修复 `AfterResize` 动画后无条件执行 `SetMinMaxSize(true)` 导致尺寸永久锁死的陈年 Bug；最小缩放尺寸下调至 48 px。

---

## 27. 1.8.7：外设蓝牙设备类型智能匹配记忆、设置界面透明度隔离、多彩模式闪退修复与时钟外围视觉精准还原

### 27.1 蓝牙与外设电量组件设备识别与手动类型绑定
- **痛点**：部分蓝牙耳机、蓝牙手柄连接后系统仅报告通用音频或外设标识，无法正确识别类别与对应图标。
- **方案**：
  - 在电量组件设置页面（`BatteriesSettings`）中枚举已配对/已连接的全部蓝牙与外设设备列表；
  - 提供设备类型手动下拉选择（耳机 Headset、游戏手柄 Gamepad、鼠标 Mouse、键盘 Keyboard、笔 Pen、音箱 Speaker 等）；
  - 配置持久化存储在 `BatteriesModel.DeviceOverrides` 字典中，下次设备重连时自动匹配并显示准确的 Apple 风格外设图标与电量。

### 27.2 设置窗口透明度与全透明内容消失 Bug 隔离
- **修复方案**：
  - 修复了在液态玻璃与多彩主题下，设置窗口（`Settings.axaml`）受背景材质影响变成全透明、界面内容消失仅剩红绿灯按钮的 Bug；
  - 将设置窗口与组件窗口完全解耦隔离，明确锁定 `TransparencyLevelHint="Mica, None"`，确保窗口底色始终为固定不透明的 Apple 高质感 Mica / 固体背景。

### 27.3 多彩模式闪退彻底修复（InvalidCastException）
- **修复方案**：
  - 修复了在外观设置中点击“多彩”主题按钮瞬间程序崩溃的致命 Bug；
  - 根本原因为 `Accent.axaml` 中 `ClockStrokesBrush` 的 `Color` 属性误绑定到了 Brush 资源（`SystemControlForegroundBaseHighBrush`），导致 Avalonia 样式动态重求值时类型转换失败抛出 `InvalidCastException`；
  - 将绑定更正为标准颜色资源 `SystemBaseHighColor`，彻底消除崩溃风险。

### 27.4 时钟样式 1（单表盘 AnalogI）与日历样式 1 视觉效果精准还原
- **时钟样式 1（AnalogI）**：
  - 亮色模式下：表盘内部圆形区域为纯白色（`#FFFFFF`），数字（1~12）、时针、分针为清晰黑色，秒针为经典苹果橙（`#FF9500`），刻度为细腻灰；圆形区域以外的背景（小组件卡片 4 个圆角外围）采用暗色模式的深炭黑（`#1C1C1E`），还原经典 Apple 时钟卡片设计。
  - 暗色模式下：内部表盘圆形区域为 `#2C2C2E` 暗灰，数字与指针为纯白高对比度，外围保持 `#1C1C1E` 深炭黑。
- **日历组件（Date）**：
  - 移除任何强制黑底或暗色锁定的特殊限制，完全恢复标准的亮色模式（白底）与暗色模式（黑底）自由切换。

---

## 28. 1.8.8：组件库全量类别归纳、待办事项长文本保护、天气/剪贴板面板与铺满组件视觉优化

### 28.1 扩展组件库全量归类与「其他扩展」收敛
- **改进方案**：
  - 将独立番茄钟组件（`Pomodoro`）正式归入「时钟与日历」分类（`timeAssemblies`）；
  - 将外设与电池电量组件（`Batteries`）正式归入「系统监控」分类（`monitorAssemblies`）；
  - 16 款原生小组件全部归属对应分类，收敛消除小组件库侧边栏多余的「其他扩展」项。

### 28.2 待办清单组件长文本删除按钮防挤出修复
- **痛点**：单条待办事项文本过长时，横向延伸导致右侧“✕”删除按钮被挤出窗口裁剪区域而不可见。
- **修复方案**：在 `RemindersPopupWindow.axaml` 列表 `ScrollViewer` 上锁定 `HorizontalScrollBarVisibility="Disabled"`，并在标题输入框启用 `TextWrapping="Wrap"`，实现长文本优雅自动换行，删除按钮永久锁定在最右侧。

### 28.3 多彩模式天气面板字体颜色纯白统一
- **改进方案**：在 `WeatherPopupWindow.axaml` 注入 `TextBlock` 的 `WeatherForegroundBrush` 动态样式，在多彩模式亮色（天蓝底色）下将字体颜色统一设置为纯白色并保持透明度层级，其他模式与暗色不受影响。

### 28.4 多彩模式笔记与地图外围描边与底色露白修复
- **痛点**：多彩模式下卡片描边使得 `ContentPresenter` 相比卡片尺寸缩进 1px，导致铺满卡片的 `Note` 黄色顶栏四周漏出白色/黑色底色与描边，`MapView` 卫星地图四周露出一圈黑白边。
- **修复方案**：在 `Widget.axaml.cs` 中针对 `Note`、`MapView` 等自带全幅自绘底色的组件，在多彩模式下将 `WidgetOutlineThickness` 设为 0、`WidgetOutlineBrush` 设为 `null`、`WidgetCardBackground` 设为 `Transparent`，彻底贴合圆角并消除边缘描边。

### 28.5 多彩模式亮色剪贴板管理器面板纯白优化
- **改进方案**：将 `ClipboardPopupWindow.axaml.cs` 多彩模式亮色卡片底色由 `#2B6CB0`（天蓝）修改为纯白色（`#FFFFFF`），并适配关闭按钮与副文本的亮暗对比度。

### 28.6 多彩模式聚合信息组件背景恢复与分割线优化
- **痛点**：在多彩模式下，聚合信息组件 `AggregateView` 因包含 `Flush` 样式类（用于边距紧凑贴合），被误识别为自带全铺满底色的自绘组件，导致外层 `WidgetCardBackground` 被置为 `Transparent`，使得聚合信息组件背景完全消失露底。
- **修复方案**：
  1. 在 `Widget.axaml.cs` 中细化判定逻辑为 `IsSelfFramingWidget`，仅针对真正自带全幅自绘底色/瓦片的 `Note` 和 `MapView` 隐藏外层描边和卡片底色；`AggregateView` 等依赖外层卡片背景的紧凑组件恢复纯白/深灰背景（`WidgetCardBackground`）和精致卡片外轮廓。
  2. 将 `AggregateView.axaml` 中的中央垂直分割线由硬编码的 `#25FFFFFF` 升级为 `SystemControlForegroundBaseHighBrush` 搭配 15% 透明度，在亮色和暗色卡片背景下均呈现清晰柔和的微光分隔效果。

## 29. 重叠组件（WidgetStack）与清单/间距自适应重构 (v1.9.0)

### 29.1 重叠组件（Widget Stack）全新架构与功能
- **全景分类独立**：在组件库左侧独立划分“重叠组件”一级分类，配备专属叠层矢量图标。
- **指示器与多主题兼容**：重叠组件右侧指示点放置于组件所在网格外侧最右侧，兼容四大主题（多彩模式下采用毛玻璃亚克力效果）。
- **动效架构与零闪烁/零崩溃根治**：彻底废除反弹归零的 Avalonia `PageSlide`，采用 `DispatcherTimer(Render)` + `Stopwatch` 进行高精度 `CubicEaseOut`（220ms）垂直物理插值；离屏保留离屏坐标防止回弹原位；引入 `IsTransitionActive` 锁防抖吸收高频滚轮微动。
- **子组件设置双通道**：右键菜单直达当前激活子组件配置；重叠组件管理面板内为支持设置的各个子组件提供独立配置入口。
- **天气组件零重载缓存**：切断自保存重入死循环，添加全局静态天气缓存，秒开不重载。

### 29.2 清单面板超长文本防挤压与自动沉底
- **“✕”按键物理固定**：行模板使用严格 `ColumnDefinitions="28,*,30"` 三列 Grid 搭配文本框 `MaxWidth="275"` 与 `TextWrapping="Wrap"`，物理杜绝挤出面板。
- **打勾自动排到队列末尾**：勾选某项后平滑移至队列最底部。

### 29.3 间距（Margin）增大后的多组件自适应
- **剪贴板组件**：操作按钮最高优先级，标题超出省略，小尺寸自动折叠筛选按钮，永不截断删除与展开键。
- **翻译器、音乐、番茄钟、搜索**：在小尺寸和宽边距下自适应收缩间距与控制栏，全面防止元素被截断。

## 30. 设置界面全主题毛玻璃与重叠组件缩放/边距保真 (v1.9.1)

### 30.1 设置界面所有主题强制锁定毛玻璃（AcrylicBlur）
- **全主题 OS 级 AcrylicBlur**：在 `Settings.axaml.cs` 的 `ApplyTransparencyHint()` 中移除对主题表面样式的判断，统一锁定为 `[WindowTransparencyLevel.AcrylicBlur, WindowTransparencyLevel.None]`，确保不论系统当前切换为亚克力、液态玻璃、纯色还是多彩，设置界面均固定强制采用 Windows DWM 硬件级毛玻璃。
- **背景底色半透明化**：在 `App.axaml`、`Transparent.axaml` 与 `LiquidGlass.axaml` 中将 `SettingsWindowBackground` 的不透明度从原本遮蔽毛玻璃的 `1.0` 调整为 `0.80`，使底层桌面与活动窗口的磨砂模糊效果通透呈现，且文本对比度清晰稳定。

### 30.2 重叠组件（WidgetStack）交互与渲染保真
- **初始空白卡片自适应**：组件初次从组件库拖出时默认为自适应空白卡片（支持四大主题与亮暗模式），提供虚线框与半透明交互引导。
- **桌面组件跨窗口拖拽收纳**：新增 `StackDropCoordinator` 服务，在编辑重叠组件窗口打开时监听松手位置；桌面组件拖放至编辑界面即刻被吸收收纳（保留其全部配置与尺寸），并自桌面移除；支持在编辑界面随时一键将子组件“↖ 移至桌面”。
- **物理整卡片滑走淡化消失动效**：重叠卡片独立封装在包含背景、轮廓与内容的物理卡片容器中，切换时整张卡片沿滑动方向位移（`±0.85 * height`）并线性淡化消失（`Opacity 1.0 → 0.0`，240ms `CubicEaseOut`），新卡片自底层柔和浮现。
- **子组件边距（Margin）与缩放（ContentScale）100% 保真**：
  - 彻底铲除对 `control.Margin` 的硬编码覆盖，使各小组件完美保留自身 XAML 边距（`AnalogI`/`AnalogII` 保持 8，`Forecast` 保持 0，普通组件保持 12），消除表盘贴边撑满或天气卡片收缩缩水的假象；
  - 空白重叠组件收纳首个小组件时，自动继承该组件的宽高（`Width`/`Height`）与缩放比例（`ContentScale`），并在宿主窗口中即时响应尺寸调整；
  - `StackedWidgetEntry` 持久化记录 `ContentScale`，提取回桌面时精确还原。
- **多彩模式表盘底色一致性**：为多彩主题亮色与暗色模式下的经典表盘 I（`AnalogI`）独立渲染纯黑深炭底色（`#1C1C1E`），消除表盘外圆周的白色直角。

## 31. 重叠组件全主题材质背景联动与剪裁优化 (v1.9.2)

### 31.1 重叠组件（WidgetStack）四大主题材质背景动态适配
- **痛点**：在重叠组件中，当从“多彩”主题切换到“毛玻璃”（Acrylic）、“液态玻璃”（LiquidGlass）或“纯色”（Solid）时，子组件（如清单、多仪表盘、电量与设备、天气预报等）背景仍然保留多彩模式下的纯白或纯黑（`#FFFFFF` / `#1C1C1E`），而文本和图标元素已切换为对应主题，导致背景与前景脱节、毛玻璃和液态玻璃无法透射底层效果。
- **根因分析**：
  1. 重叠组件子卡片及空白卡片背景解析依赖 `TryFindResource("WidgetBackground", ...)`，在主题切换时获取到静态冻结的完全不透明白色/炭黑画刷；
  2. 液态玻璃（LiquidGlass）主题下，窗口底层已有 `LiquidGlassSurface` 进行壁纸折射和高光渲染，但重叠组件子卡片绘制了不透明纯色底色，完全遮挡了液态玻璃材质；
  3. 毛玻璃（Acrylic）主题下，子卡片未采用用户配置的不透明度（`OpacityLevel`）与背景色合成半透明画刷，导致 OS 原生 DWM 亚克力磨砂背景被完全遮挡；
  4. 天气预报组件在多彩模式下使用 Apple 天气专属蓝/深蓝渐变，切换至其他主题时未平滑回退至对应主题背景。
- **修复方案**：
  1. 在 `WidgetStackView.axaml.cs` 中重构 `ResolveCardBackground` 与 `ResolveDefaultCardBackground`：
     - **液态玻璃（LiquidGlass）**：子卡片及空白卡片背景统一返回 `Brushes.Transparent`，让宿主窗口的 `LiquidGlassSurface` 折射与光晕完美透射；
     - **毛玻璃（Acrylic）与纯色（Solid）**：根据当前深浅色模式与用户自定义底色（`EffectiveSolidBackgroundLight`/`Dark`）动态构建 `new SolidColorBrush(baseColor, theme.OpacityLevel)`，确保毛玻璃的半透明磨砂与纯色不透明度实时联动；
     - **多彩（Colorful）**：严格保持 Apple HIG 风格，经典表盘 I（`AnalogI`）深炭底（`#1C1C1E`）、天气预报（`Forecast`）蓝天/午夜蓝渐变、便签与地图全幅透射（`Brushes.Transparent`），其余子卡片纯白/深炭黑；
     - **便签（Note）全主题适配**：便签组件仅在多彩模式（自带黄色顶栏与全幅纸张）下将外层卡片背景置为 Transparent 避免重叠留白；在纯色（Solid）与毛玻璃（Acrylic）主题下，正确继承对应主题半透明/纯色卡片底色，彻底修复纯色主题下便签卡片背景消失露底的问题；
     - **空卡片与轮廓线**：同步适配四大主题画刷与高光轮廓线（`ResolveOutlineBrush`）。

### 31.2 消除非毛玻璃主题下重叠组件多余的 GDI 区域剪裁
- **优化**：在 `Widget.axaml.cs` 的 `ApplyWidgetRegion()` 中移除多彩模式对重叠组件的多余 1-bit GDI 区域裁剪，仅在真正需要 DWM 亚克力裁剪的 `theme.UsesNativeBlur`（毛玻璃）生效，彻底杜绝 32-bit 透明通道抗锯齿圆角被物理剪裁导致的边缘毛刺。

## 32. 网格编辑器崩溃修复与参数面板可读性重构 (v1.9.3)

### 32.1 高级设置修改网格数值时的 `InvalidOperationException` 崩溃
- **现象**：新建配置方案后，在「设置 → 高级 → 网格」修改列数/行数/格子边长/位置等数值，弹出
  `System.InvalidOperationException: Cannot change source while update is in progress.`（堆栈落在
  `Avalonia.Controls.Selection.SelectionModel.SetSource`）。
- **根因（属性通知重入）**：`AdvancedViewModel.RaiseAllProperties()` 会在一次**尚未结束的双向绑定写入内部**被同步调用：

  ```
  数值框写入 ─► AdvancedViewModel.SaveGrid(grid)          // 绑定写入中
                └─► appSettingsProvider.Save(...)          // 同步抛 DataChanged
                     └─► Widget.OnAppSettingsUpdated ─► Widget.AfterMove()   // 每个桌面组件各跑一次
                          └─► WidgetLayoutProvider.Save(...)
                               └─► LayoutProvider.Save ─► DataChanged
                                    └─► AdvancedViewModel.OnLayoutDataChanged ─► RaiseAllProperties()
                                         ├─ PropertyChanged(ScreenTargets)       // 每次调用都新建 List
                                         │    └─► ComboBox.ItemsSource 重置 ─► ItemCollection.SetItemsSource
                                         │         └─► SelectionModel.SetSource  ← 选择项更新尚未结束 ⇒ 抛异常
                                         └─ PropertyChanged(SelectedScreenTarget)
  ```

  新建方案走 `ProfilesViewModel.CreateProfileAsync(..., copyCurrent: true)`，桌面上组件齐全，且切换方案额外触发
  `ActiveProfileChanged` / `ScreensChanged`，通知风暴叠加，因此必现。
- **修复**：`RaiseAllProperties()` 改为**合并 + 延迟投递**（`Dispatcher.UIThread.Post(..., DispatcherPriority.Background)`），
  保证通知永远不在绑定/布局过程内部执行；并加入 `raiseAllScheduled` / `raisingAllProperties` 双重标志防重入与多余重复通知。

### 32.2 网格编辑器参数面板与按钮可读性
- **现象**：全屏网格编辑器（`GridEditor`）右键唤出的参数面板内，按钮与按钮文字几乎不可见。
- **根因**：
  1. 面板背景硬编码深色 `#EE202028`，而应用运行在浅色主题下；面板内**继承主题**的控件（✕ 关闭按钮文字、按钮 hover/pressed 文字、`NumericUpDown` 模板内部部件）出现「深色画在深色上」；
  2. 面板第二个按钮在 XAML 中**完全没有 `Content`**（`<Button Click="OnCloseClicked" />`），始终渲染为空白色块；
  3. 编辑器窗口尺寸依赖 `Screen.Scaling`，与窗口真实缩放不一致时（远程桌面 / 混合 DPI），内容 DIP 宽度大于物理屏幕，右对齐的面板与 ✕ 被推出可见区域。
- **修复**：
  1. 参数面板改为亮色磨砂白表面（`#F7FFFFFF` + 描边 + 投影），标题/标签/分隔线全部显式配色；
  2. 新增窗口级按钮样式 `Button.panelButton`（白底 `#FFFFFF` / 近黑字 `#1C1C1E` / 灰描边）与 `Button.primary`（亮蓝 `#007AFF` 白字），并定义 hover/pressed 三态，**完全不再依赖主题变体**；
  3. 编辑器窗口设 `RequestedThemeVariant="Light"`，保证面板内模板部件统一为浅色；
  4. 补上关闭按钮文案（复用 `Settings_Appearance_Glass_Align_Close`），方向键改为 4 个等宽按钮；
  5. 窗口尺寸改用窗口自身 `RenderScaling`（屏幕模型仅作回退）；参数面板默认可见，右键**任意位置**均可唤出。

---

## 33. 1.9.4：第五主题「柔光玻璃」（取长补短：液态玻璃的光学 + Android 液态玻璃库的柔光/光谱）

### 33.1 需求与定位

在**外观 → 应用主题**新增第五个表面预设「柔光玻璃」（`SurfaceStyle.SoftGlow = 5`），
**排在「液态玻璃」与「多彩」之间**（`SurfaceTemplates` 索引 3）。它与液态玻璃同源——
同一套壁纸采样材质、同一套光学骨架——但换了一条**柔光配方**：

| 维度 | 液态玻璃（LiquidGlass） | 柔光玻璃（SoftGlow） |
| --- | --- | --- |
| 折射 | 12~24 DIP 弯月透镜环，峰值位移大 | **1.55× 更宽、0.45× 更浅**（宽阔而轻柔的弯折，无透镜环） |
| 边缘光 | 1.6 DIP 定向发丝亮线（hairline） | **7× 宽的弥散光晕**，方向性减半（光包住边缘，不画线） |
| 柔光晕 | 无 | **`Glow` 滑块**：沿弯月面外扩的柔和光晕，宽度 ≈1.6 个透镜带且 ≤ 短边 22% |
| 镜面 | 双叶水光（指数 28/8） | **单叶宽幅丝光（指数 3/2）**，幅度 0.38× → 缎面而非水珠 |
| 色散 | RGB 三通道 ±n 位移 | 可选 **`Spectrum` 七抽全光谱**（红/橙/黄/绿/青/蓝/紫，权重按通道归一） |
| 霜化 / 天光 | FrostMix / +2.5% 天光 | ×1.15（上限 0.11）/ +4% 天光 |

**取长补短**的落点（对应 `AndroidLiquidGlassView评估.md` 的结论）：
保留 uWidgets 现有的 **`BevelField` 法线场**（圆角纯径向、直边纯正交、无中轴折痕）与 Screen 混合、
vibrancy、自适应霜化——即那份调研里判定「不要照搬」的画质底座；
只吸收 Android 库真正强的地方：**弥散柔光（无硬边线）** 与 **七抽光谱色散**。
GPU runtime effect 那条路（SKRuntimeEffect，实测 0.25 ms/卡）不在本次范围内，见该评估文档 §5-A。

### 33.2 模型与接线

1. **`SurfaceStyle.SoftGlow = 5`**（追加在末尾，历史配置里的 0/1/2/3/4 语义不变）。
2. **`Theme`**：
   - `IsSoftGlow`（本主题）、`UsesRenderedGlass`（= LiquidGlass ∨ SoftGlow，「材质由应用自绘、卡片背景必须透明」的唯一判据）；
   - `IsGlass` 纳入 SoftGlow（描边设置行继续可用），`UsesNativeBlur` 仍只属于毛玻璃；
   - `DefaultSoftGlowOpacity = 0.14`（比液态玻璃的 0.18 略薄，因为光晕自带亮度）。
3. **`LiquidGlassSettings` 新增 `Glow` / `Spectrum`（0–100，默认 0）**：
   默认 0 ⇒ **液态玻璃像素级不变**（回归断言见 §33.4）；随 `Normalize()` 钳制（NaN→0，越界→0/100）。
   预设 `LiquidGlassSettings.SoftGlowPreset` = `10 / 16 / 40 / 46 / 24 / 225 / 40` + `Glow 70` + `Spectrum 62`。
4. **`AppSettings.GetThemeForSurface(SoftGlow)`** 返回该预设；`AppearanceViewModel.SurfaceTemplates`
   插入同一预设（主题按钮预览与真正应用保持同一份数据）。
5. **渲染**：`LiquidGlassRenderer.Render` 读 `frame.Theme.IsSoftGlow` 选择配方，
   全部差异都在 `soft` 分支内（`soft=false` 时逐行与旧实现等价）。
6. **接线替换**：14 个「窗口/弹窗自绘材质」判定点由 `IsLiquidGlass` 改为 `UsesRenderedGlass`
   （Weather/Reminders/Folders/Stack/Tools/Clock 的字形主题、`PopupLiquidGlassService`、
   `LiquidGlassPreRenderService`、`LiquidGlassSurface`、`ThemeService` 的样式切换、
   `Widget.ShowsGlassMaterial`、`ThemeButton.IsRenderedGlass`）；
   弹窗材质的缓存键补入 `EffectiveSurface` + `Glow` + `Spectrum`，避免两套配方互相命中旧位图。
7. **设置页**：光学区块标题/描述随主题切换文案（`GlassOpticsTitle/Description`），
   仅柔光玻璃显示「柔光晕」「光谱弥散」两行（`ShowSoftGlowSettings`），
   「重置参数」回到**当前主题**的预设（柔光玻璃 → `SoftGlowPreset`，液态玻璃 → 出厂默认）。
8. **本地化**：`Settings_Appearance_Surface_SoftGlow` / `SoftGlow_Title` / `_Description` /
   `_Glow(_Hint)` / `_Spectrum(_Hint)`，中英双语（resx + `Locale.Designer.cs`）。

### 33.3 视觉与实测

- `docs/soft-glow/01-grid-compare.png`：20 px 网格壁纸上 液态玻璃 / 柔光玻璃 / 柔光晕+光谱 100%（1:1）；
- `docs/soft-glow/02-real-wallpaper.png`：真实壁纸上三张 1:1 卡片（同一采样区域，只换材质）；
- `docs/soft-glow/03-rim-strip.png`：边缘裁切 ×4 —— 左边是 1 px 发丝线，中间是柔光晕。

量化（`tests/LiquidGlassChecks`，`SoftGlowProfile`，机器实测）：

| 指标 | 液态玻璃 | 柔光玻璃 |
| --- | --- | --- |
| 边缘光峰值 / 25% 宽度 / 40 px 积分 | 145 级 / **1 px** / 493 | 78 级 / **11 px** / 861 |
| 柔光晕 0→50 / 0→100 变化量 | — | 11.9 / 23.7 级（单调），卡内平坦区 0.00 |
| 光谱弥散 0 vs 100（边缘带） | — | 均值 3.2 级、峰值 95 级，**平坦中心 0.00** |
| 折射 100%（平滑渐变壁纸） | 均值 1.27 / 峰值 14 级 | **均值 0.73 / 峰值 6 级**（更柔） |
| 卡内平均亮度（同参数同区域） | 71.9 | 74.7（不泛白，仅 +3 级） |
| 1200×800（96 万像素，光谱 + 柔光晕） | ~75–135 ms | ~90–135 ms |

### 33.4 回归与验证

- `tests/LiquidGlassChecks`：原有光学画像（透镜剖面、亮边线、色散、清晰度、性能）**全部保持通过**——
  因为 `soft=false` 分支未变，这就是「液态玻璃没被动过」的回归证明；
- `SoftGlowProfile`（新增，已 `git add -f`，因 `/tests/` 在 .gitignore 中）：
  枚举/标志位/预设/`Normalize`/JSON 往返与向后兼容（旧配置 `Glow=Spectrum=0`）、
  柔光晕宽度与单调性、光谱只作用于边缘带、折射更柔、平坦中心位移恒为 0、性能上限；
- `ColorfulThemeChecks`（预设由 4 → 5、柔光玻璃位于 3 号位、`Glow/Spectrum` 行为与中英本地化）、
  `ClockThemeChecks`、`WidgetStackChecks` 全数通过；Solution Debug/Release 构建 0 错误。
- ⚠️ `MapAndTitleBarChecks` 仍有 **2 项既有失败**（与本次改动无关）：
  `LiquidGlass.axaml` 的 `SettingsWindowBackground` 断言期望 `Opacity="0.96"`，而 1.9.1 起实际为 `0.80`；
  `ClipboardPopupWindow.axaml` 断言期望 `TabCode`/`TabLink`，该分类标签现已不存在。测试期望值待同步。

### 33.5 版本号

- 主程序 `AssemblyVersion`：1.9.3 → **1.9.4**（`src/uWidgets/AssemblyInfo.cs`）。
- 小组件无改动（本次只动材质与外观主题）。

---

## 34. 1.9.5：修复日历组件与聚合组件日历部分的「当日数字镂空」

### 34.1 现象

「今日数字镂空」（`HollowTodayNumber`，§24.5）失效：今日数字不再是打出来的洞，而是**实心白字画在圆盘上**。
两个组件都中招——日历组件（`Calendar / Month`）与聚合信息组件的日历部分（`FixedWidgets / AggregateView`）。

### 34.2 根因（两处，彼此独立）

1. **日历组件：暗色模式下被硬性关闭。**
   `src/Widgets/Calendar/Views/Month.axaml.cs` 的 `UpdateTodayBrush()` 里有一行
   `if (dark) HollowToday = false;`（由 1.8.x「多彩主题 + 全暗色支持」那次提交 `ef20bd9` 加入）。
   `HollowToday` 是视图上的运行时属性（视图构造时从模型读一次，之后主题变体/强调色变化时重算），
   被这行覆盖后：**暗色下无论模型与设置页怎么开，都按"实心白字"渲染**。
   应用主题为「跟随壁纸自动」且系统深色（本机即如此）时必然命中。
2. **聚合组件：洞打出来了，但看不见。**
   逻辑链路（模型默认 `true` → VM `HollowTodayNumber` → 视图绑定 → 控件 `Exclude` 布尔差）经离屏渲染逐像素验证**是正常的**
   （镂空 278 px 透明孔、非镂空 0 px）。真正的毛病是**对比度**：单色（黑白）模式下今日圆盘在暗色下是**纯白**，
   孔里透出的是卡片背后的玻璃/壁纸；壁纸偏亮时数字与圆盘几乎同色 → 观感就是"一个实心白圆，数字没了"，
   于是也被当成"镂空失效"。

### 34.3 修复

1. **删除暗色覆盖**：镂空在所有主题、明暗两种模式下都严格跟随设置项。
2. **给数字孔沿加对比色发丝边**（两份 `HollowTodayMark`：`Calendar/Views/Controls` 与 `Fixed/Views/Controls`）：
   在镂空路径上，沿字形轮廓描一条半透明细线——盘亮（亮度 > 0.55）用黑 32%，盘暗用白 38%，线宽 = 圆盘半径 × 0.11（最小 1 px）。
   数字内部**依旧是透明的**（玻璃/壁纸照旧透出来，镂空语义不变），但无论背后的壁纸是亮是暗，
   都能一眼看出这是"打出来的洞"，不会再与圆盘同色而消失。观感即真实打孔玻璃的孔沿。

### 34.4 回归

`tests/CalendarHollowChecks` 重写为 **2 个组件 × 明/暗 2 种变体 = 4 组渲染、每组 6 条断言（共 24 条）**，
并加载应用真实的主题资源（Accent / MonochromeBlackWhite / LiquidGlass）：

- 镂空：圆盘绘出、孔内透明像素 ≥ 20、孔是数字而非整个圆盘、孔内**无不透明**像素（发丝边是半透明，不计为"画上去"）；
- 非镂空：圆盘内无透明像素、数字以不透明方式画在盘上。

修复前：暗色变体失败（`hole 0 px / painted 349 px`，与线上截图一致）；修复后 **24/24 通过**。
另：`docs/hollow-today/` 给出修复后（暗色/亮色）两个组件在真实壁纸上的 1:1 效果与孔沿 5× 放大图。

### 34.5 版本号与交付

- 主程序 `AssemblyVersion`：1.9.4 → **1.9.5**。
- 按 `build.ps1` 重新产出单文件 `dist/win-x64/uWidgets.exe`。

---

## 35. 1.9.6：柔光玻璃边缘「晕染」、玻璃边缘宽度可归零、无边框时钟跟随全局主题

### 35.1 三个问题与修复

**① 无边框时钟「跟随全局主题」不生效（永远是毛玻璃、没有柔光玻璃效果）**

- **根因 A（配方缺失）**：`GlyphLiquidGlassRenderer` 只实现了液态玻璃那一套配方，**完全不认识 `Glow` / `Spectrum`**——
  全局切到柔光玻璃后，数字只按"宽窄/位移"处理，观感仍是一块磨砂玻璃。
- **根因 B（染色被覆盖）**：视图把主题的 `EdgeTint` 覆盖为组件自己的 `DyeIntensity`（默认 10%），
  而柔光玻璃的身份恰恰是边缘染色 → 数字上几乎只剩模糊底色。
- **修复**：
  1. 抽出 `FramelessThemeResolver.Resolve(themeMode, globalTheme)`（纯函数，可断言）：
     `ThemeMode = 0` 跟随全局主题，**柔光玻璃与液态玻璃一样算「自绘玻璃」**（走字形玻璃管线），
     毛玻璃走 OS 丙烯、纯色走矢量填充；1/2/3 仍为显式覆盖。视图改为调用它。
  2. 字形渲染器补齐**柔光配方**：透镜 1.55× 更宽 / 0.45× 更浅、弥散边缘取代发丝亮线、
     单叶宽幅丝光取代双叶水光、七抽光谱色散、以及随 `Glow` 的柔光晕（并有独立于透镜的宽度下限，
     因此 `EdgeWidth = 0` 时依然存在）。
  3. 染色强度对柔光玻璃取 `max(组件 DyeIntensity, 主题 EdgeTint)`：
     主题的染色是柔光玻璃的身份，不该被默认的 10% 抹平；想更强仍可继续调高（"更多"允许，"更少"不允许）。

**② 柔光玻璃的边缘染色改为「晕染」（本次的核心观感需求）**

- **旧行为（点对点）**：每个边缘像素用它**正下方那一点**的颜色染色 → 红底上的一小块绿，
  绿就只在那一小块，两侧**硬截断**回红（`docs/soft-glow/04-dye-bloom.png` 左图）。
- **新行为（晕染）**：新增 `LiquidGlassRenderer.AuraField`——
  把已模糊的背景降采样成粗色格（边长 ≈ 短边/22，钳制 4–18 px）→
  **两轮「最鲜艳者向外扩散」**（按 色度×亮度 取局部最强，按 70% 混入）→ 3×3 模糊消除格边 →
  主循环对这个"光场"做双线性采样取染色颜色。
  于是那一小块绿会像**光源**一样向两侧晕开，与红色形成柔和的叠加过渡（同图第 2、3 张）。
- **仅柔光玻璃启用**：液态玻璃保持历史点对点染色，两个主题因此有了明确区分；
  柔光玻璃的染色带也刻意更宽（==短边 14%==，不随透镜宽度归零）。
- **稳定性**：色格锚定在**组件自身**（而不是随模糊/透镜变化的 padded 面），
  否则参数微调会让色格漂移、染色颜色跟着跳（`EdgeWidth = 0` 下"折射 0 vs 100 像素差恰好为 0"就是这条的证明）。
- 无彩色/极暗区域仍走原有的色度与亮度闸门，不会把灰底染成彩色噪点。

**③ 玻璃边缘宽度最低可调到 0**

- `LiquidGlassSettings.Normalize` 下限 **4 → 0**；设置页滑杆与数字框下限同步。
- 语义：`0` = **完全关掉透镜环**（不再弯折底图），但弥散、染色、柔光晕、亮边线照旧 ——
  即"纯柔光/磨砂 + 边缘光"的形态。
- 两个渲染器都补齐了 `lensWidth == 0` 的防护（`invLens` / `invAura`、柔光晕宽度下限、
  字形 `ResolveLens` 返回 0 透镜但保留色散），不再可能出现除零/NaN 像素。

### 35.2 回归与实测

| 断言（`tests/LiquidGlassChecks/SoftGlowProfile`） | 结果 |
| --- | --- |
| 染色坡道宽度（红底 + 跨边绿块，沿边缘测 G−R 10%→90%） | 液态玻璃 **0 px（硬截断，227 级跳变）** vs 柔光玻璃 **16 px 平滑过渡（338 级）** |
| `EdgeWidth = 0`：折射 0 ↔ 100 的渲染差 | **0.000 级**（彻底不弯折） |
| `EdgeWidth = 24`：同上 | 1.77（柔光）/ 2.81（液态）级（仍然弯折） |
| `EdgeWidth = 0` 的圆角裁剪 / 实心内部 / 染色+光晕 | 角 0、中心 255、边缘处理仍在 |
| `EdgeWidth = 0` 通过配置校验、旧配置默认不变 | 通过 |

`tests/ClockThemeChecks` 新增（全部通过）：

- `FramelessThemeResolver`：毛玻璃 / 纯色 / 多彩 / 液态玻璃 / 柔光玻璃五种全局主题 × `ThemeMode=0` 的映射，
  显式 `ThemeMode` 覆盖全局，无全局主题回落丙烯；运行实例上"ThemeMode=0 + 全局柔光玻璃"解析为自绘玻璃。
- 字形透镜：柔光下 `9.88px/9.14px → 11.96px/4.11px`（更宽更浅）；`EdgeWidth=0` → 透镜 0、色散保留 0.30。
- **同一光学参数**下柔光玻璃与液态玻璃的数字像素平均差 **4.87/255**（证明柔光配方真的作用到数字上）。

全量回归：`LiquidGlassChecks`、`ClockGlassChecks`、`ClockThemeChecks`、`CalendarHollowChecks`、
`ColorfulThemeChecks`、`WidgetStackChecks`、`FixedSpanChecks` 全绿；Debug/Release 构建 0 错误。

### 35.3 视觉

- `docs/soft-glow/04-dye-bloom.png`：液态玻璃（细边、硬截断）| 柔光玻璃（宽晕染）| 柔光玻璃 + 柔光晕；
- `docs/soft-glow/05-edge-width-0.png`：液态 24 DIP | 柔光 24 DIP | 柔光 **0 DIP**；
- `docs/soft-glow/06-frameless-clock.png`：实机无边框时钟（跟随全局 = 柔光玻璃，边缘出现光谱色边与柔光折射）；
- `docs/soft-glow/07-frameless-clock-before.png`：修复前的同一位置（整块乳白磨砂，无色边、无折射）；
- `docs/soft-glow/08-live-card-bloom.png`：实机小组件卡片的边缘晕染（红色壁纸在卡片边缘柔和地化开）。

### 35.4 版本号

- 主程序 `AssemblyVersion`：1.9.5 → **1.9.6**；重新产出 `dist/win-x64/uWidgets.exe`。

---

## 36. 1.9.7：新增「染色扩散」——边缘染色向卡内扩散的程度可调（0 = 只在最外一圈）

### 36.1 需求与问题

1.9.6 的柔光玻璃边缘染色是「晕染」的，但**向内扩散的范围是写死的**：
染料带宽度 = 短边的 14%，并且内部还有 35% 的「环境扩散」权重（`innerSpread ≈ 染料带 × 1.3–1.6`）。
实测：**染料的覆盖面积占整卡 45.8%** —— 大半个卡面被染色覆盖，观感"太脏"，
而且没法调小。

### 36.2 方案：新增 `DyeSpread`（染色扩散，0–100，默认 35）

- **语义**：只控制染色**向内**的深度，不改变沿边缘的「晕染」色彩过渡（那是 `AuraField`，
  是 1.9.6 要的效果，保留）。
  - **0** = 染色只留在最外一圈（染料带 = 短边 **4%**，内部权重 **0**）——干净边缘，卡面完全不被染色覆盖；
  - **100** = 染料带 短边 **14%** + 内部权重 0.35（= 1.9.6 之前的观感）；
  - 中间线性过渡。
- **实现**（`LiquidGlassRenderer`）：
  - 染料带宽度：`minSide × (4% + 10% × spread)`（并保留 `EdgeWidth = 0` 时的宽度下限，所以 0 扩散时依然有边缘色）；
  - 光晕混合权重：`ambientWeight = 0.35 × spread`（0 时 `softAura` 只剩弯月面那一圈）；
  - **仅柔光玻璃生效**：液态玻璃完全没有这个旋钮，取值任意都不影响它的逐点染色（有回归断言守着）。
- **默认值 35**（而非 100）：老配置（含升级前保存的柔光玻璃快照）没有这个字段，
  落到 35 后"太脏"的问题默认就缓解了；需要以前的观感就把它拉到 100。
  柔光玻璃预设 `SoftGlowPreset` 显式取 **55**。
- **设置页**：「外观 → 应用主题 → 柔光玻璃」区块新增「染色扩散」滑杆 + 数值框（0–100），中英双语；
  与「柔光晕」「光谱弥散」同属仅柔光玻璃可见的行。

### 36.3 回归与实测（`tests/LiquidGlassChecks/SoftGlowProfile`）

- **向内扩散距离**（均匀红底、其他参数关闭，沿左边缘量到"染色不再可见"的深度）：
  **染色扩散 0 / 50 / 100 → 7 / 19 / 33 px**（单调递增；0 时是一圈干净边缘而非无染色）；
- **染色覆盖面积占比**（真实壁纸 + 用户当前柔光玻璃参数）：
  0 → **11.4%**、35 → 22.4%、70 → 34.2%、100 → **45.8%**（对照图 `docs/soft-glow/09-dye-spread.png`）；
- **液态玻璃不受影响**：`DyeSpread` 0 vs 100 的渲染差 **0.000 级**；
- **校验**：越界钳制（−5→0、300→100）、`NaN` → 默认 35、老配置默认即 35。

全量回归：`LiquidGlassChecks`、`ClockThemeChecks`、`ClockGlassChecks`、`CalendarHollowChecks`、
`ColorfulThemeChecks`、`WidgetStackChecks` 全绿；Debug/Release 构建 0 错误。

### 36.4 版本号

- 主程序 `AssemblyVersion`：1.9.6 → **1.9.7**；重新产出 `dist/win-x64/uWidgets.exe`。

---

## 37. 1.9.8：无边框时钟新增「柔光玻璃」主题选项（并把 1.9.4–1.9.7 一并发布）

### 37.1 需求

无边框时钟的「视觉主题」下拉此前只有 跟随全局主题 / 毛玻璃 / 液态玻璃 / 纯色，
缺一个**显式的柔光玻璃**（用户想把时钟单独钉在柔光玻璃上，而不是依赖全局主题）。

### 37.2 实现

1. **`FramelessThemeResolver`** 新增 `ThemeMode = 4` → `FramelessMaterial(IsSoftGlow: true)`
   （与 `2 = 液态玻璃`、`3 = 纯色` 同级的显式覆盖；0 仍为跟随全局）。
2. **主题选择列表**（`FramelessClockSettingsViewModel.ThemeModeOptions`）在「液态玻璃」之后插入
   `new(4, Locale.Clock_ThemeMode_SoftGlow)`（顺序：跟随全局 / 毛玻璃 / 液态玻璃 / **柔光玻璃** / 纯色）。
3. **本地化**：Clock 组件自己的 resx 新增 `Clock_ThemeMode_SoftGlow`（中「柔光玻璃」/ 英 "Soft Glow Glass"）。
4. **关键修正：显式模式还要选「配方」**。`ThemeMode = 2/4` 以前只切换渲染分支，
   而渲染器是**按主题的 `Surface` 决定用清脆还是柔光的光学**——所以全局是液态玻璃时，
   即使选「柔光玻璃」也会渲染成液态玻璃。现在显式模式会同时把 `Surface` 定为对应材质
   （4 → `SoftGlow`，2 → `LiquidGlass`）；「跟随全局」本来就带着正确的 `Surface`，不受影响。
5. 柔光玻璃下时钟的染色强度仍取 `max(组件 染色强度, 主题 边缘染色)`（见 §35.1），
   所以显式选柔光玻璃时，数字边缘的晕染不会被组件默认的 10% 抹掉。

### 37.3 回归（`tests/ClockThemeChecks`）

- `ThemeMode = 4` 在全局为**毛玻璃 / 无主题**时都解析为柔光玻璃（不被全局带跑）；
- 主题列表含 `value = 4` 且仅一次、显示名非空，0/1/2/3 仍在；
- **渲染验证**：全局液态玻璃下，`ThemeMode = 4` 与 `ThemeMode = 2` 的数字像素平均差 **6.69/255**
  （证明选柔光玻璃时确实换了配方，而不是沿用全局的液态玻璃）；
- 全量回归：`LiquidGlassChecks`、`ClockThemeChecks`、`ClockGlassChecks`、`CalendarHollowChecks`、
  `ColorfulThemeChecks`、`WidgetStackChecks`、`FixedSpanChecks` 全绿。

### 37.4 版本号与发布

- 主程序 `AssemblyVersion`：1.9.7 → **1.9.8**；
- `build.ps1 -Portable -Msi` 产出 `uWidgets.exe` / 便携 zip / MSI，并作为 GitHub Release `v1.9.8` 的附件发布
  （更新日志汇总 1.9.4–1.9.8：柔光玻璃主题、日历镂空修复、时钟跟随全局 + 柔光玻璃、染色晕染、染色扩散、边缘宽度归零）。


## 38. 1.9.9：柔光玻璃并入液态玻璃 + GPU 实时液体玻璃（兼容动态壁纸）

### 38.1 需求

1. 把「液态玻璃」和「柔光玻璃」**合并为一个「液态玻璃」主题**，算法需要调整；
2. 参考 `KaranocaVe/LiquidGlassAvaloniaUI`：**实时采样**，**兼容动态壁纸**，并且**可以关闭回退到当前的静态行为**；
3. **GPU 优先**——桌面场景 GPU 基本空闲，逐像素光学没有任何理由留在 CPU 上；
4. 目标：尽量贴近 macOS 27 的原生液态玻璃观感。

### 38.2 主题合并

1. **`SurfaceStyle.SoftGlow = 5` 保留为历史值**（已存储的 0–5 语义不变），但 `Theme.EffectiveSurface`
   把 `SoftGlow` 归一到 `LiquidGlass`：合并后只有一个表面、一条管线。
2. **迁移**：`Theme.NormalizeMaterial()` 把旧表面折进液态玻璃，**并原样保留它的光学参数**；
   `AppSettingsProvider.Normalize` 同时把 `SurfaceThemes["SoftGlow"]` 搬到 `LiquidGlass`
   （当前选中的柔光自定义优先于更久以前存的透镜预设）。迁移后的配置仍然渲染成柔光观感。
3. **预设从 5 个减为 4 个**（毛玻璃 / 纯色 / 液态玻璃 / 多彩）。液态玻璃预设回到
   `new LiquidGlassSettings()` 的**出厂清脆配方**——合并绝不允许改变默认观感，否则老配置会凭空变样。
   由于 `ThemeButton.IsSelected` 按 `EffectiveSurface` 比较，两个预设共用同一表面必然同时高亮，所以柔光
   不能继续作为一个并列的预设存在。
4. **柔光配方改由旋钮选择**：`LiquidGlassSettings.IsSoftRecipe => Glow > 0 || Spectrum > 0`，
   `Theme.IsSoftGlow` 因此同时覆盖「历史柔光表面」与「合并后把柔光料加上来的配置」。
   `柔光晕 / 光谱弥散 / 染色扩散` 三个旋钮现在都属于合并后的主题，恒常显示（`ShowSoftGlowSettings`
   == `ShowGlassOpticsSettings`）。
5. **显式主题模式要跟着改**：无边框时钟 `ThemeMode = 2`（强制液态玻璃）以前只把 `Surface` 换成
   `LiquidGlass`；合并后仅换表面**不再足以关掉柔光配方**，必须同时把 `Glow/Spectrum` 清零
   （下面的光学参数照旧保留，等价于 1.9.8 的行为）。`ThemeMode = 4` 走 `SoftGlow` 表面，不变。

### 38.3 GPU 光学管线

**为什么必须上 GPU。** CPU 逐像素循环渲染 1200×800 的一张卡约 **110 ms**。作为「按需重算一次然后缓存」
的静态材质这没问题，但实时采样下每帧、每个组件都付这个代价是完全不可行的——几个组件 10 fps
就能吃满所有核心。而这段计算恰好是 GPU 最闲的那类负载。

1. **分工**：CPU 每帧只做**一次**「桌面采样 → 降采样 + 模糊 → 一张共享背景纹理」；
   **逐像素光学全部搬到 SkSL**，由 `LiquidGlassGpuEffect` 在渲染线程求值。
2. **`LiquidGlassGpuEffect`** 忠实移植 `LiquidGlassRenderer.Render` 的像素循环：`BevelField` 法线/深度、
   月牙透镜位移、色散（三抽头 + 可选七抽头光谱）、染色晕染、鲜度与自适应霜度、边缘描边 +
   镜面高光 + 环境光泽、柔光晕。权重推导与 CPU 版逐项对齐，并复用同一批常量
   （`HighlightReference` / `Saturation` / `Frost*` / `GetAdaptiveOptics` 档位）。
3. **`LiquidGlassSourceCache`**：按（采样帧, 模糊强度, 缩放）缓存降采样+模糊后的背景，多组件共享一张纹理。
   `GlassSource` 引用计数，`Get()` 交付**调用方持有的引用**——缓存随时可能因为更新的采样帧把条目淘汰，
   借出去的指针会变成已释放位图。
4. **`LiquidGlassSurface`** 用不可变的 `Prepared` 记录把（背景纹理 / 染色网格 / 帧几何 / CPU 位图）
   作为一个引用整体换入换出：渲染线程每帧只读一次，绝不会读到「新背景 + 旧几何」的组合。
   被替换的全屏纹理延迟 250 ms 再释放（渲染线程可能正在采样它），把重叠量限制在一两张纹理。
5. **CPU 回退**：没有 runtime shader（编译失败）或没有 GPU 上下文时，完全走原来的 CPU 渲染器。

#### 38.3.1 三条实测出来的硬约束

- **`SKRuntimeEffect` 在本 Skia 上只支持 GPU 后端。** 在光栅画布上绘制一个 runtime shader 会以
  **不可捕获的 `SEHException` 直接终止进程**（用最小着色器复现：`SKCanvas.DrawRect` 崩在 `sk_canvas_draw_rect`），
  `catch (Exception)` 拦不住。所以绘制前**必须**检查 `ISkiaSharpApiLease.GrContext`：为空就永久回退 CPU 材质
  （先画一帧未折射的背景顶住），并重新触发渲染。安全模式、RDP、纯软件渲染都走这条路。
- **SkSL 方言缺失的东西**：没有 `smoothstep`、没有 `mod`、没有双参 `atan`，而且**函数不能返回结构体**
  （`functions may not return type 'Bevel'`）。因此 `BevelField` 用 `out` 参数重写，
  `smoothStep` / `atan2NonNegative`（内区两个分量恒非负，`atan(y/x)` 就是精确解）自行实现。
- **位图着色器只有最近邻采样**：`SKBitmap.ToShader` / `SKImage.ToShader` 在 2.88.8 都不提供采样选项，
  实测确认不做插值。所以背景与染色网格的取值都在着色器里**手写双线性**（`contentTexel` / `sampleRgb` /
  `auraTexel` / `sampleAura`）。否则透镜位移会吸附到整数纹素（边缘出现台阶），降采样背景也会放大成马赛克。

### 38.4 实时采样与动态壁纸

1. **采样源改成多候选**（`DesktopCapturer`）：记住的上次宿主 → **动画壁纸 WorkerW**
   （`SHELLDLL_DefView` 所在 WorkerW **之后**的那个顶层 WorkerW，即 Wallpaper Engine 桌面模式的宿主）
   → `Progman`（静态壁纸）→ 静态文件回退。命中一个可信来源后就记住它。
   *原先只抓 `Progman`，而壁纸引擎根本不画进 Progman，所以「动态壁纸」这条需求实际上没被满足。*
2. **记住的宿主不再逐帧校验**：动态壁纸的暗场景会被 `LooksLikeWallpaper` 判成抓取失败，
   于是画面会在「动态壁纸」和「Progman 的静态壁纸」之间来回跳。只有**解析新宿主**时才做校验。
3. **`ConfigureLiveSampling(enabled, intervalMs)`**：默认 **100 ms（10 fps）**，
   范围 **3–1000 ms**；模型里的 clamp、设置界面的 `Minimum/Maximum` 与默认值三者一致
   （原实现是模型默认 33、clamp 16–2000、界面最小 80，`NumericUpDown` 会把 33 抬到 80 并悄悄改写设置）。
   模糊背景本来就吃掉运动细节，10 fps 观感已经连续，而代价只有 30 fps 的三分之一。
   **下限 3 ms 的含义是「不再由间隔决定速度」**：采样是需求驱动的——tick 只是请求新帧，
   真正的桌面抓取发生在上一次采样被消费并渲染之后——所以 3 ms 不会变成每秒几百次全屏抓取。
   相应地，放弃等待的兜底时延必须**与间隔解耦**（`MinRetryMs = 250`）：3 ms 的「两倍间隔」是 6 ms，
   那会在上一帧根本来不及被消费时就再次作废，把一个快速档位变成连续全屏抓取。
4. **没人看就不采样**：没有「可见 + 液态玻璃」的表面时停止；全屏覆盖时随 `SuspendAll` 一起停
   （否则计时器会立刻把刚释放的整屏位图重新抓回来）。
5. **关闭 = 冻结当前帧**：开关关掉后不再连续采样，但仍然允许**一次**采样——否则 `Release()` 之后缓存为空，
   玻璃会永久回落到**静态壁纸文件**，与冻结的桌面帧完全是两张图。
6. **计时器 tick 只做 `Invalidate()` + 立即刷新**：
   - 不能用表面的 90 ms 防抖——采样间隔比防抖短时，防抖每次都被重置，**永远不会触发**，玻璃直接冻住；
   - `framePending` 用来避免抓取速度跟不上间隔时堆积，但它**必须有时间上限**
     （两个间隔后强制放行）：一次 tick 完全可能撞上所有表面都忙、什么都没消费，
     无上限的标志位会就此永久置位，再没有任何 tick 能清掉它，采样直接停摆。

### 38.5 设置与本地化

- 外观 → 应用主题 → 液态玻璃 新增「**实时采样动态壁纸**」开关与「**采样间隔（毫秒）**」；
- 文案进 `Locale.resx` / `Locale.zh-Hans.resx` / `Locale.Designer.cs`（与页面其它行一致，不再硬编码中文）；
- 顺带清理这次改造不再需要的东西：`Vortice.Direct3D11` 依赖、`AllowUnsafeBlocks`、
  以及一个「解码 PNG 却从不使用」的 `gpuBitmap` 字段。

### 38.6 回归

- **构建**：`src` 下 19 个项目全部 0 错误。
- **测试**：22 个套件中 20 个全绿。`MapAndTitleBarChecks`（2 项：`SettingsWindowBackground` 不透明度、
  `ClipboardPopupWindow` 页签过滤）与 `SingleInstanceChecks`（1 项：手动摆放位置）在 **1.9.8（HEAD）上同样失败**，
  属于既有问题，与本次改动无关——基线用 `git worktree` 检出 HEAD 逐套对比确认。
- **`LiquidGlassChecks` 新增 `GpuMaterialCheck`**（本机唯一能做的 GPU 校验）：
  SkSL 能否编译、**每一个 uniform 与 child 是否都能绑定**、光学参数是否全部有限
  （含 1×1 与极端长宽比，避免 NaN 毒化整张卡）、`EdgeWidth = 0` 是否干净地关掉透镜、
  **对齐偏移没有被重复应用**、`GrContext` 守卫在位且在创建着色器之前、
  手写插值仍然在位。实际像素必须在有 GPU 的机器上看。
- **CPU 光学回归全部保持**：`OpticsProfile` / `SoftGlowProfile` 的透镜剖面、边缘描边宽度、色散、
  鲜度、霜度、染色晕染、染色扩散全部通过；并新增「出厂液态玻璃光学就是清脆配方」与
  「柔光配置迁移后仍然选中柔光配方」两条断言。

### 38.7 已知取舍

- **柔光配方目前是离散选择。** 柔光晕 / 光谱弥散从 0 抬到 1 会同时切换月牙宽度（×1.55）、
  位移（×0.45）与描边宽度（×7），也就是「一个旋钮同时当选材开关」。合并后的默认（两者都是 0）是
  macOS 清脆配方，历史柔光配置迁移后原样保留；但中间地带不存在。要改成连续过渡需要重新调参并
  重做 `SoftGlowProfile` 的基线。
- **GPU 输出无法离线验证。** 光栅后端会直接终止进程，所以本地只能做「编译 + uniform 绑定」级别的校验；
  折射、色散、边缘高光、染色晕染的实际观感需要在装有 GPU 的机器上确认。
- **`Progman` 之外的宿主是尽力而为。** WorkerW 链覆盖 Wallpaper Engine 的桌面模式；
  若用户用的是「独立窗口」型壁纸（Lively 的 window 模式等），需要 DXGI Desktop Duplication 才能抓到，
  那会引入新依赖，并且必须给自己的窗口设 `WDA_EXCLUDEFROMCAPTURE` 以免自采样反馈——本次没有走这条路。

### 38.8 版本号

- 主程序 `AssemblyVersion`：1.9.8 → **1.9.9**。

### 38.9 GPU 材质的设备端自检与自动降级（1.9.9 实测追加）

GPU 路径在实机上**始终不可见**，排查结论分三步：

1. **`SKRuntimeEffect.Create` 通过 ≠ 设备端能用。** 前者只做 SkSL→IR 解析；真正的设备程序在**绘制时**
   才编译，且要降级成 **ES2 兼容方言**——一个窄得多的子集。失败时**不报错，只是什么都不画**。
   本机光栅后端一绘制就整进程崩，所以这条路径**离线完全无法验证**。
2. **`out` 参数是该方言不接受的构造**，且有**两处**：`bevelAt(... out float2 n, out float depth)` 与
   `rgbToHsl(... out float hh, out float ss, out float ll)`。分级探针给出直接证据：
   `d1`（去掉 out）出颜色、`d2`（同样逻辑改回 out）返回 `#00000000`。两处都已改为返回值
   （`float3 bevelAt(...)` 打包 xy=法线 z=深度；`float3 rgbToHsl(...)`）。
3. **改完两处之后完整模型仍然不可见**，而把七抽头光谱与四抽头手写双线性都拿掉（`e3`）依然不可见
   —— 说明卡点是**某个构造**而不是采样数量。剩余嫌疑集中在「声明两个 child shader」与「全局 `const`」，
   探针新增 `f1`/`f2` 两级继续定位。

**因此增加了设备端自检 + 自动降级**（这是本次真正的兜底）：

```
第一次 GPU 绘制 → 把材质渲染到离屏 GPU 表面并读回中心像素
  有颜色 → 判定可用，继续走 GPU
  全透明 → 判定不可用（设备程序没建起来）→ 永久切回 CPU 渲染器并重新渲染
```

```csharp
if (Interlocked.Exchange(ref selfTested, 1) == 0 && !RendersOpaque(lease, shader, destination))
{
    Interlocked.Exchange(ref gpuState, 2);
    GlassDiagnostics.Event("selftest: the GPU material renders empty — switching to the CPU material");
    ...
}
```

这样即使设备端编译失败，用户拿到的仍是**可用的玻璃**（CPU 渲染器），而不是一张空白卡片；
在 GPU 可用的机器上则自动享受 GPU 路径。本机日志确认降级生效：`selftest: alpha 0` 之后全部转为
`published (cpu)`。

**排查用的痕量日志**保留在 `%LocalAppData%\uWidgets\glass-debug.log`（上限 800 行，
记录发布/丢弃/异常/自检/分级探针结果）。这是 GPU 路径唯一可用的诊断通道，因此随发布保留。





## 39. 1.9.10：GPU 液态玻璃真正可用（Skia 内联器 + 斜面坐标两处根因）与内存回归

v1.9.9 交付时 GPU 路径**从未在设备上画出过一帧**：自检判定设备端编译失败后永久回退 CPU，
因此着色器里的视觉错误也无从暴露。本版把它跑通，并修掉随之暴露出来的内存问题。

### 39.1 需求

1. 让 GPU/SkSL 路径真正可用，而不是永远降级到 CPU 回退；
2. 修复液态玻璃主题下内存占用极高的问题。

### 39.2 根因一：Skia 内联器不保证内联「含子着色器采样」的函数

`sample(child, coord)` 会被 Skia 展开成对子 fragment-processor 链的调用，并以**入口点的**
输入参数 `_input` 作为实参。这要求该调用最终落在 `main` 内；一旦它留在一个**没有被内联**的
函数里，设备端程序就会在缺少 `_input` 的函数中引用 `_input`，编译失败且**不报错**——
结果是一张**全透明的卡片**。

用 `tests/GlassGpuProbe`（真机 ANGLE/EGL 窗口 + 离屏读回）二分测出内联器的实际形状：

| 辅助函数形态 | 调用 1 次 | 多次 |
|---|---|---|
| 4 tap 双线性（多语句） | 内联 | **2 次即不内联 → 全透明** |
| 12 tap（多语句） | 内联 | **2 次即不内联 → 全透明** |
| 36 tap（多语句） | 内联 | — |
| 单次采样（1 tap） | 内联 | **40 次仍内联** |
| 含提前 `return` 且采样 | **不内联 → 全透明** | — |

真实 shader 的 `sampleRgb` 有 4 个 tap 且被 `main` 调用约 10 次，`sampleAura` 同类——
两者都超出预算，于是一直是全透明。**不能依赖这个启发式**，因此改成构造上就安全的形式：

- 所有内容采样用 `@fetch(target, coord)` / `@fetchAura(target, coord)` 占位符写在 `main` 内，
  编译前由 `LiquidGlassGpuEffect.Expand` 展开为 4 tap 双线性块；
- 只保留 `contentTexel` / `auraTexel` 两个**单次采样**辅助函数（实测 40 个调用点仍内联）。

`GpuMaterialCheck` 新增源码级守卫：断言每个含 `sample()` 的函数要么是 `main`，要么只含
**一次** `sample()`，并断言 `@fetch` 已全部展开（37 处 texel 查找）。

> 为什么不用 GPU 双线性省掉 4 tap：SkiaSharp 2.88 的 `SKBitmap.ToShader`、
> `SKShader.CreateImage`（含带矩阵重载）**全部是最近邻**，没有 `SKSamplingOptions` 重载，
> 实测（`GlassGpuProbe` 的 `ReportChildSampling`）三种构造方式都是 NEAREST。手写双线性必须保留。

### 39.3 根因二：斜面（bevel）坐标多减了一次半宽

`LiquidGlassRenderer.BevelField.Evaluate(x, y)` 接收的是**卡片绝对坐标**（内部自己减半宽），
CPU 侧调用为 `Evaluate(x + 0.5f, y + 0.5f)`。而 shader 里是：

```glsl
float2 p = rxy - h;                 // 已经是中心相对坐标
float3 bevel = bevelAt(p, h, radius);   // 内部又 v = p - h，减了第二次
```

于是整张法线/深度场偏移了半个卡片：中心 `depth` 为负、法线方向错乱，渲染成
**一片平涂 + 右下角一块折返的深色圆角矩形**。着色器此前从未真正绘制过，所以这个错误一直没被发现；
应用内诊断分级探针 `d1` 是照着同一处错误约定写的，因此只证明了「能画出像素」，没能证明「画得对」。

修复：`bevelAt(rxy, h, radius)`，并同步修正诊断探针的 `MaskStage` / `MaskOutParamStage`。

**验证**：`tests/GlassGpuProbe` 新增 `Compare`——同一帧分别用 GPU 与 CPU 渲染器渲染并逐像素比对，
导出 `dist/glass-gpu-compare/{cpu,gpu,backdrop,aura}.png`。

| | 平均 \|Δ\|（/1020） | 中心像素 |
|---|---|---|
| 修复前 | **393.67** | CPU `#2a3e31` / GPU `#d7f3e2`（平涂薄荷色） |
| 修复后 | **23.13** | CPU `#2a3e31` / GPU `#223629` |

剩余差异来自两条路径的背景纹理本就不同（GPU 采样共享的降采样桌面纹理，CPU 在卡片分辨率重建壁纸），
并非几何或采样分歧。

### 39.4 根因三：桌面抓取快照按固定 3 秒宽限期保留

`LiquidGlassWallpaper` 每次采样都新建一张**整个虚拟桌面物理分辨率**的位图（2560×1440×4 ≈ 14 MB），
旧的那张则交给 `Retire()` 延迟 **3000 ms** 再释放。默认 10 fps 下这意味着**同时存活约 30 张
≈ 420 MB**。这正是「液态玻璃主题内存占用极高」的主因。

修复：`WallpaperSnapshot` 改为**引用计数**（新增 `WallpaperLease`），去掉定时器宽限期：

- `Get()` 交给调用方一个引用，调用方负责 `Dispose()`（5 个调用点全部改为 `using`）；
- `CaptureOnce` / `FromFileFallback` 只释放**缓存自己**那一个引用，仍在渲染的帧不受影响；
- `Bitmap` 从位置参数改为 `init` 属性上的**租约引用**，这样 `with` 复制的是租约引用而不是裸位图——
  否则副本会各自持有同一批像素并**双重释放**；预览用的 `with { Style, Tile }` 改走 `WithPlacement()`，
  由副本自己 AddRef。

### 39.5 根因四：绘制操作不持有它要画的位图（释放后使用 → 访问违规）

GPU 路径一旦真正启用，应用随即以 `0xC0000005`（访问违规）崩溃，栈顶是
`SKBitmap.ToShader → LiquidGlassGpuEffect.Create`。

原因：Avalonia 会把 `ICustomDrawOperation` 留在合成树里并在**之后的帧**重新调用它，而
`LiquidGlassSurface` 只用一个 250 ms 的延迟释放来「大概保证」位图还活着。这个保证不成立——
首个 GPU 帧的 backdrop 会被回收，而队列里的绘制操作仍指向它，于是 Skia 内部踩空。

修复：`Prepared` 改为**引用计数**，两个绘制操作
（`GlassDrawOperation` 与新增的 `ImageDrawOperation`）在构造时 `AddRef`、在
`ICustomDrawOperation.Dispose()` 中释放；`RetirePrepared` 立即释放自身引用，**不再有定时器**。
CPU 回退路径也改走自定义绘制操作（`ImmediateDrawingContext` 没有 `DrawImage`，故直接用
`lease.SkCanvas.DrawBitmap`），顺带把 CPU 材质的 **PNG 编码/解码往返整条去掉**
（`LiquidGlassRenderer.RenderBitmap` 直接返回位图，`Render` 保留编码形式供离线比对）。

### 39.6 回归

- `src` 与 22 个测试套件：**20 套全绿**；`MapAndTitleBarChecks`（2 项）与
  `SingleInstanceChecks`（1 项）仍是既有失败（v1.9.8 上同样存在，与本次改动无关）；
- `LiquidGlassChecks` 的 `GpuMaterialCheck` 新增内联器不变量与 `@fetch` 展开断言；
- `GlassGpuProbe` 在真机上 **0 失败**：真实着色器在 crisp / soft+染色场 / soft 无染色场 /
  EdgeWidth=0 / 1×1 退化几何下均绘制成功；三个「必须为空」的反向对照
  （4 tap 调用 2 次、12 tap 调用 2 次、提前 return + 采样）如实返回全透明；
- 实测内存（同一套 8 个组件、液态玻璃 + 实时采样 100 ms）：

| | 4 s | 8 s | 之后 |
|---|---|---|---|
| 修复前 | 468.8 MB | 591.1 MB | **崩溃**（0xC0000005） |
| 修复后 | — | — | 稳定 ~150–200 MB 工作集 / ~310 MB 私有，44 s 无异常 |

  毛玻璃（Acrylic）基线为 ~158 MB 工作集 / ~312 MB 私有，即液态玻璃现已与基线持平。

### 39.7 已知取舍

- `tests/` 在 `.gitignore` 中被忽略（`/tests/`），只有先入库的 21 个文件受版本控制。
  **`tests/GlassGpuProbe` 目前未被跟踪**——它是 GPU 路径唯一的真机验证手段，建议显式 `git add -f`。
- GPU 路径仍需真机验证：设备端编译在绘制时才发生，离线测试只能守住源码级不变量。


## 40. 1.9.11：实时采样回合共享修复（3 ms 档位 4 fps → 18 fps）

### 40.1 需求

用户报告：实时采样间隔调到 3 ms 仍然一顿一顿。实测确认，而且方向是反的 ——
**间隔越小越慢**：6 个组件下 3 ms 只有 24 publishes/s（每组件 4 fps），
100 ms 反而是 60/s（每组件 10 fps）。

### 40.2 根因：一次采样回合没有被共享

一个回合的成本是「抓取桌面（2560×1440，16 ms）+ 构建共享背景（降采样+模糊，31 ms）」
≈ 47 ms，设计上**无论多少个组件都只该付一次**。三处缺陷让它退化成每个组件各付一次：

1. **抓取帧的缓存新鲜度窗口等于用户间隔。** `CaptureOnce` 用
   `captureExpiresTicks = now + liveIntervalMs` 判断帧是否还可用。3 ms 窗口下，
   先到的组件抓完帧，几毫秒后到的组件已判定过期 —— 各自重抓一次桌面并各自重建
   一次全桌面背景，共享比退化为 1:1。
2. **tick 在组件忙时仍然 invalidate。** 忙的组件不会被 `RefreshAllImmediate` 触及，
   但 `cached` 已被清空，它们稍后取帧时缓存已空，又各自抓一次。同时
   `framePending` + `MinRetryMs = 250` 的 retryFloor 会把采样器按在 4 回合/秒上 ——
   这正是「间隔调小反而更卡」的直接来源。
3. **间隔从「上次抓取」而非「回合开始」起算。** 抓取发生在回合开始后几毫秒，
   于是下一次 tick 总被判「太早」而跳过，周期翻倍（此条是修复过程中引入并修正的：
   100 ms 档位一度从 60/s 掉到 30/s）。

### 40.3 修复

- **抓取帧不再按定时器过期**：一直有效到下一次显式 `Invalidate()`（采样 tick、
  壁纸变化、显示器变化、全屏切换）。于是一个回合无论多少个组件都只付一次抓取
  + 一次共享背景构建。
- **回合只在「所有需要玻璃的组件都不在渲染中」时开始**（`AnySurfaceRendering`）。
  保留 1 秒 staleness 兜底，保证任何情况下都不会永久停摆；删除
  `framePending` / `retryFloor` / `MinRetryMs`。
- **间隔改为回合周期的下限**，且从回合开始计时，而不是目标值。

### 40.4 实测（6 个组件，2560×1440 桌面）

| 间隔 | publishes/s | 每组件 fps | 抓取共享比 | 工作集 / 私有 |
|---|---|---|---|---|
| 3 ms 修复前 | 24 | 4 | 1 : 1 | 147 MB / — |
| **3 ms 修复后** | **108** | **18** | **1 : 6** | 219 / 362 MB |
| 100 ms 修复后 | 60 | 10 | 1 : 6 | 178 / 315 MB |

`captures` 计数直接验证共享：108 publishes/s ÷ 18 captures/s ＝ 6，即每次抓取
服务全部 6 个组件。

### 40.5 天花板与已知限制

单回合 ≈ 47 ms（抓取 16 ms + 背景构建 31 ms）就是当前实现的速率上限，即
**每组件约 21 fps**。这是本版留下的明确边界：采样间隔是一个**请求**，
实际速率由管线决定。3 ms 档位目前达到 18 fps，已接近该上限。

要把上限继续顶上去，必须压缩这 47 ms，方向见 §41：
把背景构建移出关键路径、降低背景分辨率、以及缓冲区复用（每回合新分配约 42 MB，
是 3 ms 档位私有内存 362 MB 的主因）。

### 40.6 量测内置

`GlassDiagnostics.Published` 每秒汇总一次：实际发布速率、平均/最差耗时、
抓取/背景两段耗时、累计抓取数。「卡不卡」从此是可读的数字而不是感觉。

### 40.7 回归

8 个相关套件（LiquidGlassChecks / ClockGlassChecks / ClockThemeChecks /
FullscreenChecks / WidgetStackChecks / ColorfulThemeChecks / MultiScreenGridChecks /
GalleryVisualChecks）全绿；真机 `GlassGpuProbe` 0 失败；GPU 与 CPU 逐像素对比不变
（平均 |Δ| 23.13/1020）。
